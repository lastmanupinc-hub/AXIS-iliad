node_modules/
dist/
.pnpm-store/
.pnpm-ci/
*.tsbuildinfo
.env
.env.local
*.db
*.db-wal
*.db-shm
.ai-output/
.ai-output-*
.dogfood-closer-output/
coverage/
cov*.txt
coverage-*.log
*-fail.log
axis-analyze-results.json
redundancy-sweep-report.json
redundancy-sweep-output.txt

.mcpregistry_*
*.env

*-key.pem

keys.env

# Local transient test and scratch outputs
.tmp*
.vitest*
.ai-vitest-latest.txt
vitest-api-src-output.txt

# Claude Code local session state
.claude/

# Local-only generated fintech MCP artifacts
repo-run-stats.json
mcp/fintech-mcp-surface-package.md
mcp/fintech-domain-schema.yaml

# WO-15: machine-specific perf measurement, rewritten by every vitest run
# (regenerate via `pnpm bench:compliance` or the perf-compliance test suite)
packages/generator-core/perf-results.json

# WO-16: transient tsc stage of the axis-iliad self-contained bundle
# (build.mjs removes it on success; ignore leftovers from failed builds)
apps/cli/.tsc-out/

# Root-level run logs / tool output (H0.10): never track run artifacts in a
# public repo — output logs are a classic secret-leak vector. llms.txt is a
# deliberate, curated file and stays tracked.
/coverage-*.txt
/vitest*.txt
/*-output.txt
/test_output.txt
/docker-ci-*.txt
/ls-coverage.txt
/stalling fix.txt
/*.log
/axis_toolbox_*.txt

# spoke_05 storefront build output (regenerate: node scripts/build-storefront.mjs)
.storefront-dist/
