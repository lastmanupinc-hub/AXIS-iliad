# Messaging System
# Project: axis-iliad
# Summary: axis-iliad is a monorepo built with TypeScript using React. It contains 500 files across 9 top-level directories. It defines 278 domain models.

product:
  name: axis-iliad
  type: monorepo
  primary_language: TypeScript
  stack: React

taglines:
  primary: axis-iliad — React monorepo
  technical: "React monorepo — 500 files, 108,805 lines"
  conceptual: "Built around apps/ (monorepo_apps), docs/ (documentation), packages/ (monorepo_packages)"

value_propositions:
  - id: stack
    headline: React Expertise
    detail: Built with React ^19.1.0 — stack-native patterns throughout.
  - id: api_surface
    headline: "174 API Endpoints"
    detail: "Complete API with 102 GET, 65 POST, 1 PATCH, 6 DELETE — ready for integration."
  - id: domain_model
    headline: "278 Domain Entities"
    detail: "Rich domain model with AlertThresholds, Counters, DebounceState, WindowResult, AnalyticsCountByBucketResult and 273 more."
  - id: quality
    headline: "Test-Driven Quality"
    detail: Tested with vitest across 177 test files.

feature_messages:
  api_surface:
    count: 174
    message: "174 API endpoints ready for integration"
    routes:
      - GET /health
      - GET /v1/health
      - POST /v1/accounts
      - GET /v1/account
      - PATCH /v1/account
      - DELETE /v1/account
      - POST /v1/snapshots
      - GET /v1/admin/stats
      - GET /v1/admin/accounts
      - GET /v1/admin/activity
  language_support:
    count: 10
    message: "10 languages detected and analyzed"
    breakdown:
      - "TypeScript: 73,592 lines (72%)"
      - "YAML: 12,895 lines (12.6%)"
      - "Markdown: 8,954 lines (8.8%)"
      - "JSON: 2,991 lines (2.9%)"
      - "JavaScript: 1,815 lines (1.8%)"
      - (+5 more languages)
  framework_detection:
    count: 1
    message: "1 frameworks detected with stack-aware output"
    detected:
      - name: React
        version: "^19.1.0"
        confidence: 0.95

# calls_to_action are starter placeholders — replace with your product's real CTAs.
calls_to_action:
  primary:
    label: "Get Started"
    context: "Main landing page, empty states"
  secondary:
    label: "See How It Works"
    context: "Feature sections, docs"
  api:
    label: "Read the API Docs"
    context: "Developer documentation, API reference"

# Source-derived messaging
package_description:
  value: "Axis' Iliad - one API call that turns any codebase into 142 deterministic AI-agent-ready artifacts (AGENTS.md, CLAUDE.md, design tokens, Visa CE 3.0 compliance kit, MCP configs, and more)"
