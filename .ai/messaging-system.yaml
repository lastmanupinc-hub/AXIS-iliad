# Messaging System
# Project: axis-iliad
# Generated: "1970-01-01T00:00:00.000Z"
# Summary: axis-iliad is a monorepo built with TypeScript using React. It contains 500 files across 16 top-level directories. It defines 242 domain models.

product:
  name: axis-iliad
  type: monorepo
  primary_language: TypeScript
  stack: React

taglines:
  primary: axis-iliad is a monorepo built with TypeScript using React. It contains 500 files across 16 top-level directories. It defines 242 domain models.
  technical: "React monorepo — 500 files, 115,124 lines"
  conceptual: "Built around apps/ (monorepo_apps), packages/ (monorepo_packages), docs/ (documentation)"

value_propositions:
  - id: stack
    headline: React Expertise
    detail: Built with React ^19.1.0 — stack-native patterns throughout.
  - id: api_surface
    headline: "163 API Endpoints"
    detail: "Complete API with 92 GET, 66 POST, 5 DELETE — ready for integration."
  - id: domain_model
    headline: "242 Domain Entities"
    detail: "Rich domain model with AlertThresholds, Counters, DebounceState, WindowResult, AnalyticsCountByBucketResult and 237 more."
  - id: architecture
    headline: Clean Architecture (0.65 separation)
    detail: "monorepo, containerized with 1 layer boundaries."
  - id: quality
    headline: "Test-Driven Quality"
    detail: Tested with vitest across 189 test files.

feature_messages:
  api_surface:
    count: 163
    message: "163 API endpoints ready for integration"
    routes:
      - GET /v1/health
      - POST /v1/accounts
      - POST /v1/snapshots
      - GET /v1/admin/stats
      - GET /v1/admin/accounts
      - GET /v1/admin/activity
      - GET /v1/admin/mcp-usage
      - GET /v1/admin/revenue
      - GET /llms.txt
      - GET /.well-known/skills/index.json
  language_support:
    count: 8
    message: "8 languages detected and analyzed"
    breakdown:
      - "TypeScript: 89,597 lines (80%)"
      - "YAML: 10,597 lines (9.5%)"
      - "Markdown: 6,295 lines (5.6%)"
      - "JavaScript: 2,273 lines (2%)"
      - "JSON: 1,922 lines (1.7%)"
      - (+3 more languages)
  framework_detection:
    count: 1
    message: "1 frameworks detected with stack-aware output"
    detected:
      - name: React
        version: "^19.1.0"
        confidence: 0.95

calls_to_action:
  primary:
    label: "Upload Project"
    context: "Main landing page, empty states"
  secondary:
    label: "View Results"
    context: "After snapshot processing complete"
  api:
    label: "Send Snapshot via API"
    context: "Developer documentation, API reference"
