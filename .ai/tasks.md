# axis-iliad — Implementation Tasks

Tasks are ordered by phase. Check items off as they land. New tasks land at the bottom of their phase — don't re-order completed items.

## Phase 0 — Foundations

- [ ] README with one-line description, install, run, and contributing instructions
- [ ] LICENSE file at repo root
- [ ] Lockfile committed (`pnpm/yarn/npm/bun`)
- [x] Test runner configured (vitest)
- [x] CI pipeline runs on every PR (github_actions)
- [x] Container build (Dockerfile + docker-compose for local validation)

## Phase 1 — Core Domain

- [x] 252 domain models defined
  - `AuthContext` (interface, 3 fields, `apps/api/src/billing.ts`)
  - `EnvSpec` (interface, 5 fields, `apps/api/src/env.ts`)
  - `ValidationError` (interface, 2 fields, `apps/api/src/env.ts`)
  - `ValidationResult` (interface, 3 fields, `apps/api/src/env.ts`)
  - `ZipEntry` (interface, 4 fields, `apps/api/src/export.ts`)
  - `PullRequestPayload` (interface, 5 fields, `apps/api/src/github-webhook.ts`)
- [ ] Pure-function business logic with no I/O side effects
- [ ] Validation at every system boundary (input parsing, deserialization)

## Phase 2 — Entry Points & API Surface

- [x] 497 HTTP routes registered
  - `GET /v1/health` — `apps/api/src/admin.test.ts`
  - `POST /v1/accounts` — `apps/api/src/admin.test.ts`
  - `POST /v1/snapshots` — `apps/api/src/admin.test.ts`
  - `GET /v1/admin/stats` — `apps/api/src/admin.test.ts`
  - `GET /v1/admin/accounts` — `apps/api/src/admin.test.ts`
  - `GET /v1/admin/activity` — `apps/api/src/admin.test.ts`
  - `GET /llms.txt` — `apps/api/src/agent-discovery.test.ts`
  - `GET /.well-known/skills/index.json` — `apps/api/src/agent-discovery.test.ts`
  - …and 489 more
- [ ] Each endpoint has at least one integration test covering the happy path + one error path
- [ ] Error responses follow a single canonical shape (status code + machine-readable code + human message)

## Phase 3 — UI

- [ ] Root layout / App shell with global error boundary
- [ ] Theme tokens wired through (design-tokens.json → CSS variables)
- [ ] Loading + empty + error states on every async surface
- [ ] Keyboard-navigable on every interactive element
- [ ] Lighthouse / Web Vitals budget defined and enforced in CI

## Phase 4 — Operations

- [x] Container build documented and reproducible
- [ ] `/health` endpoint exposes liveness + readiness
- [ ] Structured logs (JSON lines) with request IDs
- [ ] Metrics endpoint (Prometheus / OpenMetrics format)
- [ ] Runbook for the top 3 alert conditions
- [ ] Graceful shutdown handler (SIGTERM drain timeout)

## Phase 5 — Hardening

- [ ] No lockfile found — dependency versions may be inconsistent

## Phase 6 — Release & Distribution

- [ ] Version bump strategy documented (semver / calver)
- [ ] CHANGELOG.md updated for every release
- [ ] Release workflow attests the build and publishes to the registry
- [ ] Rollback procedure tested at least once before the first paid customer

---

_Generated by Axis Artifacts. Tasks marked complete are inferred from snapshot signals — verify each on first read._