# Deploy Qualification Report — axis-iliad

Generated by AXIS `deploy` program. The emitted artifacts are intended to pass Render's existing-image and Cloudflare's wrangler-driven deploy qualifications with minimal setup — set your image owner (`REPLACE_OWNER`/`<owner>`), confirm the entrypoint, and install the worker dependency where flagged below.

**Targets:** Render (`runtime: image`) · Cloudflare Containers  •  **Image:** `ghcr.io/<owner>/axis-iliad:prod`  •  **Port:** 8080  •  **Healthcheck:** `/health`

**Summary:** 7 pass  /  2 warn  /  0 fail

| Check | Status | Note |
|---|---|---|
| Stack detected | PASS | Detected 'node-server' (package manager: pnpm) — Dockerfile template matches. |
| Monorepo entrypoint | WARN | Monorepo detected. One Dockerfile can't know which app is the deployable service — the build runs at the repo root and CMD assumes ./dist. Point WORKDIR/COPY/CMD at your app (e.g. apps/<service>) before pushing. |
| Healthcheck route | PASS | Detected route /health — render.yaml healthCheckPath is set to it, so Render's probe will pass. |
| Honors $PORT env | PASS | Server reads PORT from env. |
| Image tag convention | PASS | Push to ghcr.io/<owner>/axis-iliad:prod — render.yaml expects this exact path. |
| Non-root runtime user | PASS | Dockerfile drops to non-root before CMD. |
| Build-context .dockerignore | PASS | deploy/Dockerfile.dockerignore is read by BuildKit for this Dockerfile — the ignore for this build context. |
| Cloudflare target match | PASS | Detected 'node-server' → recommended Cloudflare target is Containers. Use deploy/wrangler.containers.toml; ignore the other. |
| @cloudflare/containers dependency | WARN | deploy/worker.ts imports @cloudflare/containers. Install with: npm i @cloudflare/containers |

## Zero-pipeline-minutes flows

### Render (existing-image)
1. `docker build -f deploy/Dockerfile -t ghcr.io/<owner>/axis-iliad:prod .` — your machine does the work.
2. `docker push ghcr.io/<owner>/axis-iliad:prod` — GHCR storage only, no Actions minutes.
3. Render pulls the image when you click Manual Deploy (or auto on push if you flip `autoDeploy: true`). No Render build pipeline runs.

### Cloudflare Containers (backend, wrangler-driven)
1. `npm i @cloudflare/containers` once.
2. `./deploy/deploy-cloudflare.sh containers` (or `.ps1`) — wrangler runs your local Docker against `deploy/Dockerfile`, pushes the image to CF's managed registry, and binds the Worker to it. No CF build minutes used.

## If any check is WARN/FAIL

- **No /healthz route:** add a handler that returns HTTP 200 on `GET /healthz`. Without it the first deploy will appear to hang then fail health.
- **Hardcoded port:** read `process.env.PORT || 8080` (Node) / `os.environ.get('PORT', 8080)` (Python) / `os.Getenv("PORT")` (Go) before passing to your listener.
- **Unknown stack:** the default Node template will likely build, but review CMD and EXPOSE manually.
- **Missing `@cloudflare/containers`:** required by `deploy/worker.ts`. Run `npm i @cloudflare/containers` before `wrangler deploy`.

Once everything is PASS, the deploy is direct-portable — push once, deploy to Render and Cloudflare with zero platform pipeline minutes.


---

## ⟳ Continue the loop

- **You are here:** `deploy/deploy-qualification-report.md` — agent step 71 of 71.
- **▶ The loop closes here.** The artifact sequence is complete. Re-enter the loop now: **begin** (re-read `begin.yaml`).
- **Stop only when:** `continuation.yaml` has no open candidates, a human stops you, or you hit a decision only the human can make.
