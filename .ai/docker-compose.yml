version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: closer-app
    ports:
      - "8080:8080"
    environment:
      - PRODUCT_NAME=axis-iliad
    restart: unless-stopped