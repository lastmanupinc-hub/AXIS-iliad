# axis-iliad — Design Doc

Architecture, data flow, and UX decisions for axis-iliad. Use this as the source of truth when proposing changes that cross module boundaries.

## Tech Stack

- **Language**: TypeScript
- **Frameworks**: React
- **Build tools**: vite, make
- **Test runner**: vitest
- **CI**: github_actions
- **Deploy target**: docker

## Architecture

**Patterns detected:**
- monorepo
- containerized

**Layer boundaries:**
- `[object Object]`

**Separation score**: 0.65/100 — low separation; consider extracting a stable interface layer.

## Top-Level Layout

| Path | Purpose | Files |
|------|---------|-------|
| `apps/` | monorepo_apps | 153 |
| `packages/` | monorepo_packages | 144 |
| `examples/` | project_directory | 17 |
| `mcp/` | project_directory | 16 |
| `payment-processing-output/` | project_directory | 16 |
| `packaging/` | project_directory | 7 |
| `.github/` | project_directory | 5 |
| `algorithmic/` | project_directory | 4 |
| `artifacts/` | project_directory | 4 |
| `brand/` | project_directory | 4 |
| `canvas/` | project_directory | 4 |
| `debug/` | project_directory | 4 |

## Data Flow

Requests enter via 497 HTTP routes and fan out to domain logic backed by 248 typed models.

```
  Client
    │
    ▼
  HTTP entry (497 routes)
    │
    ▼
  Domain layer (248 models)
```

## UI / UX Decisions

- **Component model**: React function components. No class components.
- **Accessibility**: WCAG 2.1 AA on primary flows. Every interactive element ships keyboard-navigable.
- **State**: prefer co-located state and URL-driven routing; introduce a store only when state spans 3+ unrelated components.
- **Loading & error states**: every async surface ships explicit loading + error UI, no silent fallbacks.

## Key Decisions

- TypeScript strict mode
- Linter configured
- Formatter configured
- Makefile build

## Open Questions

- _Authentication and authorization model — bearer tokens, OAuth, session cookies?_
- _Caching strategy — in-process, Redis, CDN edge?_
- _Observability stack — logs / metrics / traces backends?_

---

_Generated by Axis Artifacts from snapshot `3ebb4d4b-c362-4689-8bd8-45b110a5da49`. Re-run after every structural change._