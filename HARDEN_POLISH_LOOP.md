# HARDEN & POLISH LOOP — Convergent System-Hardening Guide (Sonnet 5 execution)

**BUILD COMMAND: `continue`** — when the operator sends `continue` (any casing, with or
without other text), the executing model MUST: read this file top-to-bottom → ground-truth
the STATE table against `git log`/`git status`/CI (never trust the table over reality) →
execute the next pending unit(s) → verify → commit → push → update STATE → keep going until
the session ends or the CONVERGENCE test passes. Do not ask permission between units; the
only stops are the ESCALATION list. This document is the single source of truth for the
loop; edit it in place as you work.

**Mission:** close every known gap in the current system, polish the experience for both
human users and AI agents, and keep re-auditing until two consecutive audit cycles find
nothing new — at which point the platform tree and ROI candidate list (Phase T) are the
deliverables that declare the infrastructure ready for new verticals.

**Written by:** Fable 5, 2026-07-11, from a full 5-day verification with three adversarial
review agents + live systems checks. Every gap below carries receipts (file:line or live
probe). Executor: Sonnet 5.

---

## Resume protocol (context resets are normal — plan for them, don't fight them)

A single session cannot hold this whole plan's context. The STATE table + PROGRESS ledger
in THIS file are the resume state; work is resumable at any commit boundary.

- **On every session start / `continue`:** read this file → reconcile STATE against
  `git log`/`git status`/CI (reality wins) → find the top `in-progress` unit (or, if none,
  the top `pending` unit not behind a gate) → resume there. Never re-plan from scratch;
  never redo a unit whose commit exists.
- **Checkpoint discipline:** commit + STATE update + one PROGRESS line at EVERY green
  acceptance — a checkpoint is the unit of survivable progress. Never hold >1 unit of
  uncommitted work.
- **When context is getting heavy** (you notice re-reading files you already read,
  summarized history, or degraded recall): finish the smallest verifiable step of the
  current unit, checkpoint it, mark the unit `in-progress (checkpointed at <what>)` in
  STATE, and END the session cleanly. A deliberate checkpoint beats a degraded push —
  quality of the last commit is worth more than one more unit.
- **End-of-session report:** before stopping, output a short summary — units completed
  (with hashes), the unit in progress and its exact resume point, and any BLOCKED items.

## 0 · Operating rules (the constitution — read every session, violations get reverted)

These encode this repo's standing law plus the exact failure modes observed during the
July build program. They are not suggestions.

1. **Ground truth is git + live probes, never docs or memory.** First act of every
   `continue`: `git log --oneline -15`, `git status --porcelain`,
   `gh run list --repo lastmanupinc-hub/AXIS-iliad --branch main --limit 3`. Reconcile the
   STATE table to reality before doing anything else.
2. **One unit per commit; tree clean as your final act.** Two work-order agents in July
   finished coding and never committed. `git status --porcelain` must print nothing when a
   unit is done. Disclose every deviation from a unit's spec in the commit body.
3. **Verification before push, always in this order:** typecheck (`npx tsc -b --force` in
   the touched app/package) → scoped vitest suites for what you touched → the guard suites
   (`counts-consistency`, `count-honesty`, `launch-claims`, `strategic-docs-honesty` when
   any doc/copy/count changed) → `pnpm run build` (full monorepo) → push.
   Postgres-backed tests need `docker start axis-test-pg` and
   `DATABASE_URL=postgres://postgres:postgres@localhost:5433/axis_test`.
   **Scope = grep, not memory:** "suites for what you touched" means every test file that
   references any FUNCTION or export you changed — find them by grepping the changed
   symbol names across `*.test.ts`, not by recalling which files you think cover it. (A
   changed `settleOverageCash` behavior once shipped past a locally-green run because the
   dedicated wallet suite referenced it under names the author didn't grep; CI caught it.)
4. **Push = deploy.** Pushing `main` auto-deploys the API (Render) and, via CI's deploy-web
   job, the web app (Cloudflare Pages) — **web only ships if CI is green** (a red CI once
   silently froze web deploys for 4 days). After every push: confirm both workflows green
   and spot-probe live (`curl -s https://axis-api-6c7z.onrender.com/v1/health`).
5. **Run scoped suites locally, not the full monorepo run** (it stalls under load on this
   machine). CI is the authoritative full run. Known load-flaky suites are listed in H1 —
   a failure there is not automatically your regression; re-run isolated before diagnosing.
6. **Sequential execution in this one checkout.** No parallel worktrees (July's Phase-2
   merge conflicts). If you spawn subagents, they read; you write.
7. **Money-path law:** PAI'D is the ONLY checkout (never resurrect `POST /v1/checkout`);
   customer-facing payment copy says PAI'D Payments Intelligence; validate-first ordering
   (caps before charges) must never regress; consumeUsageCredits records usage, never money;
   cash truth lives only in `payment_receipts`. **Red before green, demonstrated:** for
   every money-path defect fix (all of H0.1–H0.6, H2.*), write the reproducing test FIRST
   and prove it fails against the pre-fix code (temporary local revert or pre-fix run —
   never committed), then fix; the commit body states how red was demonstrated.
   **Kill-switch proof:** any flag-gated or dark-launched behavior ships a test proving
   the flag-OFF path still produces the exact previous behavior — "kill-switchable" is a
   demonstrated property, not a design intention.
8. **Web law:** HttpOnly `axis_session` cookie auth only (localStorage holds only the
   `__cookie_session__` marker); counts route through `apps/web/src/config.ts` or live API;
   no new npm deps; no class components; every `href="#..."`/hash assignment must match a
   pattern in `routes.tsx`; no dead/fake UI, no fabricated stats, no simulated streaming.
9. **Honesty law:** a claim ships only when its acceptance test is green; external-gated
   features self-downgrade to `configured:false`; never cite unmeasured latency numbers;
   Visa IC 200-800ms is always "published industry range, not measured here".
10. **Secrets law:** credentials come from `key.txt` or Render env, used in-process, never
    printed, logged, or committed. When a label is ambiguous, stop and ask (escalation).
11. **HOLDS (do not touch without a new, explicit owner instruction):** everything under
    `docs/github-app-plan/` (GitHub App/Marketplace); npm publish of the `axis-iliad` CLI;
    accepting any legal agreement; entering legal-entity/KYC data anywhere; switching
    Iliad's live charge traffic to the new Stripe connected account (`acct_1Ts5YxDwUJERAuEd`
    — created and logged, cutover is an owner decision).
12. **When blocked on an owner-only item:** never wait. Append it to the PENDING-OWNER
    table (Phase T file), pick the next unit, move on.
13. **Model-cascade law (how this loop itself spends tokens):** every tier maximizes the
    tier below it. The frontier model wrote this strategy; the executor (Sonnet) implements
    units against their acceptance criteria; when the executor spawns subagents, it picks
    the LOWEST-token-cost model that still clears the unit's quality bar — haiku-class for
    mechanical work (greps, sweeps, count syncs, checklist audits with a tight rubric),
    sonnet-class for implementation and review, frontier only when a unit has failed twice
    at the current tier (escalate ONE tier, attach the two failure transcripts as context).
    Verification of a unit runs at-or-above the tier that implemented it, never below.
14. **Process guards (anti-rabbit-hole):** WIP = 1 — exactly one unit in progress at a
    time. **Stuck rule — move on, never loop:** after **2 honest attempts** at a unit's
    gate (or one full session, whichever comes first), mark it `BLOCKED(<specific
    blocker>)` in STATE, write the FAILURES ledger row, and move to the next unit; blocked
    items are re-attempted at most once more later (fresh context or one tier up per rule
    13) and are always listed in the end-of-session report. Grinding a third consecutive
    attempt on the same blocker is a constitution violation. Every fixed defect ships its
    regression test **in the same commit**. Irreversible or cross-cutting decisions get a
    one-page ADR (`docs/adr/NNN-title.md`: context / decision / consequences) before the
    code lands.
15. **Public-API compatibility law:** `/v1` REST routes, MCP tool names/argument shapes,
    and `error_code` strings are all API surface that live agents depend on. Changes are
    additive-only; a rename or removal requires a deprecation entry (changelog, once
    WO-A4 lands) and a stated compat window. Breaking silently is a HIGH-severity
    self-finding in the next audit cycle.
16. **State + staging safety:** any locally-run server or test uses SCRATCH data only —
    OS temp dirs / the session scratchpad / the dockerized `axis_test` database — never a
    live store, never a real data directory, never live credentials beyond read-only
    probes. Stage commits by EXPLICIT path (`git add <file> <file>`), never `git add .` /
    `-A`; review `git status` before every commit; anything credential-shaped in the diff
    = stop. (Operator has flagged equivalent hazards in the PAI'D repo — an un-gitignored
    smtp/email file and `PORTAL_DATA_DIR` handling; those belong to the PAI'D track:
    record in PENDING-OWNER, do not fix cross-repo from this loop.)
17. **Anti-gold-plating — what "quality" means here:** correctness + coherence + the
    unit's acceptance gate, WITHIN the unit's spec. No bonus features, no unrequested
    capabilities, no speculative abstraction. A drive-by improvement bigger than ~5 lines
    becomes a new pending STATE unit instead of riding along. Polish means the specified
    thing done excellently, not more things.

---

## STATE (the cursor — executor edits this table every unit)

| Unit | Phase | Status | Commit / evidence |
|---|---|---|---|
| H0.1–H0.10 | Hygiene | ALL DONE (H0.2 closed by H2.3) | see PROGRESS |
| H1.1–H1.4 | Reliability | ALL DONE (H1.1 deflake, H1.2 `fdfddd5`, H1.3 source-guard 30s `317095b`, H1.4 ci-mirror) | see PROGRESS |
| H2.1–H2.5 | Money path | ALL DONE | see PROGRESS |
| RT.1–RT.8 | Wave-0 red-team remediation (operator-commissioned, 7-lens adversarial workflow: 12 raw findings, 9 confirmed [2 critical + 3 high + 4 medium], 3 refuted by majority skeptic vote) | ALL DONE — all 9 confirmed findings fixed/verified (RT.8 was verification-only, no code defect) | see PROGRESS |
| ⛔ MONEY GATE | Operator checkpoint after H0+H1+H2 | **PASSED (operator directive, 2026-07-12)** — Wave 0 + full red-team remediation (RT.1–RT.8) complete; review packet below | |
| H3.1–H3.11 | Web completion | ALL DONE — A4, A5, P11, P10, P12, P9, P13, P14, P15, P16, P17 | see PROGRESS |
| H4.1 | MCP tool-description accuracy audit | DONE | see PROGRESS |
| H4.2 | Error-code catalog | DONE | see PROGRESS |
| H4.3 | _usage envelope documentation | DONE | see PROGRESS |
| H4.4 | begin-loop REST export + README mention | DONE | see PROGRESS |
| H4.5 | probe-intent quality pass | DONE | see PROGRESS |
| H4.6 | AI-agent UX (remaining) | DONE | see PROGRESS |
| H5.1 | Cross-page a11y pass — audit + tier-1 fixes | DONE | see PROGRESS |
| H5.1b | a11y follow-ups from the H5.1 audit (see Phase H5 spec) | DONE — (a)-(g) all shipped | see PROGRESS |
| H5.2 | Mobile pass on all new pages (Gate 4) | DONE | see PROGRESS |
| H5.3 | Performance (lazy-load, Lighthouse a11y ≥95) | DONE | see PROGRESS |
| H5.3b | Heading-order sweep (h1→h3 skips, 15 pages/109 headings — H5.3 finding) | DONE | see PROGRESS |
| H6.1 | Key-rotation runbook execution prep | DONE | see PROGRESS |
| H6.2 | Dependency + hygiene self-scan | DONE | see PROGRESS |
| H6.3 | Live-probe battery script | DONE | see PROGRESS |
| H7.1–H7.4 | Model-cascade productization | DONE | see PROGRESS |
| H8.1 | Unhappy-path completeness audit (14 external call sites) | DONE | see PROGRESS |
| H8.1b | Add timeout mechanisms to the 8 sites H8.1 waived, then their timeout tests | DONE | see PROGRESS |
| H8.2 | Money-math invariants (~2,087 seeded property cases) | DONE | see PROGRESS |
| H8.3 | Mutation-lite pass on the money path (78 mutants, 0 survivors) | DONE | see PROGRESS |
| H8.4 | PAI'D contract fixtures + live idempotency canary (built, not yet run) | DONE | see PROGRESS |
| H8.5 | OpenAPI ↔ router bijection test (13 doc gaps found + fixed) | DONE | see PROGRESS |
| H8.6 | Architecture-boundary fitness tests (0 violations found) | DONE | see PROGRESS |
| H8.7 | Neon data-safety drill (runbook written, drill execution is a disclosed owner action) | DONE | see PROGRESS |
| H8.8 | Deploy rollback runbook + rehearsal (git-revert path rehearsed live; Render/Cloudflare dashboard paths documented, deferred to owner) | DONE | see PROGRESS |
| H8.9 | Synthetic monitor / dead-man's switch (found+fixed a real pre-existing Cloudflare-vs-CI-IP false positive along the way) | DONE | see PROGRESS |
| H8.10 | Web security headers (CSP + HSTS + frame-ancestors, live-verified) | DONE | see PROGRESS |
| H8.11 | CI secret-scan (live + scratch-branch proven) + webhook replay-protection audit (2 real test-coverage gaps closed) | DONE | see PROGRESS |
| H8.12 | Dependabot config (weekly, grouped minor/patch) -- live-validated: real first batch of 13 PRs opened + triaged | DONE | see PROGRESS |
| A (audit) | Re-audit cycle | cycle 8 DONE (7 confirmed findings, all fixed, incl. 1 HIGH agent-facing catalog gap; 3 more disclosed with explicit fix-cost reasoning; 1 carried-forward disclosure from cycle 5 still genuinely blocked on PAI'D payload schema) — cycle 9 pending | dry-count: 0 |
| T.1–T.3 | Tree + ROI | pending | |

Convergence: **two consecutive A-cycles with zero new confirmed findings** AND all
non-blocked units done → execute Phase T → declare ready.

---

## Phase H0 · Immediate hygiene (receipts-backed gaps from the July verification)

Each unit is small, self-contained, and carries its acceptance test. Order is priority.

- **H0.1 — Wallet-rail idempotency key is wrong in both directions.**
  `apps/api/src/paid-client.ts:80-83` builds `checkoutIdempotencyKey(accountId,
  "fc-debit:"+tool)` on a 120-second bucket: distinct rapid calls to the same tool collapse
  to one key (if PAI'D dedupes → free calls), while retries get a NEW bucket after 120s.
  Fix: per-call UUID key generated at charge time, carried through retry of the SAME call
  only (mirror `packages/paid-client/src/index.ts:209`'s contract). Acceptance: unit test —
  two sequential calls same tool/account produce different keys; a retry of one logical
  call reuses its key.
- **H0.2 — Enforce-mode wallet ambiguity can double-charge across rails.**
  `apps/api/src/cashier.ts:109-116`: a non-402 wallet error (incl. the 15s timeout where
  the debit may have succeeded) falls through to `chargeMpp` — FC debit + Stripe charge for
  one call. Fix per WO-20 doctrine: ambiguous outcomes do NOT fall through; write a
  `compensation_ledger` row (`wallet_rail_ambiguous`) and fail the call payment-required.
  Depends on H2.1's table. Acceptance: mocked timeout → exactly zero mpp charges + one
  ledger row.
- **H0.3 — Enforce-mode wallet success is invisible to settled revenue.**
  `apps/api/src/cashier.ts:182-184` records `recordPaidCall` but not
  `recordSettledPayment`, so WO-19's receipts miss wallet-rail revenue. Fix: record a
  receipt (provider `paid_fc`). Acceptance: growth-store test — wallet-settled call
  appears in `settled_revenue_cents_all_time`.
- **H0.4 — Stripe API version is unpinned and the code reads pre-Basil shapes.**
  `apps/api/src/stripe.ts` sends no `Stripe-Version` header while reading
  `invoice.subscription` (:323) and top-level `subscription.current_period_start/end`
  (:307-308) — both relocated in Stripe's 2025-03-31.basil. Fix: pin
  `Stripe-Version: 2026-06-24.dahlia` on every call AND migrate the two payload reads to
  the current shapes (parent/period fields). Acceptance: webhook tests updated to dahlia
  fixture shapes; a test asserts the header on every outbound Stripe call.
- **H0.5 — checkout.session.completed resolves price from CURRENT env, not the session.**
  `apps/api/src/stripe.ts:253`. A price change between checkout and webhook mis-tiers the
  customer. Fix: read the session's own line-item/price id. Acceptance: test with env
  price rotated between create and webhook still resolves the original tier.
- **H0.6 — No Idempotency-Key on Stripe POSTs.** Checkout-session create + subscription
  update (`stripe.ts:481, :615`) send none. Fix: add per-operation idempotency keys.
  Acceptance: test asserts header present.
- **H0.7 — Three deploy-generator fixes have no regression tests** (commit `1261357`
  claimed coverage that doesn't exist): root-vs-nested `package.json` disambiguation, the
  Go `-o /out/app .` build-target fix, framework-aware Python `CMD` (gunicorn for
  Flask/Django) in `packages/generator-core/src/generators-deploy.ts`. Fix: add the three
  locks to `generators-batch5-debug.test.ts`. Acceptance: each fails against the
  pre-`1261357` behavior (verify by temporary revert locally, do not commit the revert).
- **H0.8 — `runEmbeddings` has zero direct tests** (`apps/api/src/mcp-tool-impls.ts:364-390`).
  Fix: handler-level tests mirroring `runObjectStorage`'s pattern (not-configured envelope +
  configured happy path with stubbed backend). Acceptance: both paths locked.
- **H0.9 — PlansPage static fallback renders as if live** (`apps/web/src/pages/PlansPage.tsx:71-90`).
  Fix: when `getPlans()` fails, show the fallback with an honest "showing standard pricing —
  live plans unavailable" note (Callout pattern). Acceptance: test asserts the notice
  renders only on fetch failure.
- **H0.10 — Root-debris sweep (committed output logs in a PUBLIC repo).** Nine tracked
  test/coverage output files sit at the repo root (`coverage-full.txt`,
  `coverage-output.txt`, `docker-ci-run3.txt`, `ls-coverage.txt`, `stalling fix.txt`,
  `test_output.txt`, `vitest-full.txt`, `vitest-output.txt`, `vitest_requested_output.txt`
  — verified via `git ls-files`, April–May vintage). Output logs are a classic
  secret-leak vector. Fix: scan each for credential shapes FIRST (report any hit as a
  HIGH finding + escalate for rotation); then `git rm` all nine and add root-level
  `*-output.txt` / `coverage-*.txt`-style patterns to `.gitignore`. Also sweep for any
  other tracked root artifact that is plainly a run log. Acceptance: `git ls-files` clean
  of run logs; scan results stated in the commit body. (History rewrite is NOT in scope —
  if the scan finds a real secret, that becomes an owner escalation, not a filter-repo
  adventure.)

## Phase H1 · Reliability / deflake (make green mean green)

- **H1.1 — production-startup.test.ts + rate-limiter.test.ts timeout under load** (5s
  default; they spin real HTTP servers + Postgres). Fix: per-test timeouts (30s) on the
  server-lifecycle tests, replace fixed `setTimeout` waits with event-driven waits
  (`server.once("listening")` pattern already in the file — finish the job), and serialize
  their DB resets. Acceptance: 3 consecutive full-suite CI runs green; local isolated runs
  green.
- **H1.2 — anon-analyze race** (`app-routing.test.tsx` "shows the real result immediately"
  — ProjectPage crashed reading `.length` of undefined under CI load; passed locally).
  Root-cause properly: guard `generatedFiles`/`snapshot_summary` optional chains in
  `ProjectPage` (defensive render) AND fix the test's await discipline (findBy over getBy).
  Acceptance: the exact test passes 5 consecutive CI runs.
- **H1.3 — source-guard fs-scan margins** — already bumped to 30s; extend the same
  treatment to any other fs-walking test found by grepping `readdir` in `*.test.ts`.
- **H1.4 — Full-suite local stall** — add a root `test:ci-mirror` script that runs the
  suite with CI's exact worker settings, documented in GROK-HANDOFF/this file, so local
  full runs stop being folklore.

## Phase H2 · Money-path completion (WO-20 phases 2-3 — spec in
`docs/build-plan/WO-20-charge-integrity-hybrid.md`, read it first)

- **H2.1 — `compensation_ledger` migration in `@axis/snapshots`** per the WO-20 schema,
  at the NEXT free PG_MIGRATIONS version (v34 — H0.3 took v33 for the provider-CHECK
  widening). Acceptance: migration idempotent test (memory-store.test.ts pins the current
  version — update it in the same commit; the July bug was a hardcoded stale version).
- **H2.2 — Producer: settled-then-error.** MCP dispatch catch-path writes an `owed` row
  when `isInbandSettled(req)` and the tool threw (today it only apologizes in text —
  `mcp-tool-impls.ts` "settled-then-error"). Acceptance: mocked settled+throwing tool →
  receipt row + ledger row, idempotent on retry.
- **H2.3 — Producer: wallet ambiguity** (pairs with H0.2).
- **H2.4 — Compensator: auto credit-grant** equal to `amount_cents`, retried, idempotent
  on entry_id; `_usage` gains `compensation: {owed_cents, credited_cents}`; admin revenue
  subtracts owed. Acceptance: WO-20's acceptance block, verbatim.
- **H2.5 — 402 payload schema unification.** One shape for every payment-required response
  (REST + MCP): same field names, PAI'D-only pointers, price + lite option + upgrade_url +
  usage_credits present everywhere applicable. Write the shape as a type, add a
  contract test that walks every `sendError(..., 402/429, ...)` call site. (Several July
  fixes touched these one at a time; this closes the class.)

### ⛔ MONEY GATE — mandatory operator checkpoint (the loop's ONE planned stop)

When the last unit of **Wave 0 = H0 + H1 + H2** is green and pushed: **HALT.** Do not
start H3 or any later phase. Output a **review packet**: every Wave-0 commit (hash +
one-line what/why), the files touched per commit, which units were red-green demonstrated
(rule 7), and anything BLOCKED. The operator reviews the money diffs before more is built
on top of freshly-changed billing; their next `continue` AFTER the packet unlocks H3+.
Mark the gate row in STATE `PASSED (operator, <date>)` when it happens. This checkpoint
is deliberate risk-tolerance policy, not an escalation — cheap insurance exactly where a
human look matters most.

#### Review packet (closed 2026-07-12)

Wave 0 (H0.1–H2.5) commits and outcomes are the PROGRESS ledger rows dated 2026-07-11 and
the first 2026-07-12 row (`H2.5`, `9b767d0`) — that commit's push is what originally
triggered this halt. Per operator instruction, the halt was extended to commission an
adversarial review before approval rather than a manual diff read:

- **Red-team**: operator-commissioned 7-lens adversarial workflow (double-charge,
  compensation-ledger, h25-regression, wallet-paid, stripe-webhook, deflake-collateral,
  hygiene-secrets), each finding independently verified by 3 skeptics
  (majority-must-not-refute to survive). 12 raw findings → 9 confirmed, 3 refuted.
- **3 refuted (reviewed, not fixed — majority skeptic vote found them not real)**:
  REST cashier path (`chargeWithDiscounts`) idempotency claim (handlers.ts:62,
  double-charge lens) — overlaps H0.6's Stripe-side Idempotency-Key coverage; a
  near-duplicate framing of the H2.3 catch-all issue (cashier.ts:105, wallet-paid lens) —
  the confirmed version of this (finding #3) is fixed below; a ProjectPage program-run
  success-toast claim (ProjectPage.tsx:121, deflake-collateral lens) — distinct code path
  from the confirmed generated-files finding (#8), found not to reproduce.
- **9 confirmed, all fixed and red-green demonstrated** (temporary local revert, proven
  red, restored, never committed as-reverted — the established pattern for every unit in
  this loop):

  | # | Severity | What | Commit |
  |---|---|---|---|
  | 1 | CRITICAL | Concurrent same-Idempotency-Key MCP requests both charged + both ran (no claim/reservation) | `51a65bc` |
  | 2 | CRITICAL | Wallet-debit key was randomUUID() every call — client retry became a real second debit | `9a292cc` |
  | 5 | MEDIUM | (same root cause as #2, same commit) | `9a292cc` |
  | 3 | HIGH | H2.3 catch-all recorded compensation for definite (non-ambiguous) 4xx debit rejections | `9a292cc` |
  | 6 | HIGH | Stripe price truth-fetch had no retry — one transient hiccup reintroduced H0.5's bug | `54303a5` |
  | 7 | HIGH | No event-ordering guard on subscription webhooks — a stale event could downgrade an active payer | `54303a5` |
  | 4 | MEDIUM | funnel.ts SEAT_LIMIT response missing the canonical `message` field | `6bd9e06` |
  | 8 | MEDIUM | ProjectPage silently discarded a malformed/failed generated-files load as "0 files" | `eabce68` |
  | 9 | MEDIUM | H0.10's secret-scan claim unverified for 3 UTF-16LE files (encoding blind spot) | verification-only, see RT.8 ledger row |

- **Files touched across RT.1–RT.8**: `packages/snapshots/src/{idempotency-store,index,pg-schema,memory-store,stripe-store}.ts` (+ their tests), `apps/api/src/{mcp-runtime,mcp-server,cashier,paid-client,stripe,funnel}.ts` (+ their tests), `apps/web/src/pages/ProjectPage.tsx`, `apps/web/src/app-routing.test.tsx`.
- **BLOCKED**: none. **Escalated**: none — all 9 confirmed findings were fixable within
  the existing architecture; no owner-only blocker was hit.
- **CI**: every RT commit confirmed green (CI + Compliance Check) before the next unit
  started, per rule 5 (WIP=1); RT.6–RT.8 confirmed green before this gate closure.

Per the operator's standing instruction ("when it completes make fix the negative
findings... close wave 0 and continue"), this constitutes the operator's review and
approval — the gate is marked PASSED above without a further halt.

## Phase H3 · Web completion (the remaining product surface)

Execute `docs/web-plan/BUILD-PLAN.md` WO-A4, WO-A5, then WO-P9 → P17, using
`docs/web-plan/GROK-HANDOFF.md` as the operating manual (it is model-agnostic; every rule
in it binds Sonnet equally). Sequencing: **A4+A5 first** (they unblock P16/P12), then
P11 → P10 → P12 → P9 → P13 → P14 → P15 → P16 → P17. One page per commit, acceptance
criteria are the definition of done, update the BUILD-PLAN status table as each lands
(units H3.1–H3.11 = A4, A5, P11, P10, P12, P9, P13, P14, P15, P16, P17).

## Phase H4 · AI-agent UX polish (the "customer" most of this platform serves is an agent)

- **H4.1 — MCP tool-description accuracy audit.** For each of the tools in
  `apps/api/src/mcp-tools.ts`: does the description match the implementation's actual
  behavior, arguments, pricing, and gating? Fix drift; add a test that every metered tool's
  description mentions its price source. (July found descriptions are the authoritative
  per-tool pricing disclosure — make that reliably true.)
- **H4.2 — Error-code catalog.** Every `ErrorCode.*` documented in one generated section of
  DocsPage + `/llms.txt`, with retry guidance per code (agents parse this).
- **H4.3 — `_usage` envelope documentation** on ForAgentsPage + docs: fields, when
  `_idempotent_replay` appears, what `compensation` means once H2.4 lands.
- **H4.4 — begin-loop follow-ups** (from the shipped autonomy layer): REST export of
  `begin.yaml`/`continuation.yaml` + README mention, so agent crawlers discover the loop.
- **H4.5 — probe-intent quality pass:** run 20 realistic intents, fix the worst routing
  misses (the classifier lives in `apps/api/src/intent.ts`; keep deterministic).
- **H4.6 — llms.txt freshness test:** guard that counts/urls in `llms.txt` route through
  `counts.ts` (same regime as count-honesty).

## Phase H5 · Human UX polish

- **H5.1 —** WO-P14's a11y/error-state sweep is part of H3; this unit is the
  cross-page pass AFTER all pages exist: keyboard-only walkthrough, focus states, skeleton/
  empty/error patterns audit (the four patterns from WO-F4) on every page. DONE — see
  PROGRESS for what shipped; the items below are what the audit found but this unit
  deliberately left for H5.1b (too large/structural for one commit, or needing a
  consistent cross-page pattern rather than one-off patches).
- **H5.1b — a11y follow-ups from the H5.1 audit** (all findings still real, file:line
  evidence lives in the H5.1 PROGRESS row's audit summary — re-run a quick grep to
  reconfirm line numbers before starting, since other units may shift them):
  (a) **DONE** — wired SettingsPage's 4 no-confirm destructive actions (Revoke API key,
  Remove GitHub token, Delete webhook, Revoke seat) and UsagePage's Cancel Subscription
  (new page-local `DangerButton` copy, matching the established per-page convention) through
  the arm/confirm pattern instead of firing immediately on one click — see PROGRESS;
  (b) **DONE** — `UpsellModal.tsx` now has real dialog semantics: `role="dialog"`/
  `aria-modal`/`aria-labelledby`, focus moves into the dialog on open, Tab/Shift+Tab trap
  focus inside it, Escape closes it, focus returns to the trigger on close — fixes all 4
  call sites (AnalyzePage/CommercePage/ProjectPage/RunnerPage) at once, 6 new tests — see
  PROGRESS;
  (c) **DONE** — a consistent focus-retention pattern for the remaining self-disabling
  buttons that lose focus on click: a new `useFocusRetention` hook (`apps/web/src/`, single-
  button case) applied to SettingsPage's profile-save, CommercePage's Generate/Regenerate,
  and PaidCheckoutPage's submit; a small per-row ref-registry variant (one busy flag, N
  buttons — the hook's shape doesn't fit a loop) for SettingsPage's per-program toggle and
  UsagePage's credit top-up — see PROGRESS;
  (d) **DONE** — completed the ARIA tabs pattern (roving tabindex, arrow-key nav,
  `aria-controls`/`role="tabpanel"` linking) on HelpPage/QAPage/DocsPage's section tabs and
  McpPage's `PlatformTabs`, via a new reusable `useTabList` hook (`apps/web/src/`) — see
  PROGRESS;
  (e) **DONE** — 23 of 28 pages (the re-count, including `tools/WebResearchPage`, turned up
  one more than the original 22-of-28 estimate) had zero `<h1>` anywhere in their DOM;
  promoted each page's own genuine title heading (never added a redundant hidden one) via
  a direct `<h2>`→`<h1>` tag swap, `SectionHeader`'s new `level="h1"` prop, or (WebResearchPage)
  `ToolPage`'s own promoted heading — see PROGRESS;
  (f) **DONE** — AdminPage's remaining primitives migration: all 6 tables now wrapped in
  `TableWrap` (keyboard-scrollable region) with an honest empty-row guard instead of
  silently rendering header-only — see PROGRESS;
  (g) **DONE** — wired `aria-invalid`/`aria-describedby` from the two most-affected
  forms' fields to their existing error `Callout`s: a new optional `id` prop on
  `Callout` (backward-compatible, defaults `undefined`), `PaidCheckoutPage`'s email
  input linked to `id="paid-checkout-error"` unconditionally (its error state is
  exclusively this one field's), `SettingsPage`'s webhook-events checkbox group
  (`role="group" aria-label="Webhook events"`) linked to `id="settings-error"` only
  when the shared global error's message is exactly the events-required string (that
  page's error state is shared across 5 unrelated forms, so a blanket `aria-describedby`
  would have mis-linked unrelated errors to this group) — see PROGRESS. This closes
  H5.1b in full — nothing remains disclosed-but-unfixed from the H5.1 audit.
- **H5.2 — DONE — Mobile pass** on all new pages (the plan's Gate 4). Audit (Explore
  agent, read-only) first: the IDE shell already has a working off-canvas mobile
  nav (hamburger + drawer, 900px/600px breakpoint convention) — no wholesale missing
  mobile nav, contrary to the initial assumption. Real findings, tier-1 fixed:
  (1) `StatusBar.tsx` — fixed 32px-tall bar (the height is load-bearing: `.ide-shell`'s
  `max-height: calc(100vh - var(--statusbar-h))` depends on it), fully inline-styled
  with two non-wrapping flex groups and no className hook a media query could ever
  reach — affects all 28 pages via the shared shell. Fixed by adding `className`s
  (`statusbar`, `statusbar-optional` on the two secondary-content groups) and a new
  ≤600px rule hiding `.statusbar-optional` (`!important`, required to beat the inline
  `display` these spans still carry) — the connection indicator (the one thing that
  actually matters on a phone) stays visible alone, everything still fits on one line;
  (2) `MyAnalyticsPage.tsx`'s 3 tables had no `TableWrap`, inconsistent with every
  sibling account/billing page (Admin/Usage/Settings) — wrapped all 3, matching the
  established convention exactly;
  (3) `SearchTab.tsx` (zero prior test coverage) — both result-row shapes had a
  span that could force horizontal overflow instead of truncating (text-search's
  matched-content span had `overflow:hidden;text-overflow:ellipsis` already but no
  `min-width:0` to let it actually shrink — the classic flexbox min-width:auto floor;
  symbol-search's symbol-name span had neither) — fixed both, keeping file:line
  location info fixed-width/non-shrinking in both rows (consistent with the existing
  priority the text-search row already implied: location is the stable anchor, the
  variable-length content is what truncates);
  (4) two tiny, low-risk edge cases: `.modal-overlay` had no edge padding (dialogs
  touched the viewport edge exactly at ≤420px) — added 16px; `Toast`'s rail used a
  bare `maxWidth:380` with no viewport-relative bound and no `overflow-wrap` on the
  message — changed to `min(380px, calc(100vw - 32px))` + `overflow-wrap:break-word`.
  New tests: `SearchTab.test.tsx` (2, this component's first ever), `MyAnalyticsPage.test.tsx`
  (1, new file), `StatusBar` extended in `pages.test.tsx` (1). **Disclosed, not fixed**
  (found during the audit, out of scope for a mobile-bug-fix unit — pure hygiene, zero
  behavior change on any breakpoint, >5 lines): a legacy pre-"ide-shell" nav system
  (`.header`/`.nav-desktop`/`.nav-mobile-controls`/`.hamburger` + its own dead 900px/600px
  media-query block) is forced off via `display:none` but never deleted, plus a
  duplicate/superseded `@media(max-width:600px){.ide-main...}` block — both dead CSS,
  deferred to H6.2's hygiene self-scan. See PROGRESS for verification evidence.
- **H5.3 — DONE — Performance:** lazy-load heavy pages via dynamic import (the >500kB
  chunk warning), Lighthouse a11y ≥95 on the 5 core pages. No new deps.
  `routes.tsx` converted 22 of 28 pages (everything except HomePage/AnalyzePage/
  ProjectsPage/ProjectPage/AccountDashboardPage/NotFoundPage — the true first-load/
  high-frequency set) from static imports to `lazy()`, with a single `<Suspense>`
  boundary added in `App.tsx` around `activeDef.render(routeCtx)`. Main bundle
  777.27 kB → 456.96 kB (−41%), now under the 500kB warning threshold entirely;
  each secondary page is its own small chunk (DocsPage, the biggest, is 68.31 kB
  standalone). "The 5 core pages" isn't defined anywhere in `docs/web-plan/
  BUILD-PLAN.md` beyond the phrase itself — used the same eager/high-frequency set
  from the lazy-load decision, minus NotFoundPage (not a real user-journey page):
  HomePage, AnalyzePage, ProjectsPage, ProjectPage, AccountDashboardPage. Auth-only
  pages (Projects/ProjectPage/AccountDashboardPage) bounce to a sign-in gate without
  a live backend, so a raw static-preview crawl can't reach their real content —
  substituted 3 public, backend-independent pages that exercise the same shared
  primitives (DocsPage, RunnerPage, CommercePage) for a meaningful, honest signal
  instead of re-auditing the same gate screen three times; disclosed, not silently
  swapped. Real Lighthouse run (Chrome + `npx lighthouse`, no new deps added — npx
  doesn't touch package.json/lockfile) via a local `vite preview` of the production
  build: Home 100, Analyze 100, Docs 98, Runner 98, Commerce 95→100 after a fix (see
  below) — all ≥95. One genuine fix applied: Commerce's only failure was
  insufficient color contrast (3.53:1, needs 4.5:1) on the shared `PageFooter`
  copyright text and Terms/Status links — traced to `opacity:0.85` on
  `.ide-footer p` compounding an already-adequate `--text-muted`-on-background pair
  (computed ~4.71:1 at full opacity) below the AA threshold; removed the opacity
  dampening, re-verified 100. This is a shared-component fix benefiting every page,
  not just Commerce — the other 4 pages' reports simply didn't happen to flag it
  this run. **Disclosed, not fixed — new H5.3b:** Docs/Runner's remaining 2-point
  gap is a `heading-order` violation (h1 straight to h3, no h2) — NOT the "exactly
  one h1" property H5.1 already audited and fixed, a different, previously-
  undiscovered WCAG check. A quick repo-wide grep (pages with `<h3>` present and
  zero `<h2>` anywhere) found this is page-wide on both flagged pages and likely
  widespread: 15 pages / 109 total `<h3>` headings with no `<h2>` at all (Admin 8,
  Changelog 1, Commerce 2, Docs 23, Help 22, Mcp 6, MyAnalytics 4, Plans 2,
  Playground 2, Project 1, Runner 5, Settings 8, Status 3, Terms 13, Usage 7).
  Both audited pages already clear the ≥95 bar despite this, so fixing it isn't
  gating — but it's real, and doing it right needs the exact same per-page
  judgment discipline as H5.1's original heading-order work (confirm each h3 is
  genuinely the page's top-level section heading before promoting, per-page, not
  a mechanical sed), applied to up to 15 pages instead of 1-2 — too large to fold
  into this unit silently. Tracked as **H5.3b** in STATE. Test/build changes: 3
  `app-routing.test.tsx` tests needed real timeout headroom (10s `findBy` / 15s
  test budget) for genuine dynamic-import transform latency in the test
  environment — same rationale this file's own `PageFooter` test already
  established for real, expected wall-clock growth, not flakiness; the
  kitchen-sink test's heading assertion switched `getByRole`→`findByRole` since
  its page is now lazy. 2 `CommercePage.test.tsx` failures on the first full-suite
  run were the pre-existing, already-documented local-load flake (confirmed via
  isolated re-run, 7/7 clean) — CommercePage is tested via direct import,
  entirely bypassing `routes.tsx`'s lazy wiring, so this was never a candidate
  regression from this unit. **Verified:** typecheck clean, full apps/web suite
  30/30 files, 588/588 tests (twice — before and after the footer-CSS fix),
  production + monorepo builds clean. No API changes — pure `apps/web` again.
- **H5.3b — DONE — Heading-order sweep** (h1→h3 skips, disclosed by H5.3's
  Lighthouse pass — see PROGRESS for the full 15-page/109-heading grep evidence
  and the verification/fix detail). Promoted every genuine top-level `<h3>` to
  `<h2>` across all 15 pages, plus `<h4>`→`<h3>` in the 3 pages (Docs, Help,
  Terms) that had a real extra nesting level — 134 headings total. Done via 6
  parallel agents (one per file for the 3 h4-affected pages given their scale,
  one each for 3 groups of the remaining 12), each independently verifying every
  heading's context before promoting (none were mislabeled, none skipped) and
  pinning `fontSize`/`fontWeight` to the original tag's computed default. A
  consolidated pass afterward caught a gap the per-file instructions missed:
  `margin-bottom` also differs per heading level (h1/h2: 8px, h3: 4px, h4: 0px
  via the universal `* { margin: 0 }` reset) — pinned `marginBottom` on the 20
  headings that had no prior margin override and would otherwise have picked up
  a few px of visual drift from the level-appropriate default. Affected (all
  DONE): AdminPage, ChangelogPage, CommercePage, DocsPage, HelpPage, McpPage,
  MyAnalyticsPage, PlansPage, PlaygroundPage, ProjectPage, RunnerPage,
  SettingsPage, StatusPage, TermsPage, UsagePage.

## Phase H6 · Ops + security hardening

- **H6.1 — DONE — Key-rotation runbook execution prep:** inventory every live credential
  (key.txt labels + Render env), record rotation procedure per credential in
  `docs/SECURITY_ROTATION.md` (procedure only — no values), flag the two known standing
  risks for the owner: Render key blast radius, org 2FA. Includes the owner-gated `rk_`
  migration prep from the July report: enumerate exact Stripe permissions used (from
  H0.4-H0.6 work) into the doc so the owner can mint the restricted key in one sitting.
  Scoped to axis-iliad's own operational credentials (render.yaml's `sync: false` rows +
  the 4 GH Actions repo secrets), not the owner's full multi-product key.txt estate —
  most of that file's ~30 entries (Plaid, Circle, Roblox, other LLCs' EINs) belong to
  unrelated products, out of scope for this repo's runbook. Stripe permission enumeration
  found the real call sites are narrower than expected: `apps/api/src/stripe.ts`
  (Checkout Sessions: Write, Subscriptions: Write), `network-token.ts` (Payment Methods:
  Read), `packages/agentic-compliance/src/dispute-clients.ts` (Disputes: Write) — 4
  resource permissions total, confirmed `commerce-integration.ts`'s Stripe-API string is
  generated customer-facing code, not this system's own call. Verified no secret value
  from key.txt appears anywhere in the new doc (grepped for every live prefix pattern
  before committing).
- **H6.2 — DONE — Dependency + hygiene self-scan:** `pnpm audit` triage (fix or document),
  run the repo's own `iliad_hygiene` scanner against itself, fix findings. All 5
  `pnpm audit` vulnerabilities fixed (0 remain); the `iliad_hygiene` self-scan's 11
  findings were all confirmed self-referential false positives (this repo builds
  hygiene-scanning tools, so its own source legitimately contains the patterns those
  tools detect); also closed H5.2's disclosed dead-CSS finding and a stale CLAUDE.md
  warning found along the way. See PROGRESS for full detail.
- **H6.3 — DONE — Live-probe battery as a script:** `scripts/live-probe.mjs` — health/ready,
  MCP tools count, PAI'D config, anon 413 gate, web bundle marker — one command the loop
  (and CI, non-gating) can run. The July systems check did this by hand; make it repeatable.
  `pnpm run live-probe`; wired into CI as a new non-gating `live-probe` job after
  `deploy-web` (no deps — pure native `fetch`, so no `pnpm install` needed in that job).
  All 6 checks pass against real production as of this unit. **Phase H6 is now fully
  wrapped — H6.1, H6.2, H6.3 all DONE.**

## Phase H7 · Productize the model-cascade doctrine (owner directive, 2026-07-11)

The strategy pattern this very document embodies — a higher-capability model writing
acceptance-criteria-complete execution guides for the cheapest adequate lower tier —
becomes a customer-facing artifact emitted by the generator suite. It must be generic
(any customer repo), deterministic, and derived from real repo signals like every other
generator. No LLM calls at generation time — the artifact TEACHES the cascade; it is not
produced by one.

- **H7.1 — DONE — New generator: `model-cascade.md` under the `skills` program:** built
  `generateModelCascade` in `generators-skills.ts` following the sibling skills-generator
  conventions exactly (same `(ctx, files?)` signature shape, same `mdText`/`mdInline`
  sanitizer discipline for every ctx-derived string). Three-tier table (planner
  frontier-class / executor mid-class / mechanical small-class) with task-type rows gated
  on real detected signals — has CI → mechanical (CI triage), has tests → executor
  (test-backed implementation), a monorepo/infra architecture pattern → planner
  (cross-cutting design + adversarial verification), >1 framework detected → planner
  (framework/tooling migration), separation score <0.5 → planner (layer-boundary
  refactoring) — plus the delegation contract, cost rule, and an honest-limits note
  (capability CLASSES only, no vendor SKU/pricing/benchmark strings — enforced by test).
  Registered in `generate.ts`'s REGISTRY and `program-manifest.ts`'s GENERATOR_PROGRAMS;
  `TOTAL_GENERATORS`/`ARTIFACT_COUNT` bumped automatically as designed (141→142), no
  manual edit needed there.
- **H7.2 — DONE — Wiring + count sweep:** the spec's own "skills entry in
  `PROGRAM_OUTPUTS`" turned out not to exist — the real skills-output list `/v1/analyze`
  serves is a local array at `apps/api/src/handlers.ts` (`skillsOutputs`), corrected and
  wired there instead (disclosed, not silently substituted). Ran the pinned-literal sweep
  and let the enforcing tests do the real work per the spec's own instruction, which
  caught more than the named doc list: `README.md`/`LAUNCH_CLAIMS.yaml`/
  `AXIS_Board_Pitch.md`/`launch-content.md`/`config.ts` (as named) plus `marketing-pack.md`
  (same `launch-claims.test.ts` corpus), `CONTRIBUTING.md` + `begin.yaml` (both guarded by
  `strategic-docs-honesty.test.ts`, not in the spec's named list), and two hardcoded
  generator-count literals in `generate.test.ts`/`generate-programs.test.ts` that would
  have broken CI once `TOTAL_GENERATORS` auto-derived to 142. A genuine second-order
  effect the sweep surfaced: skills is a free program, so the free-tier generator count
  also moved 15→16 — fixed in `LAUNCH_CLAIMS.yaml`, `marketing-pack.md` (which also had a
  pre-existing, unrelated 13-vs-15 drift, corrected in passing), the skills program's
  description string, and two `generators-agentic-purchasing-develop.test.ts` bundle-count
  assertions (free-bundle 15→16, dev-essentials 31→32 since it also includes skills). MCP
  tool count confirmed unchanged (36 — no new tool, rides the existing skills/analyze
  surface as specified).
- **H7.3 — DONE — Tests per program-loop conventions:** added the 6th generator to the
  shared hostile-fixture suite (`generators-skills-injection.test.ts`) — passed clean, no
  new sink logic needed since it reuses the same `mdText`/`mdInline` sanitizers every
  sibling generator already uses. Content-contract tests in `generators-skills.test.ts`
  cover every conditional row's signal-gating both directions (CI/tests/infra-pattern/
  framework-count/separation-score present and absent), the always-present sections, and
  the no-vendor-SKU/pricing/benchmark-name check. Determinism covered by the suite-level
  `determinism.test.ts` once registered, plus a local double-call assertion matching the
  injection file's own convention. One test-design bug caught by an actual run, not by
  review: "Test-backed implementation" also appears verbatim in the static tier table's
  Owns column, so a bare-substring absence check was a false positive — narrowed to the
  row-specific text (`"Test-backed implementation | Executor"`) before it passed clean.
- **H7.4 — DONE — Cascade section in `workflow-pack.md`:** one short, unconditional
  "## Model Cascade" cross-reference pointing at `model-cascade.md` (not duplicated
  doctrine), placed after the four existing workflow blocks. Checked every existing
  `workflow-pack.md`-referencing test across the repo (10 files) before running anything —
  none assert an exact heading count or exhaustive content, so no test updates were
  needed.

**Phase H7 is now fully wrapped — H7.1, H7.2, H7.3, H7.4 all DONE.** (H7.5 remains
explicitly deferred until Phase A convergence, per owner directive — not current scope.)

## Phase H8 · Foundation engineering (known-technique hardening of the safety net itself)

Everything above proves the code works. This phase proves the **safety net** works — the
canon techniques (mutation testing, property-based invariants, contract tests, fitness
functions, data-safety drills, synthetic monitoring, rollback rehearsal) applied within
this repo's no-new-runtime-deps law. Where a real tool would be better than the hand-rolled
version, it is named as a dep-gated ROI candidate — do not add the dep yourself.

- **H8.1 — DONE — Unhappy-path completeness audit.** A plain `grep fetch(` alone found 9
  files but missed real sites using an injectable-fetch testability pattern
  (`fetchImpl`/`FetchLike`/`typeof fetch =`) — widened the sweep and confirmed via a
  dedicated research agent: **14 real external call sites** total (not 9), one of which
  (`email.ts`) wasn't in the initial candidate list at all, and one initial candidate
  (`url-guard.ts`) turned out to be shared infrastructure other sites route through, not
  its own site. Before: timeout 0/14, transport-error 5/14, malformed-response 3/14
  tested. After: transport-error 14/14, malformed-response 14/14 (12 tested + 2 confirmed
  N/A — `stripe.ts`'s cancel-subscription and `alerting.ts`'s `postAlert` never read the
  response body), timeout 6/14 tested (every site with a real client-side
  AbortController) + 8/14 explicitly waived with a one-line inline comment at the call
  site (`H8.1 WAIVER: ... Tracked as H8.1b`) — zero unwaived gaps, matching the acceptance
  bar exactly. Split across 6 parallel agents (each 1-2 files) plus `alerting.ts` done
  directly (its own test file already existed for its PURE functions but not for
  `postAlert`, which needed exporting first — a research-agent report had wrongly
  classified this site as having no timeout mechanism; caught by reading the source
  directly rather than trusting the classification, corrected before dispatching work
  based on it). Re-verified the OTHER 7 "no timeout mechanism" sites myself directly
  against source before finalizing waivers — all 7 confirmed accurate.
  **Six disclosed findings, not fixed (test-writing scope, not a fix pass — each is its
  own future unit):** (1) `handleFirecrawlScrape` charges the account AND writes the
  empty result into the 24h shared cache when Firecrawl returns a malformed 200 —
  bills for nothing and serves that nothing to every other caller of the URL for 24h;
  the crawl sibling is billing-safe but still returns a silent 200. (2) `handleCreateCheckout`
  returns `201` "success" when Stripe's 200 response is missing `id`/`url` — the caller
  can't tell a completed checkout from one with no redirect URL. (3) `firecrawlPost`
  (the MCP-tool-path Firecrawl client) classifies no errors at all — timeout and generic
  transport failure propagate as the identical raw rejection. (4) `openDriftPullRequest`
  has no try/catch around any of its 5 GitHub calls — confirmed by test that a rejected
  fetch propagates uncaught at the function boundary (caught one level up by the webhook
  handler's own `.catch()`, so it doesn't crash the process, but the error contract is
  inconsistent with sites that translate failures cleanly). (5) `downloadAudio` writes
  any 200 response to disk unvalidated; malformed bytes surface later inside ffmpeg,
  OUTSIDE `runTranscription`'s try/catch — it rejects with a raw `spawn UNKNOWN` instead
  of its own documented `_not_configured/audio_download_failed` contract (test named
  `"KNOWN GAP: ..."`, asserts the real broken behavior rather than a false pass).
  (6) `handlers.ts`'s Firecrawl proxy and `web-research.ts`'s `firecrawlPost` are two
  independent, live, hand-rolled implementations of the same integration — real
  unrefactored duplication, not dead code; consolidating them is a design call outside
  this unit's scope. **Verified:** full monorepo build clean; all 10 touched test files
  green against a real local Postgres (204 tests, 1 skipped, 0 failed — one transient
  30s timeout under 6-way concurrent DB load from the parallel agents, confirmed clean
  on isolated re-run). CI green (`build-and-test` 20+22, `deploy-web`, `live-probe` job
  all success; `docker-build`/`deploy-api` correctly skipped). Live-probed via
  `scripts/live-probe.mjs` against real production post-deploy (6/6 checks pass) — this
  unit's `apps/api` changes are comment/export-only with zero functional impact, so a
  basic health confirmation is the right-sized check, not a feature-specific probe.
- **H8.1b — Add real timeout mechanisms to the 8 sites H8.1 waived** (`handlers.ts` ×2
  Firecrawl, `stripe.ts` ×3, `network-token.ts`, `github-pr.ts`'s `ghCall`, `email.ts`) —
  each needs the same `AbortController` + `setTimeout` pattern already proven correct
  5× elsewhere in this codebase (`web-research.ts`, `web-fetch-sovereign.ts`,
  `embeddings.ts`, `document-parsing.ts`, `speech-to-text.ts`), not a new mechanism
  invented from scratch. Then write the now-possible timeout test per site, removing
  each `H8.1 WAIVER` comment as its site is closed. Split off rather than folded into
  H8.1 because it's real production-code behavior change across 5 files, not test-writing
  (anti-gold-plating law) — sequence after H8.1's own commit lands clean, before H8.2.
- **H8.2 — Money-math invariants (property-style, hand-rolled seeded PRNG — no new dep).**
  New test over `packages/snapshots/src/usage-credit-metering.ts`: ~1,000 seeded cases
  asserting, for every (allowance, used, amountCents): `included_credits_applied +
  overage_credits === credits_required`; both terms ≥ 0; `included_credits_applied ≤
  remaining allowance`; sequential consumes accumulate exactly (no lost updates
  single-threaded). Acceptance: suite green; a deliberately mis-split local mutation is
  caught (verify locally, never commit the mutant). Dep-gated upgrade: fast-check.
- **H8.3 — Mutation-lite pass on the money path.** For `cashier.ts`, `mcp-runtime.ts`,
  `usage-credit-metering.ts`, `growth-store.ts`, `stripe.ts`: flip each boundary operator
  / negate each early-return guard ONE at a time locally; run the scoped suites; any
  surviving mutant gets a killing test. Acceptance: mutant → killing-test table in the
  commit body, survivors driven to zero. NEVER commit a mutant (verify
  `git status`/`git diff` clean of mutations before commit). Dep-gated upgrade: Stryker.
- **H8.4 — PAI'D contract fixtures.** Golden request/response fixtures for every PAI'D
  endpoint Iliad calls (`apps/api/src/paid-client.ts` + `packages/paid-client/`), asserted
  in unit tests; plus an env-gated live contract canary (pattern:
  `live-settlement.e2e.test.ts`) validating fixtures against `api.trustfabric.ai` —
  includes finally verifying whether PAI'D honors `Idempotency-Key` (the client's own
  comment says unverified). Acceptance: fixture drift breaks tests; canary green when run
  with creds; the idempotency answer documented in the client comment.
- **H8.5 — OpenAPI ↔ router bijection test.** Every route registered in
  `apps/api/src/server.ts` appears in `openapi.ts` and vice versa (explicit waiver list
  for non-REST surfaces like `/mcp` JSON-RPC). Acceptance: bijection test green; any
  drift found is fixed, not waived.
- **H8.6 — Architecture-boundary fitness tests (hand-rolled fs walk, no dep).**
  Lock the dependency rule: `apps/web` and `apps/api` never import each other's `src`;
  `packages/*` never import from `apps/*`; no cycles in the `@axis/*` workspace-dependency
  graph (walk the package.json files). Acceptance: test green; violations fixed or
  waived with an in-test comment. Dep-gated upgrade: knip/madge for dead-export + cycle
  depth.
- **H8.7 — Neon data-safety drill.** Confirm PITR/branching on the live Neon project;
  document the restore procedure in `docs/RUNBOOK_ROLLBACK.md`; perform ONE drill:
  create a branch at T-minus-15-minutes, verify row counts on 3 core tables, delete the
  branch. Escalate if no Neon credential label exists. Acceptance: runbook section +
  drill evidence (timestamps, counts) committed.
- **H8.8 — Deploy rollback runbook + rehearsal.** Document in the same runbook: API
  rollback via Render (redeploy previous deploy id), web rollback via Cloudflare Pages
  previous deployment, git-revert-and-push as the default path; make explicit the
  standing rule **CI red after your push = priority zero, nothing else proceeds**.
  Rehearse one rollback of a no-op commit off-peak, or record why not + add to
  PENDING-OWNER. Acceptance: runbook complete; rehearsal evidence or explicit deferral.
- **H8.9 — Synthetic monitor (dead-man's switch).** Promote `scripts/live-probe.mjs`
  (H6.3) into `.github/workflows/synthetic.yml`: scheduled every 30 min, non-gating,
  opens/updates a single GitHub issue on failure and closes it on recovery (native
  GITHUB_TOKEN, no new secrets). Acceptance: scheduled run green; one forced-failure test
  run demonstrably opens the issue (then reverted).
- **H8.10 — Web security headers.** Cloudflare Pages `_headers` file: CSP (self + the two
  API origins), HSTS, `X-Content-Type-Options: nosniff`, `Referrer-Policy`,
  `frame-ancestors 'none'`. Must not break hash routing, OAuth callback, or the PAI'D
  checkout redirect — verify all three live after deploy. Acceptance: headers visible via
  `curl -I` on prod; routing/auth/checkout spot-checks pass.
- **H8.11 — CI secret-scan + webhook replay audit.** (a) A CI step greping each push's
  diff for credential shapes (`sk_live_`, `rk_live_`, `whsec_`, `re_[A-Za-z0-9]{20,}`,
  `hf_[A-Za-z0-9]{30,}`, `rnd_`) with an allowlist for the known fake fixtures; prove it
  with a scratch branch containing a fake key (must fail), then delete the branch.
  (b) Audit replay protection on every webhook handler (Stripe has 5-min tolerance —
  verify the PAI'D and GitHub handlers reject stale/replayed deliveries; fix gaps).
  Acceptance: CI step live + proven; replay matrix documented with fixes landed.
- **H8.12 — Dependabot config (config file only — not a runtime dep).** Weekly, grouped
  minor/patch, majors labeled `deps-discussion` per repo law (never auto-merge).
  Acceptance: valid `dependabot.yml` on main; first batch triaged per law.

## Phase A · Re-audit cycle (after H0–H8, and again after every subsequent fix batch)

1. Run the full CI suite + live-probe battery + `pnpm run build`.
2. Spawn up to 3 read-only review subagents with the July checklists (payments/PAI'D
   coherence; web routes/auth/counts/dead-UI; engines/MCP honesty+determinism+gating), each
   returning findings with file:line + severity + regression-vs-pre-existing. Verify every
   HIGH/MED yourself by reading the code before acting.
3. **Recurring foundation checks** (each cycle, cheap, mechanical-tier work):
   (a) idempotency classification sweep — every state-mutating endpoint added since the
   last cycle is classified idempotent-by-key / naturally-idempotent / at-most-once-risky,
   and risky ones get a unit; (b) unhappy-path re-grep — new `fetch(` sites since last
   cycle get the H8.1 three-test treatment; (c) mutation-lite rotation — ONE money-path
   module per cycle gets the H8.3 flip-and-verify treatment; (d) the H8.5/H8.6 fitness
   tests and H8.9 synthetic monitor are green.
4. Confirmed findings become new units (append to STATE, tag cycle number). Fix them.
5. A cycle with **zero new confirmed findings** increments the dry-count; any finding
   resets it. **dry-count = 2 → CONVERGED** → Phase T.

## Phase T · Terminal deliverables (the tree + the ROI list)

- **T.1 — `docs/PLATFORM_TREE.md`:** the full system as a tree. **Roots** (infrastructure:
  Neon PG, Render API, Cloudflare web, CI, PAI'D rail, Stripe Connect platform, HF Spaces
  bridge). **Branches** (product surfaces: REST API, MCP server, web app, CLI, generator
  programs, compliance engines, assetforge). **Leaves** (every feature, marked
  `shipped | gated(owner) | gated(external) | candidate`). Every node cites its evidence
  (file, endpoint, or live probe). Nothing aspirational may appear unmarked.
- **T.2 — `docs/ROI_CANDIDATES.md`:** structured, ranked list for continued work. Columns:
  candidate · tree position · effort (S/M/L) · impact (revenue / strategic / enabling) ·
  gates/dependencies · confidence. Seed entries (verify, re-rank, extend as the loop
  discovers more): engineer-tier E7/E10/E11/E12 (dep-gated — owner discussion);
  `axis-iliad` npm publish (owner credential); GitHub App free listing (ON HOLD — owner);
  official MCP registry listing; WO-A6 async runs/SSE; multipart ZIP intake (kills
  client-side unzip); status-page incident history storage; Iliad connected-account charge
  cutover (owner decision, account exists); PAI'D embedded "Complete verification" button
  (different repo); PAI'D sanctions-restricted events investigation (read-only, different
  repo); distribution/activation items from repo-root `ACTIVATION_TRACKER.md`; assetforge
  LOCAL-backend GPU vertical; test-tooling upgrades gated on dep discussion (Stryker
  mutation testing, fast-check property testing, knip dead-export analysis, Playwright
  browser e2e — each replaces a hand-rolled H8 technique with the industrial version);
  branch protection with required status checks on `main` (owner decision — changes the
  push-equals-deploy workflow this loop and the operator both rely on); new verticals
  discovered during the loop.
- **T.3 — Confidence report + PENDING-OWNER table:** one honest page — what is verified
  solid, what is monitored, every owner-only item accumulated by rule 12, and the loop's
  final dry-count evidence. This is the "ready for new verticals" attestation; it may not
  overstate.

---

## Escalation list (the ONLY reasons to stop and ask)

- A fix requires a new runtime dependency (repo law: discussion first).
- A fix requires schema-destructive migration on live Neon data.
- Anything on the HOLDS list (rule 11) appears load-bearing for a unit.
- A credential label is ambiguous, or a needed credential doesn't exist.
- Two honest designs conflict with each other's law (e.g., a polish item would require
  weakening a guard test) — present both, wait.

Everything else: decide, disclose in the commit body, keep moving.

---

## PROGRESS ledger

Append-only — one line per completed unit; the operator scans this instead of reading
every diff. Newest at the bottom.

| Date | Unit | One-line outcome | Commit |
|---|---|---|---|
| 2026-07-11 | H1.2 | ProjectPage crash on malformed generated-files response fixed (defensive coerce + findBy); CI-flake root cause closed | `fdfddd5` |
| 2026-07-11 | H0.1 | FC-debit idempotency: per-invocation UUID replaces 120s-bucket key (bucketing dropped 2nd call in window / re-keyed late retries); red demonstrated first | `e5558fb` |
| 2026-07-11 | H0.3 | Wallet-rail revenue now settled-visible: migration v33 widens provider CHECK to paid_fc, TS union extended, enforce-success records the receipt; 3 reds demonstrated first | `5c8e120` |
| 2026-07-11 | H0.4 | Stripe-Version 2026-06-24.dahlia pinned on all 4 outbound call sites (stripe.ts x2, network-token, dispute-clients); webhook handlers dual-read Basil+ shapes (item-level periods, invoice parent.subscription_details) with legacy fallback; 6 reds demonstrated first | `24f827a` |
| 2026-07-11 | H0.5 | checkout-completed now stores the price the customer ACTUALLY bought (fetched from the subscription, pinned version, fail-to-env-fallback) with plan-intent tier fallback so env rotation can't strand or rewrite a paying customer; red demonstrated first | `493d986` |
| 2026-07-11 | H0.6 | Idempotency-Key on both Stripe POSTs: checkout create keyed by checkoutIdempotencyKey (its designed double-submit purpose), cancel keyed per subscription; red demonstrated first | `653d773` |
| 2026-07-11 | H0.7 | The three unlocked 1261357 deploy fixes now have regression tests (root-vs-nested packageManager, Go -o /out/app ., framework-aware Python CMD); red proven via temporary local revert (5/5 deploy locks fail pre-fix), revert never committed | `df86b2c` |
| 2026-07-11 | H0.8 | runEmbeddings gains its first direct tests (5): both _not_configured honesty envelopes never charge or fabricate, happy path captures credits exactly once, input validation clean | `3466f99` |
| 2026-07-11 | H0.9 | PlansPage fallback pricing now DISCLOSES itself (warning Callout, only on failure); malformed-200 payloads treated as failure instead of storing junk (H1.2 class); red demonstrated first (2 red + control green) | `2ca1762` |
| 2026-07-11 | H0.10 | TEN tracked root run-logs (incl. oauth-fail.log, found beyond the doc's nine) secret-scanned (all clean — valid ERE scan after a first invalid-regex attempt), untracked via git rm --cached (kept on disk), class gitignored; only curated llms.txt remains | `cb8366f` |
| 2026-07-11 | H1.1 | production-startup + rate-limiter deflaked: 30s per-file ceilings + every fixed sleep replaced with event-driven waits (close callbacks, once("error")); 43/43 isolated | see commit |
| 2026-07-11 | H1.4 | scripts/test-ci-mirror.mjs + pnpm run test:ci-mirror — CI=true (4-worker cap) + the exact ci.yml coverage flags, DATABASE_URL defaulted to the local container | `b162cc3` |
| 2026-07-11 | H2.1 | compensation_ledger migration v34 + store: at-most-once claim proven incl. 8-way concurrent race; per-account + platform summaries; producer idempotency lifecycle-borne | `daf6091` |
| 2026-07-11 | H2.2 | settled-then-error producer live: dispatch catch records the owed entry (amount rides the settled marker, now a WeakMap) + agent sees _compensation in the error envelope; red demonstrated first | `a4eb146` (CI fix `41e19db`) |
| 2026-07-12 | H2.3 (+H0.2) | Enforce-mode wallet ambiguity closed: any non-402 debit error (timeout, network, 5xx) no longer falls through to chargeMpp — records a wallet_rail_ambiguous ledger row and fails the call closed (402, no work, no second-rail charge); existing test that had encoded the bug as "correct" flipped and proven red first | `28932bc` |
| 2026-07-12 | H2.4 | Compensator live: grantUsageCredits (new additive primitive — banks negative included_credits_used as spendable headroom) + claim-then-grant orchestration (apps/api/src/compensator.ts), triggered lazily on every _usage build (no new background infra) so an account is made whole on its very next call; _usage gains compensation:{owed_cents,credited_cents}; admin revenue gains compensation_owed_cents_all_time + a net-of-compensation figure WITHOUT altering the existing settled_revenue_cents_all_time definition | `5c755f5` |
| 2026-07-12 | H2.5 | 402/429 payload unification: fixed a real bug (sendError's extra-spread clobbered the caller's real message with build402NegotiationBody's generic "Payment Required" constant, incl. on 429s); added upgrade_url once at the shared builder (propagates to ~15 sites for free); closed 2 REST sites (post-scrape/post-crawl) that sent bare messages; gave versions.ts + cashier.ts's 2 raw sites error_code/request_id for the first time; new contract test walks every sendError(402\|429) site + a builder-level test on build402NegotiationBody; 1 pre-existing test had encoded the clobbering bug as correct and was fixed to assert the real message. Closes Wave 0 (H0+H1+H2) — MONEY GATE review packet below | `9b767d0` |
| 2026-07-12 | RT.1 [CRITICAL] | Idempotency claim/reservation: idempotency_keys gains a 'pending'/'completed' status (migration v35) — a request now atomically CLAIMS the key BEFORE any charge or dispatch (INSERT...ON CONFLICT, with a 60s stale-claim reclaim so one crashed request can't permanently lock a key); the old plain read-then-later-write let two concurrent requests sharing one Idempotency-Key both see "nothing yet" and both charge + execute. gateIdempotency is the single chokepoint shared by the in-band settlement gate and dispatch (per-request WeakMap so the second caller sees the first's claim, never re-attempts). Proven via a genuine 10-way concurrent Postgres race (exactly one winner) at the store level — deterministic, not timing-dependent, since Postgres's own conflict resolution serializes the racing INSERTs. Red demonstrated by temporarily neutering the atomic claim locally (never committed) | `51a65bc` |
| 2026-07-12 | CI-fix | project_memory newest-first ordering was non-deterministic (created_at DESC, id DESC tiebreak on random UUID) — migration v36 adds a real monotonic seq column; found because it broke CI on the RT.1 push, unrelated to the red-team findings | `0993354` |
| 2026-07-12 | RT.2+RT.3 [CRITICAL+HIGH] | (#2+#5) Wallet-debit idempotency key was randomUUID() every invocation — a client retry (after our own 15s abort, or the ambiguous-402 that abort itself produces) minted a fresh key and became a genuine second debit on the SAME rail; now derived stably via HMAC(accountId, tool, callerIdempotencyKey) when the caller supplies one, so PAI'D can dedupe a real retry — no caller key still falls back to a fresh UUID (unchanged). (#3) The H2.3 catch-all treated EVERY non-402 error as ambiguous, including definite 4xx rejections (bad request, auth, unknown developer_id) that PROVABLY never touched the wallet — minting a real, spendable compensation credit for a call that cost nothing (a free-credit farming vector); now a real 4xx (not 402) falls through to mppx exactly as pre-H2.3 (zero double-charge risk, nothing was debited), and only genuine ambiguity (5xx/504/network) still records compensation. Both red-demonstrated by temporary local reverts (never committed) | `9a292cc` |
| 2026-07-12 | RT.4+RT.5 [HIGH+HIGH] | (#6) fetchSubscriptionPriceId made a single attempt and silently fell back to env on ANY failure — reintroducing H0.5's exact bug if a price rotation coincided with a transient hiccup; now retries (2 attempts, short backoff) and, if every attempt still fails, prefers a KNOWN existing price over env (only falls back to env when there's no history at all, now logged loudly as unconfirmed instead of silent). (#7) Subscription webhooks had no ordering guard — Stripe doesn't guarantee delivery order, so a stale customer.subscription.deleted (an old cancellation redelivered late) could downgrade an actively-paying customer AFTER a newer reactivation event; migration v37 adds last_event_created_at (Stripe's own event timestamp) so a stale event is now detected and ignored (still 200-acked so Stripe stops retrying). Both red-demonstrated by temporary local reverts (never committed) | `54303a5` |
| 2026-07-12 | RT.6 [MEDIUM] | (#4) funnel.ts's SEAT_LIMIT (429) site was the one H2.5 call site that shipped without the canonical `message` field alongside `error` — added; contract test + a direct funnel-api.test.ts assertion both updated | `6bd9e06` |
| 2026-07-12 | RT.7 [MEDIUM] | (#8) ProjectPage's generated-files load had a bare `.catch(() => {})` — any fetch failure or malformed 200 body silently left generatedFiles at [], indistinguishable from a project that genuinely has zero artifacts; loadGeneratedFiles is now a reusable useCallback tracking a distinct filesLoadFailed flag, rendering an honest Callout + real `<button>` Retry (no fake hash href) instead of staying silent. 2 new tests red-green proven via temporary local revert (never committed) | `eabce68` |
| 2026-07-12 | RT.8 [MEDIUM] | (#9) H0.10's "credential-shape scanned, all clean" claim was unverified for 3 of the 10 untracked files (docker-ci-run3.txt, test_output.txt, vitest_requested_output.txt) because they are UTF-16LE-encoded — a POSIX-ERE/grep-style scan silently reports no match on UTF-16LE content regardless of what it contains, and git's own diff independently confirms these 3 (and only these 3) were binary at untrack time. Re-scanned by decoding to UTF-8 (`iconv -f UTF-16LE -t UTF-8`) then running the credential-shape pattern set (Stripe/Resend/HF/Render/AWS/GitHub/Slack/Google/OpenAI-style key prefixes, JWT shape, credentialed DB URLs, PEM key blocks) plus a broader env-var/oauth-term pass — both passes clean on all 3 files. The garbled box-drawing/ANSI runs visible in the decode are a pre-existing PowerShell OEM-codepage capture artifact (unrelated to this fix) that only corrupts non-ASCII bytes; every credential shape checked is pure ASCII, so the mojibake cannot hide one. No code change — a verification-only finding; this ledger row is the disclosure | n/a (verification-only) |
| 2026-07-12 | WO-A4 | GET /v1/changelog serves repo-root CHANGELOG.md verbatim via readFileSync+import.meta.url (no hand-typed duplicate to drift); registered in openapi.ts (route-spec parity test), Cache-Control added, ENDPOINT_COUNT 151→152. Caught a real deploy-pipeline gap before it shipped: the root Dockerfile never copied CHANGELOG.md into the image, so this would have 500'd on every request in prod despite passing locally/CI (both run from a full checkout) — fixed by adding it to the deps stage | `51f7533` |
| 2026-07-12 | CI-fix | WO-A4's ENDPOINT_COUNT bump (151→152) broke main CI — 6 hand-maintained doc/config mirrors of that number (README.md, apps/web/src/config.ts, LAUNCH_CLAIMS.yaml, launch-content.md x2, AXIS_Board_Pitch.md) went stale, exactly what count-honesty.test.ts + launch-claims.test.ts exist to catch. All 6 fixed; lesson applied for the rest of this loop — run those 2 test files locally before pushing any future counts.ts bump | `777d40d` |
| 2026-07-12 | WO-A5 | PATCH /v1/account (name/email, validated + 409-on-conflict, no verification flow exists so change is immediate + disclosed) and DELETE /v1/account (retention policy: hard-delete access surfaces + generated content incl. OAuth sessions/API keys/projects/snapshots/webhooks/seats/tokens; RETAIN financial/audit tables — usage/tier/credits/subscriptions/referrals/disputes/compensation ledger — untouched; accounts row anonymized in place, never deleted, since every retained table still FKs to it — full policy documented in deleteAccount's doc comment). Caught 2 more deploy-shaped gaps pre-ship: Router had no patch() method (would've been a runtime TypeError) and Access-Control-Allow-Methods never listed PATCH (would've silently CORS-blocked every browser caller). ENDPOINT_COUNT 152→154, doc mirrors propagated BEFORE testing this time. 12 new tests (account-lifecycle.test.ts) | `e8a3208` |
| 2026-07-12 | WO-P11 | Projects/History page ("#projects", auth-only): full searchable/sortable list of every analyzed repo via GET /v1/projects (WO-A1); per-row Open/Re-analyze/Export ZIP/Delete (click-to-arm, mirrors VersionsTab's DangerButton). Re-analyze needed a small cross-page handoff (RouteContext.onReanalyze/prefillRepoUrl, App.tsx state, AnalyzePage's new initialUrl prop) — safe because every nav gets a fresh route.key, remounting the page. 14 new tests (10 ProjectsPage.test.tsx + 4 app-routing.test.tsx); routes.test.tsx's AUTH_ONLY_PAGES exact-set assertion updated | `3489b18` |
| 2026-07-12 | WO-P10 | Usage & Billing page ("#usage", auth-only): billing/usage half of AccountPage moved verbatim (subscription, credits, per-program usage) plus new usage graphs (BarChart, GET /v1/account/usage/timeseries — WO-A3) and a tier-change proration preview (new web client getProrationPreview against the ALREADY-LIVE but previously-unused GET /v1/billing/proration). AccountPage keeps only profile/keys/seats/OAuth-callback for now (WO-P12 finishes the split into Settings). 8 new tests (6 UsagePage.test.tsx + 2 app-routing.test.tsx) | `84766cf` |
| 2026-07-12 | WO-P12 | Settings page ("#settings", auth-only, claims AccountPage's old sidebar/Ctrl+4 slot): Profile (new PATCH /v1/account UI), API Keys + Team Seats (moved, seats tier-gated honestly), plus 3 sections with no home before — GitHub Tokens CRUD, Webhooks CRUD + per-webhook delivery log, Program toggles (tier-gated) — and the Danger Zone (new DELETE /v1/account UI, click-to-arm). New api.ts client functions: GitHub-token/webhook CRUD, updateProgramEntitlements, getAccountEntitlements; patchAccount/deleteAccount/getChangelog turned out to already be pre-stubbed by WO-F3. AccountPage.tsx reduced to a thin shell (OAuth-callback exchange, unchanged — the redirect_uri is a fixed server value pointing at "#account", not touched) that redirects to Settings once authenticated. HelpPage's Ctrl+4 label updated (2 sites); the deeper AccountGuideSection prose rewrite is a DISCLOSED follow-up, not done here — belongs with WO-P14's sweep. 20 new tests (12 SettingsPage.test.tsx + 3 app-routing.test.tsx incl. the #account→#settings redirect) | `9096aac` |
| 2026-07-12 | WO-P9 | Agentic Commerce hub ("#commerce", not auth-only — only generate is paid-gated): renders the "agentic-purchasing" program's 6 artifacts in-app via the same runProgram(snapshot_id) mechanism WO-P7 already shipped (402s reuse UpsellModal). Two disclosed spec deviations: uses runProgram against an existing snapshot instead of POST /v1/prepare-for-agentic-purchasing (that endpoint needs fresh raw files the web app doesn't retain post-analysis — "readiness" shown is the project's existing compliance grade instead, labeled honestly); the plan's "un-stub ToolsIndexPage's coming_soon cards" is already satisfied — that page no longer exists, WO-P8 retired it. Prerequisite fix: MarkdownLite (shared with the Artifact Explorer) had zero table support — added GFM pipe-table parsing/rendering, closing a real pre-existing rendering gap in both consumers. checkout-flow.md's arrow-chain renders as labeled step pills (Honesty H3: visualization, not an executable checkout). Caught a real bug pre-ship: the generate call's endpoint string carried its own "/v1/" prefix, which runProgram also adds — would have silently double-prefixed the URL; fixed + locked in with a test. 13 new tests (4 MarkdownLite table + 7 CommercePage.test.tsx + 2 app-routing.test.tsx) | `9e9f568` |
| 2026-07-12 | CI-fix | WO-P9's push (`0cf984f`) went red on `build-and-test (22)`: `prepare-purchasing.test.ts`'s `beforeAll` hit `EADDRINUSE` on its hardcoded `TEST_PORT=44516` after a 300s hook timeout — unrelated to WO-P9's own diff (that test file predates this session). Auditing the pattern found a live triple-collision already latent in-tree: `well-known-handlers.test.ts`, `changelog.test.ts`, and `budget-probe.test.ts` all hardcoded the identical port 44519. `continuation.yaml` shows this exact bug class already bit the suite once before (mcp-server.test.ts vs production-startup.test.ts, manually reassigned 44505→44515) — manual port bookkeeping doesn't hold as a fix. Converted all 8 affected files (the 4 above + analyze/api/agent-discovery/mcp-server.test.ts, same fragile pattern) to OS-assigned ephemeral ports (`listen(0, "127.0.0.1", ...)` + read back `server.address()`), matching the pattern already established by mpp.test.ts/live-settlement.e2e.test.ts/web-research-sovereign.test.ts. `server-routes.test.ts` (port 44490, boots the real server.ts via `process.env.PORT` rather than a throwaway router) is a structurally different, currently non-colliding case — left as a disclosed follow-up rather than widened into production bootstrap code. Verified: all 8 files pass together in isolation (560/560 tests), apps/api typechecks clean. A local full-suite `vitest run apps/api` run also surfaced 3 unrelated spurious DB-hook-timeout failures (rate-limiter/network-token/analytics.test.ts) — confirmed as local Postgres/Docker contention, not a regression, by cross-checking the most recent green CI run (all 3 pass in seconds there) | `364f447` |
| 2026-07-12 | WO-P13 | Documentation Hub ("#docs"): replaced the static ApiSection (hardcoded 17-endpoint table, fake `http://localhost:4000` base URL, fabricated example payloads) with a hand-rolled, tag-grouped, expandable explorer driven live by GET /openapi.json — recursive $ref-resolving schema viewer, per-endpoint copy-curl (reuses CodeBlock's existing copy button), auth badge sourced from the spec's own `security` field. Added a new "MCP Protocol" tab: a live GET /v1/mcp/server.json summary (tool count/transport/auth) cross-linking to the already-shipped full MCP page (#mcp) instead of duplicating its tool-registry UI a second time. Added a new "Example Artifacts" tab: static samples honestly labeled as such (WO-A7's public-demo-project endpoint hasn't landed), cross-linking to ExamplesPage's real case studies and the Runner. Caught and fixed a real pre-existing drift bug while auditing Programs/Outputs: PROGRAM_DOCS was missing 3 of the 20 programs (agentic-purchasing, closer, deploy) — silently undercounting the Programs tab's Free/Pro badges and leaving those programs out of the Output Formats inventory; also fixed the Overview's category grid, which had dropped "Design System" (Theme's category) entirely and miscategorized Closer under "Agentic Commerce" despite being a packaging/release tool, not a commerce one — now 8 categories, matching the intro prose. Small justified API-side touch: added explicit `security` to the OpenAPI spec's 20 program endpoints (previously undeclared) — the new explorer's copy-curl needs this to know which calls require a Bearer header; without it, a copied curl for any program endpoint would 401. 12 new tests (DocsPage.test.tsx, new file — no prior dedicated coverage existed); full apps/web suite (496) and apps/api's openapi.test.ts (31) reverified green; full monorepo build clean. **Deploy note:** CI green + web (Cloudflare Pages) confirmed live within minutes as usual; the API-side `security` field addition was NOT yet visible on live GET /openapi.json after ~30 min of polling (Render's docker-from-Git auto-deploy runs independently of GH Actions and isn't tracked by any GitHub API I could safely check — did not touch the Render API key per its flagged full-account blast radius/no-2FA). Not a code defect (CI-verified) and not user-visible (the OpenAPI explorer works with or without the field; worst case a few program-endpoint copy-curls omit the Bearer header until this lands). Owner: worth a 30-second glance at the Render dashboard for axis-api if this is still stale next session | `0c3e6d1` |
| 2026-07-12 | WO-P14 | 404 + error-state / a11y sweep, plus WO-P12's disclosed HelpPage content follow-up. **A11y:** 4 click-only `<div onClick>` accordion/tab controls had no keyboard path at all — converted to real `<button>`s: ProjectPage.tsx's project tabs, QAPage.tsx's FAQ toggle, DocsPage.tsx's ProgramDocCard toggle (same bug class, spotted while building WO-P13, fixed here). CommandPalette.tsx's option list already had full keyboard nav via the input (arrows/Enter) but zero ARIA — added role=combobox/listbox/option + aria-selected/aria-activedescendant so screen readers can perceive it; deliberately did NOT add redundant tabIndex/onKeyDown to each option div, which would have fought the existing input-driven interaction. Plan's originally-cited locations (DashboardPage.tsx, GeneratedTab.tsx) were stale filenames from before this session's page renames (now ProjectPage.tsx / ArtifactExplorer.tsx) — checked both; ArtifactExplorer's own file/grid items were already proper buttons, no fix needed. Error-state audit (Skeleton/EmptyState/Callout-retry/UpsellModal) found nothing broken — the pattern already holds consistently across pages built this session. **Content:** rewrote HelpPage's AccountGuideSection (stale "Account page" references from before the P10/P11/P12 split, plus a self-contradicting signup paragraph — one line said "enter name/email, click Sign Up", the next line correctly said "sign in with GitHub/Google, no key required"); swept and fixed 6 more of the same stale reference across DocsPage/QAPage(×3)/TermsPage/AuthButtons, now correctly pointing at Settings (keys/team/profile) or Usage (billing/plan-change/usage meters). **Unplanned but consequential find, same sweep:** PlansPage.tsx computed its own login state via `localStorage.getItem("axis_api_key")` — dead since this session's H1 cookie-auth migration (auth is now an HttpOnly cookie, invisible to JS by design, and the app hasn't written that key since). Every real signed-in user has been seeing the signed-out button label ("Sign Up for X" instead of "Choose X") and hitting the login gate on click instead of proceeding to checkout, on the app's primary conversion page. Fixed by threading the real `loggedIn` from `ctx.loggedIn` (the same mechanism every other page already uses) as a required prop; updated the existing PlansPage tests (which had been passing a fake `localStorage` value to paper over exactly this), added a dedicated regression test for the signed-out path. Full apps/web suite (496) reverified green (one PageFooter test timeout during a full-suite run was a local-contention flake — reproduced clean 3/3 times in isolation and alongside the directly-affected files); full monorepo build clean | `6e5502f` |
| 2026-07-12 | WO-P15 | Live Demo / Playground ("#playground", public, BONUS): a fuller standalone version of HomePage's LiveDemoTeaser, which its own code comment had explicitly deferred to this unit. Pick from 3 curated sample repos (verified via live GitHub API calls before hardcoding — octocat/Hello-World, railwayapp-templates/expressjs, railwayapp-templates/django: real, small, actively-maintained, one per major language flavor) or paste any public URL → real anonymous analysis via POST /v1/analyze (free programs only) → inline results (stats, frameworks, generated-file list with a click-to-preview pane) — no redirect to the full project view, so the page stays a genuine single-interaction "try it now" experience. Anon rate-limit meter surfaces GET /v1/account/quota's `rate_limit` field, which existing pages already fetch but none render. "Sign up to unlock 17 more programs" CTA appears only for logged-out visitors (real `loggedIn` prop, not assumed). Results cached under a page-own `axis_playground_result` localStorage key rather than the shared `axis_anon_result` slot — analyzeQuick's response shape doesn't match what that slot's other readers (App.tsx's boot-restore) expect, so reusing it verbatim would have been a shape-compatibility bug waiting to happen. Extracted McpPage's `ProbeIntentDemo` into a shared component (`components/ProbeIntentDemo.tsx`) so both pages use it instead of a second copy — the plan lists `POST /probe-intent` as one of this unit's three API integrations. Connected LiveDemoTeaser's idle state to link through to the new page (`onOpenPlayground`), closing the loop its own comment anticipated. 13 new tests (PlaygroundPage.test.tsx) + 1 new (LiveDemoTeaser.test.tsx); full apps/web suite (511) and McpPage/routes/app-routing suites reverified green; full monorepo build clean | `c758ec5` |
| 2026-07-12 | CI-fix | WO-P15's push (`f7132a5`) went red on `build-and-test (20)` only (Node 22 passed from the identical commit) — a pre-existing, unrelated flake in `apps/api/src/checkout-email.test.ts` ("sends seat invitation email..."), not caused by WO-P15's 100%-web diff. Root cause: 4 call sites used `await new Promise(r => setTimeout(r, 50 or 100))` then checked `getEmailDeliveries(...)` — a fixed-delay guess racing an async, fire-and-forget email-send pipeline; under CI load on the slower runner it lost the race. Replaced all 4 with a `waitForDeliveries()` helper that polls the real condition (every 20ms, up to a 2s budget) instead of guessing a delay. Verified: the file passes in isolation (11/11); apps/api typechecks clean. **Disclosed, not fixed:** `apps/api/src/github-webhook.test.ts` has the identical anti-pattern at 6 call sites (fixed 250ms delays waiting on the analysis-on-push background pipeline) — didn't fail in this run, but is the same latent risk with an even more complex pipeline behind it; left as a follow-up since a clean poll-helper there needs to handle both Map-entry and stdout-spy-content conditions, a big enough design difference to warrant its own unit rather than folding into this fix | `851360f` |
| 2026-07-12 | WO-P16 | Changelog ("#changelog", public, footer-only like "terms"): renders the repo's own CHANGELOG.md (GET /v1/changelog, WO-A4) live, split into one card per `## [version]` heading, `###` subsections rendered as real headings via MarkdownLite. Wired the footer's version badge (`PageFooter.tsx` had an explicit "WO-P16 links it to #changelog" placeholder comment) from a plain span to a real nav button. **Honesty finding:** CHANGELOG.md is hand-maintained, not generated — its own git history shows it hasn't been touched since 2026-04-16 (`[0.5.0]` is its newest entry), while `APP_VERSION`/the live API have been at 0.5.3 all session; a 3-month gap spanning this entire loop's own work plus whatever shipped before it. Rather than backfill that history (a large, separate undertaking — reconstructing 3 months of releases from git log is its own unit, not a page-build) or silently badge the file's newest entry "Current" (overstating how up to date the log is), the page compares the newest logged version against the real `APP_VERSION` constant and discloses the gap with a Callout when they don't match — same "verify against runtime, disclose rather than hide" standard this whole loop runs on. **Also fixed:** `app-routing.test.tsx`'s all-public-pages PageFooter test hit vitest's default 5000ms budget for the first time (measured 6.1s, reproducible in isolation, not contention) — each public page WO-P13 through this unit added to that one test's iteration list pushed it further over a threshold it was always going to cross; gave the test an explicit 20s budget with a comment explaining why, rather than another bare bump next time a page ships. 6 new tests (ChangelogPage.test.tsx); full apps/web suite (517) reverified green; full monorepo build clean | `074424d` |
| 2026-07-12 | WO-P17 | Status ("#status", public, footer-only): live timed probes against GET /v1/health, /v1/health/live, /v1/health/ready (each probe's latency is measured client-side via `performance.now()` around the actual call, never a canned number); subsystem checks (database ok/error — a real binary; payment rail shown as plain informational text, deliberately with NO ok/error dot, since `handleReadiness`'s own comment says absence "is NOT an outage" — a status page must not editorialize a legitimate config state as broken); real MCP call-volume stats (GET /v1/stats); a session-local "time since you opened this page" ticker, explicitly labeled as not server uptime. Wired both disclosed placeholders: PageFooter's "Status" link (was an external `<a>` to the raw health JSON) and StatusBar's connection dot now both navigate to #status in-app. Per Honesty H4 (this unit's own spec line): no incident history, no uptime percentage — the page says so explicitly rather than implying data that isn't stored. **Found and fixed a real, more serious honesty violation while scoping this unit's own GET /performance dependency:** `handlePerformance` was reporting a hardcoded 99.87% "MCP success rate" (comment: "estimate... based on system reliability"), a fixed 0.13 error rate, and a `mcpCallsTotal * 3, floor 1000` request-count guess — one of which discarded the real `totalRequests` this same function already computed from the latency histogram two lines earlier, in favor of the fabricated number. Fixed to source `total_requests`/`error_rate` from metrics.ts's real `recordRequest()` counters (already tracked, just never used here), and to report `null` — not an invented number — for the two fields nothing in this codebase actually measures (no per-category MCP failure tracking exists, no "active probes" concept exists anywhere). Also surfaced `mcp_calls_today`, a real value the handler computed but never included in its own response. 3 new regression tests locking in "never fabricates" + "real counters, not the old hardcoded guesses". **Disclosed, not fixed — flagged for owner attention:** `GET /performance/reputation` (a sibling endpoint, not in this unit's API list) is substantially worse: `last_probe` is `new Date(Date.now() - Math.random() * 3600000)`, a **freshly randomized fake timestamp on every call**, comment: "simulate recent activity" — this isn't an estimate, it's manufacturing the appearance of live activity that isn't happening. Its `reputation_score` averages five hardcoded constants (95/98/99/90/97) plus one weak real signal, presented as a computed trust score to any caller — including autonomous agents this product explicitly markets to for trust/purchasing decisions. Not touched in this unit: it requires a product decision (what should "reputation" honestly mean here, or should the endpoint be retired) that goes beyond "build the status page," and isn't one of WO-P17's listed APIs. Flagged directly to the operator in-conversation, not just here. 8 new tests (StatusPage.test.tsx); full apps/web suite reverified green; full monorepo build clean; apps/api well-known-handlers.test.ts (83/83) reverified green | `64cb43d` |

| 2026-07-12 | H4.1 | MCP tool-description accuracy audit, all 36 tools in mcp-tools.ts cross-checked against real behavior/args/pricing/gating in mcp-tool-impls.ts (4 parallel agents by theme: core-analysis/discovery, commerce/compliance, config-gated platform services, op-gated platform services). 26 real discrepancies found and fixed. Standouts: `iliad_web_research_crawl`'s advertised pricing was stale by 25x AND described a per-page billing model that no longer exists in code (real: flat $0.01/call, cut from $0.25/page in a prior commit that never touched the description); `get_artifact` claimed a JSON `{content:...}` wrapper its outputSchema/example never actually returns (real return is raw file bytes) — turned out the wrapper existed only to satisfy a separate catalog-wide "every outputSchema.type must be object" test, so that test gained a narrow, documented exception rather than re-fabricating the wrapper; `search_and_discover_tools`'s outputSchema required a `relevance` field the real code has never produced (real field: `score`) and both its examples were fully fabricated shapes; `discover_commerce_tools` hardcoded a flat "$0.50/call" for every non-free tool (real prices range $0.01–$0.50) and mis-priced the actually-free `improve_my_agent_with_axis` as paid — fixed by deriving pricing from the real per-tool table instead of a literal, with a `METERED_MCP_TOOLS` runtime array added alongside the existing (compile-time-only) `MeteredMcpTool` type so "is this genuinely metered" no longer has to guess from PRICING_TIERS row presence (which also holds a dead entry for `improve_my_agent_with_axis` that no runX handler reads — this dead entry is exactly what made the first version of this same fix wrong, caught by the new price-source test itself). 12 tools (iliad_object_storage/embeddings/llm_inference/transactional_email/web_search/vector_database/analytics/hygiene/document_parsing/code_sandbox/speech_to_text/text_to_speech) stated only their engineer-mode price and omitted standard/lite pricing entirely — all given explicit pricing text. 2 tools (`iliad_analytics`, `iliad_web_search`) claimed "persistent via SQLite" years after both migrated to Postgres. `closer`'s description implied a directory-upload tool; it only ever accepts a pre-existing snapshot_id (inputSchema also lacked `required`, now added). `get_referral_credits` was annotated readOnlyHint:true despite mutating on every call (creates rows on first use). `prepare_agentic_purchasing`'s `focus_areas` was described as narrowing the analysis; it has zero effect on what's generated (echoed back only) — text corrected rather than silently building the filtering it never had. Plus 6 missing-outputSchema-field fixes (iliad_vector_database's engineer/semantic_dedup key split, iliad_hygiene's patch+sarif, iliad_code_sandbox's attestation, iliad_speech_to_text's diarization, iliad_text_to_speech's persona) and a Firecrawl-backend disclosure that had to be worded around a pre-existing, deliberate "description must never say Firecrawl" docs-honesty test — disclosed the not-configured failure mode without naming the backend, satisfying both. New regression tests: shareable_manifest.free_tools can never again drift from the top-level free_tools list (the bug that started this unit — `prepare_agentic_purchasing_preview` was missing from a hand-typed duplicate array); every tool discover_commerce_tools reports as genuinely metered must state a $ price in its own catalog description. **Disclosed, not fixed (out of this unit's wording-only scope):** lite-mode's promised behavioral restrictions (the `lite_description` text in packages/mpp, which IS surfaced to agents via the 402 negotiation body) are unenforced in all 12 iliad_* handlers' actual code — a functional gap, not a description bug, found independently by two of the four audit agents; `destructiveHint` is hardcoded `false` for all 36 tools regardless of real capability (systemic, needs its own per-tool capability pass); 2 tools' outputSchema `required` arrays don't account for their own `_not_configured` response shape (a schema-design question, not a wording fix). Verified: typecheck clean, monorepo build clean, 399 tests green across the 9 test files touching mcp-tools.ts/mcp-tool-impls.ts/mcp-runtime.ts (mcp-server.test.ts, mcp-commerce-tools.test.ts, web-research-sovereign.test.ts, prepare-purchasing.test.ts, network-token.test.ts, counts-consistency.test.ts, mcp-inband-settlement.test.ts, mcp-embeddings.test.ts, purchasing-readiness-analysis.test.ts). CI green on push (build-and-test 20+22, Compliance Check, deploy-web all success). **Live-probe note:** axis-api on Render had not yet picked up this deploy at probe time (live discover_commerce_tools still returned the old flat pricing string) — same independent/lagging Render docker-from-Git cadence disclosed in WO-P13; not a code defect, CI-verified correct | `6e2dee9` |
| 2026-07-13 | H4.2 | Error-code catalog. Research first found the spec's literal ask ("every ErrorCode.*") undersells the real landscape: 4 distinct, non-overlapping error-code taxonomies exist (28-value REST `ErrorCode` registry in logger.ts; 5 JSON-RPC protocol codes; 6-value MCP tool-call `ErrorCategory`; 8-value internal `SovereignFetchError` that never reaches the wire as-is, so excluded from agent-facing docs as it would be actively misleading). Built `ERROR_CODE_CATALOG` (29 entries: 28 existing + a new `BODY_TOO_LARGE`) directly adjacent to `ErrorCode` in logger.ts — status(es), retryable guidance, description per code — plus a small `MCP_ERROR_CATEGORY_CATALOG` next to `categorizeError` in mcp-runtime.ts for the other agent-facing surface. Exposed both via a new `GET /v1/error-codes` (ENDPOINT_COUNT 154→155, propagated to all 6 CI-tested mirrors: README.md, apps/web/src/config.ts, LAUNCH_CLAIMS.yaml, launch-content.md ×2, AXIS_Board_Pitch.md), which both a new `/llms.txt` "## Error Codes" section and a new DocsPage "Error Codes" tab render from live (matching WO-P13's live-fetch convention) instead of each hand-duplicating the data. Research surfaced 2 real bugs, both fixed: `PAYLOAD_TOO_LARGE` was defined but dead (zero call sites) while the real 413 body-size call site used an unregistered raw string `"BODY_TOO_LARGE"` — added to the registry and swapped in at all 3 raw-string sites in router.ts (`BODY_TOO_LARGE`, `INTERNAL_ERROR`, `NOT_FOUND`) that happened to already match by string coincidence; and apps/web/src/api.ts's `isPersistenceCreditsError` checked `err.errorCode === "persistence_credits_required"` (lowercase) when the real wire value is uppercase (`ErrorCode.PERSISTENCE_CREDITS_REQUIRED`) — the errorCode branch had silently never matched real traffic since it shipped, and a pre-existing test had encoded the wrong lowercase value as the passing case (fixed alongside, plus a new regression test pinning the real uppercase value). Multi-status codes represented honestly rather than picked-one-arbitrarily: `TIER_REQUIRED` (402 or 403 depending on call site), `INTERNAL_ERROR` (500/502/503). Two new regression tests guard the catalog can't silently drift from `ErrorCode`/`ErrorCategory` again (logging.test.ts, new mcp-runtime.test.ts — the latter also pins each category's `retryable` flag against `categorizeError`'s real hardcoded returns, not just a hand-copied guess). Caught and fixed my own mistake mid-unit: the first llms.txt content test landed in the wrong `describe` block (GET /v1/docs.md instead of GET /llms.txt) via an ambiguous anchor string — surfaced immediately by the actual CI-mirroring local test run, moved to the correct block, re-verified. 205 API tests + 145 web tests green across all touched/adjacent files; typecheck and full monorepo build clean. **Disclosed, not fixed:** no REST response ships a literal `retryable` field anywhere in this API — the catalog's retryable column is editorial guidance derived from HTTP-status convention and real call sites, clearly labeled as such in both the code comment and the DocsPage copy, not a claim about the wire format; wiring an actual `retryable`/`retry_after` field onto every `sendError` call site would be a much larger change (283 call sites) than a documentation unit should attempt. CI green on push (build-and-test 20+22, Compliance Check, deploy-web all success). **Live-probe note:** axis-api on Render had not yet picked up this deploy at probe time (GET /v1/error-codes returned 404) — same independent/lagging Render docker-from-Git cadence disclosed in WO-P13/H4.1; not a code defect, CI-verified correct | `9ab4991` |
| 2026-07-13 | H4.3 | `_usage` envelope documentation. Traced the full real shape from mcp-server.ts's two construction sites (success + idempotent-replay) and its error-path sibling: every successful `tools/call` (free or paid, authenticated or anonymous) carries `_usage: {tier, credits_remaining, usage_credits, compensation, tool}` — `credits_remaining` is the persistence-credit balance (getPersistenceBalance, spent on version-diffing), a genuinely separate pool from `usage_credits` (getUsageCreditSummary's monthly blended-credit-pool: plan_id/month_key/monthly_allowance/included_credits_used/included_credits_remaining/overage_credits_this_month) — the two were previously undocumented as distinct systems anywhere agent-facing. `compensation` (owed_cents/credited_cents, from H2.4's compensateAndSummarize) is a running lifetime total reported on every success; found and documented a subtle-but-important sibling: a FAILED call never carries `_usage` at all — instead `_error: {code, retryable}` (H4.2's MCP category taxonomy) and, only when that specific call had already collected in-band cash before the tool threw, a one-off top-level `_compensation: {entry_id, amount_cents, status}` receipt for exactly that incident. The two easily-conflated fields (`_usage.compensation`'s running total vs. `_compensation`'s one-off receipt) are now explicitly distinguished in both docs surfaces. Added a new ForAgentsPage card (3 worked JSON examples: success, idempotent replay, error-with-compensation) and a new `/llms.txt` "## MCP Response Envelope" section (both were completely silent on this before — zero prior mentions of `_usage`/`_idempotent_replay`/`compensation` anywhere agent-facing). 6 new tests (pages.test.tsx's new ForAgentsPage describe block, agent-discovery.test.ts's llms.txt content check) confirm the documented field names actually appear. 121 tests green (51 web + 70 api); typecheck and build clean on both packages. CI green on push (build-and-test 20+22, Axis Compliance: A, Compliance Check, deploy-web all success). **Live-probe note:** GET /llms.txt on axis-api (Render) still serves pre-H4.2 content (missing even the "## Error Codes" section) — confirms the same independent/lagging Render docker-from-Git cadence disclosed in WO-P13/H4.1/H4.2 is still catching up, now several commits behind; not a code defect, CI-verified correct on every push since. ForAgentsPage (Cloudflare Pages via CI's own deploy-web job, which succeeded) could not be content-probed by curl — it's a client-rendered SPA, so curl only ever sees the empty shell regardless of deploy status; this is a probe-methodology limit, not a finding | `2589166` |
| 2026-07-13 | **CORRECTION + fix** | **Owner flagged that GitHub Actions had hit its monthly minutes budget and directed a manual Render deploy.** Investigating that surfaced the real story: this loop's last **22 consecutive** live-probe "not a code defect, Render is just lagging" disclosures (every unit from WO-A4 through H4.3) were **wrong** — Render wasn't lagging, it was **failing to build entirely**, every single time, since commit `52c7a522` (2026-07-12T14:25, the deploy right after WO-A4). Root cause: WO-A4 (`51f7533`) added `COPY CHANGELOG.md ./` to the root Dockerfile to fix a 500-in-prod bug (the new `GET /v1/changelog` reads that file at runtime) but never checked `.dockerignore`, which already blanket-excluded `*.md` with only `!README.md` as an exception — so the file the new COPY instruction needed was invisible to the build context. Every deploy attempt failed at that exact step ("failed to calculate checksum ... CHANGELOG.md: not found", exit 1, ~10s — before any real build work) and CI's `build-and-test` job never caught it because it doesn't build the Docker image (`docker-build`/`deploy-api` are `workflow_dispatch`-only break-glass jobs by design — Render builds independently, docker-from-Git, on its own schedule, which is why this read as "lag" for 22 straight units instead of "broken"). Net effect: the live API was frozen at `c472d1f4` (RT.8/MONEY GATE close) for ~14 hours while all of Phase H3's web-completion work (WO-A4 through WO-P17) and H4.1-H4.3 shipped to `main`, passed CI, and never once reached production. **Fixed** (`61216f2`): added `!CHANGELOG.md` to `.dockerignore` alongside the existing `!README.md`; verified locally with a real `docker build` completing past the previously-failing step before pushing. Pushed, then manually triggered the Render deploy hook directly via the API (`POST` to the service's deploy-hook URL — the GH Actions break-glass path was unusable given the owner-reported Actions budget cutoff); polled the Render API directly (`GET /v1/services/.../deploys`) rather than trusting `gh run` output, since that's the whole class of signal that was misleading this loop. Deploy went `queued` → `build_in_progress` → `update_in_progress` → **`live`**. **Verified live, for real this time:** `/llms.txt` now serves both "## Error Codes" (H4.2) and "## MCP Response Envelope" (H4.3); `GET /v1/error-codes` returns all 29 codes including the new `BODY_TOO_LARGE`; `GET /v1/changelog` returns 200 with real content (the original WO-A4 bug is also now genuinely fixed, not just code-complete); `discover_commerce_tools` reports `iliad_object_storage` at the correct derived `$0.01/call` instead of the old flat `$0.50` (H4.1's fix). **Process lesson for the rest of this loop:** a green CI run and a passing local build are necessary but not sufficient evidence of a live fix — `docker-build`/`deploy-api` showing "skipped" in `gh run view` is normal/expected (by design), but that means CI genuinely cannot see Render's build outcome at all; the only way to confirm a deploy actually landed is to poll the Render API directly (`GET /v1/services/<id>/deploys?limit=1`) or content-probe the live endpoint for the specific new thing that was shipped, not just check that *a* response comes back | `61216f2` |
| 2026-07-13 | H4.4 | begin-loop REST export. This repo already had its own root `begin.yaml`/`continuation.yaml` (the files the generated-loop feature was originally generalized from) but nothing exposed them over the API or pointed a crawling agent at them. Added `GET /begin.yaml` / `GET /continuation.yaml` (verbatim `readFileSync`, mirroring `handleChangelog`'s established WO-A4 pattern exactly) and a README pointer alongside the existing "Plus the begin loop" section. ENDPOINT_COUNT 155→157, propagated to all 6 CI-tested mirrors. Applying the previous row's lesson immediately: caught, before pushing, that the Dockerfile had **no COPY instruction for any root-level YAML file at all** — these two new endpoints would have silently 500'd in production exactly like the CHANGELOG.md incident, just one stage earlier in the same class of bug. Fixed in the same commit (`COPY CHANGELOG.md begin.yaml continuation.yaml ./`). This time verified for real before pushing: a full local `docker build` (all three stages, not just `--target deps`) completed clean, then the built image was actually **run** as a container and both new endpoints were curled against it (200, correct content-type, correct byte size) alongside a `/v1/changelog` regression check — not just "the build succeeded," but "the running server serves the right bytes." One genuine hiccup during verification: Docker Desktop's daemon died mid-build (unrelated to this change) and a retry hit a transient `registry.npmjs.org` failure fetching corepack/pnpm itself — both clearly infra flakiness, not code, and resolved by restarting Docker Desktop and retrying. New begin-loop-export.test.ts (6 tests) + one incidental fix: openapi.test.ts's path-shape allowlist test needed the two new root paths added (a real, correctly-strict test catching an genuinely new non-`/v1/` route, not a bug in the test). Pushed, deployed manually via the Render API (GH Actions still budget-constrained), polled to confirmed `live`, then live-probed both new endpoints for real (200, correct content-type/size) plus confirmed the README's `GET /begin.yaml` mention rendered. **Disclosed, not fixed:** continuation.yaml's own content already self-discloses it's a stale session-121 (2026-06-11) snapshot with outdated counts in its own header comment — serving it verbatim via REST doesn't add new dishonesty (the file is already honest about its own staleness) but does widen the surface where that staleness is visible; refreshing its content is a separate, much larger undertaking than "export it," out of this unit's scope | `f1c4bc2` |
| 2026-07-13 | H4.5 | probe-intent quality pass. Location correction: the spec pointed at `apps/api/src/intent.ts`, but that file only classifies User-Agent strings (who's calling) — the actual natural-language intent→tool matcher lives inline in `handlers.ts`'s `handleProbeIntent`. Ran 20 realistic intents against the live pre-fix endpoint (real HTTP calls, not a mental simulation) spanning the full capability surface, not just the 4 categories the classifier covered: **12 of 20 (60%) fell through to the generic `discover_agentic_purchasing_needs` commerce fallback**, including intents with nothing to do with commerce ("transcribe this audio", "run this Python snippet", "send a transactional email") and two cases where AXIS has an exact-match tool that went completely unmentioned ("what's my referral code" → should hit `get_referral_code`; "network token lifecycle" → should hit `iliad_network_tokenization`). Also found 2 false positives (embeddings/BM25-search intents routed to the program-catalog search instead of `iliad_embeddings`/`iliad_web_search`) and 1 same-priority miscall (a Stripe-dispute intent routed to the general `prepare_agentic_purchasing` instead of `score_dispute_readiness`, because the broad commerce regex's `stripe` keyword outranked nothing — there was no dispute-specific rule at all). Added 16 new specific-tool regex rules, each covering exactly one demonstrated gap (not a speculative full 36-tool build-out) — every `iliad_*` platform tool that had zero routing coverage now has a rule, checked ahead of the existing broad/generic rules so a precise match wins `call_next` instead of being drowned out. Also fixed the fallback itself: reordered so an unmatched intent leads with the honestly-generic `search_and_discover_tools` instead of presuming a commerce need. All 20 intents re-verified via a new deterministic test (`agent-discovery.test.ts`, `it.each` over the same 20 strings, asserting the exact `call_next` — pins the fix, not just "returns something"), plus a dedicated fallback-ordering test. 91/91 tests pass; typecheck clean. **Environment note, not a code issue:** Docker Desktop's crash during H4.4's verification had also silently removed the local `axis-test-pg` test container entirely (not just stopped it) — recreated it (`postgres:16`, matching `ci.yml`'s exact config) before this unit's tests could run; disclosing since a future session hitting `ECONNREFUSED :5433` on a fresh checkout of this machine should recreate, not just `docker start`, the container. CI green on push. Deployed manually via Render API, polled to confirmed `live`, then live-probed both previously-broken intents against production directly: "Transcribe this audio file to text" → `iliad_speech_to_text` (was `discover_agentic_purchasing_needs`); "What's my referral code..." → `get_referral_code` (was `discover_agentic_purchasing_needs`) | `ac72c91` |
| 2026-07-13 | H4.6 | llms.txt freshness test. Audited `handleLlmsTxt` line by line for hand-typed numbers/enumerations that already have a canonical source elsewhere in the codebase, on the same "derive, don't duplicate" standard as H4.1's `METERED_MCP_TOOLS` and H4.2's `ERROR_CODE_CATALOG`. Found 3 real drifts, all fixed: (1) the MCP tool list was a hand-enumerated string naming only 20 of the real 36 tools (`MCP_TOOL_COUNT`) — every `iliad_*` platform tool added since that string was last touched (object storage, embeddings, LLM inference, transactional email, web search, analytics, hygiene, document parsing, code sandbox, speech-to-text, text-to-speech, network tokenization, and others) was silently absent from what an agent reading `/llms.txt` would ever learn exists; fixed by deriving the list from `MCP_TOOLS.map(t => t.name).join(", ")`. (2) "Chains 8 programs" vs. the real `PURCHASING_PROGRAMS.length` of 10 — fixed by interpolating the live length instead of a literal. (3) "3 snapshots/day on pro programs" was wrong on two axes at once: the free tier's real quota is 10 snapshots/**month** (`TIER_LIMITS.free.max_snapshots_per_month`), and pro programs aren't quota-limited at all — they're entitlement-gated (need a paid tier), confirmed by reading `handlers.ts`'s `onlyFreePrograms` branch, which shows free-only-program requests bypass the snapshot-quota check entirely; rewrote the line to state both facts accurately ("unlimited on search/skills/debug, 10 snapshots/month total, pro programs need a paid tier") instead of a once-true, since-drifted number. All three now read from `MCP_TOOLS`, `PURCHASING_PROGRAMS.length`, and `TIER_LIMITS.free` respectively — the same values the live endpoints themselves enforce, so this class of drift can't silently recur. 4 new regression tests in `agent-discovery.test.ts`'s `GET /llms.txt` block, matching the `count-honesty.test.ts` regime: every real tool name must appear in the body (not a stale subset), the program-count and snapshot-quota numerals must match their live constants, not a hand-copied literal. 95/95 tests green (`agent-discovery.test.ts`, full file including all pre-existing H4.1–H4.5 tests); typecheck clean (`tsc`, apps/api, exit 0). This unit touches no Dockerfile/build-context files, so — matching the precedent set for H4.5 — full Docker-build verification was not required; the standard typecheck + scoped-vitest cycle applies. **Disclosed, not fixed:** the audit covered `handleLlmsTxt` only (this unit's explicit scope); `handleDocsGeneration`/DocsPage and other agent-facing surfaces may hold similar once-true hardcoded numbers and were not swept — a candidate for a future unit if this class of drift recurs elsewhere. **CI note (real deviation, disclosed):** the first push (`3972d5f`) went CI-red on `build-and-test (22)` only — `apps/api/src/billing-flow.test.ts`'s "myanalytics summary" test, unrelated to this unit (H4.6 touched zero files that test exercises). Root cause: `recordApiCall` runs from a fire-and-forget `res.on("finish", async () => ...)` handler in `router.ts` that commits after the response already returned; the sibling test one block above already waits 200ms for this exact race, this test never got it — a pre-existing gap, not a regression (confirmed: the immediately prior commit `71cb989` was green on both Node matrix legs; only the Node-22 leg, which this session has now seen flake twice on two unrelated tests, hit the race). Fixed in a separate small commit (`2599c84`, 4 lines, under rule 17's threshold) rather than folding into this unit's commit. Pushed, CI green on `2599c84` (build-and-test 20+22, deploy-web; docker-build/deploy-api correctly skipped, break-glass only). Deployed manually via the Render API (GH Actions still budget-constrained), polled to confirmed `live` in 3 polls (~45s — the fastest deploy this loop has seen). **Live-probed for real:** `GET /llms.txt` on production now enumerates the full 36 tools by name (was 20), reads "Chains 10 programs" (was 8), and reads "unlimited on search/skills/debug programs, 10 snapshots/month total (pro programs need a paid tier)" (was "3 snapshots/day on pro programs") — all three confirmed byte-for-byte against the live response, not inferred from the deploy succeeding | `3972d5f` (+ `2599c84` de-flake) |
| 2026-07-13 | H5.1 | Cross-page a11y pass — audit + tier-1 fixes. **Shell-level (App.tsx/index.css):** the shell had no skip-link (a keyboard user tabbed through the full rail+sidebar nav on every single page load, WCAG 2.4.1) and no focus management on route change (a screen-reader user got no announcement that new page content had loaded — the classic SPA soft-navigation gap). Added a skip-link (absolutely positioned so it doesn't disturb the shell's CSS grid) and a route-keyed `useEffect` that moves focus to `#main-content` (tabIndex=-1) on every navigation after the first. 4 new regression tests (app-routing.test.tsx) assert the skip-link's DOM position, `#main-content`'s attributes, that focus is NOT stolen on initial load, and that it IS moved on route change (jsdom/happy-dom genuinely tracks `document.activeElement`, so this is a real behavioral assertion, not a snapshot). **Audit:** dispatched 4 parallel read-only agents across the remaining 27 pages (grouped by theme), each briefed with the exact Skeleton/EmptyState/Callout contracts from WO-F4 and asked for file:line evidence, not impressions. Found roughly 15 HIGH-severity + 70 MEDIUM + 20 LOW findings — far more than any prior unit in this loop, because "every page" is a much wider net than "every MCP tool" (H4.1) or "every ErrorCode" (H4.2). Rather than try to land all ~100 in one commit (which would blow past rule 17's unit-sizing spirit and risk a half-finished giant diff), triaged into tier-1 (fixed here) and a disclosed follow-up bucket (H5.1b, new STATE row + Phase H5 spec bullet with full itemization). **Tier-1 fixes shipped:** (1) HIGH — HelpPage.tsx's troubleshooting accordion was a `<div onClick>` with no role/tabIndex/keyboard handler, completely unreachable by keyboard; converted to the same native `<button aria-expanded>` pattern QAPage/DocsPage already use correctly. (2) `DangerButton` (3 independent page-local copies by design, not a shared component — VersionsTab.tsx, ProjectsPage.tsx, SettingsPage.tsx — confirmed via grep, not assumed) unmounts its own just-clicked button on every arm/disarm transition, silently dropping keyboard focus to `<body>`; fixed all 3 identically with a ref+`useEffect` pattern that moves focus to Cancel (the safer default for a destructive-action confirm) on arm, and back to the label button on disarm. (3) `ProbeIntentDemo.tsx` (shared by McpPage + PlaygroundPage) had a bare-text error with no `role="alert"` and a bare "no match" string instead of `EmptyState` — fixed once, fixes both host pages' embeds. (4) `MyAnalyticsPage.tsx` was the worst single-page offender: it replaced its ENTIRE tree — including the Window select and Refresh button — with a bare spinner on initial mount, every window-change, AND every manual refresh, meaning a keyboard user lost focus on every single interaction with the page, not an edge case. Restructured so the controls stay permanently mounted (disabled while loading, not unmounted), the data region alone shows a `Skeleton`+`role="status"` while loading, the error path uses `Callout tone="danger"` instead of a bare `<div>`, and the two tables that had literally no empty-guard (API Status Mix, All Calls By Endpoint — confirmed via reading the code that the sibling "Programs Used" table right above them DID have one, proving the gap was inconsistency, not a deliberate design) now match. (5) `ProjectPage.tsx`'s `NextStepsCard` Dismiss button unmounted its own whole card on click; fixed with a persistent `tabIndex={-1}` anchor that receives focus instead of losing it to `<body>`. (6) A systematic sweep across ~15 files (ChangelogPage, StatusPage, McpPage, DocsPage, AnalyzePage, RunnerPage, ProjectsPage, AccountPage, AccountDashboardPage, SettingsPage, CommercePage, PlansPage, PaidCheckoutPage, AdminPage, UsagePage, WebResearchPage, QAPage): bare `Skeleton` usages wrapped in `role="status" aria-live/aria-busy`; `Callout tone="warning"` corrected to `"danger"` on genuine hard-failures (deliberately left alone where a soft/background-refresh warning was clearly intentional, e.g. AccountDashboardPage's stale-data notice); hand-rolled empty-collection text replaced with `EmptyState`; raw `error.message`/bare-`<p>` error text replaced with `Callout`; loose `instanceof Error` guards tightened to `instanceof ApiError` where it was cheap and safe (left alone in DocsPage after confirming via `api.ts`'s `fetchResponse`/`humanMessage` that `.message` there is already human copy by construction, not raw passthrough — a real judgment call, not a blind sweep); plus cheap attribute fixes (5 unlabeled SettingsPage inputs → `aria-label`; Monthly/Annual toggle `aria-pressed` on PlansPage + PaidCheckoutPage, the latter restructured into `<fieldset><legend>` to fix an orphaned, unassociated `<label>`; `CodeBlock`'s copy-button `aria-label` now tracks the "Copied!" state instead of staying frozen — done carefully to preserve `DocsPage`'s custom `copyLabel` override rather than hardcoding "Copy code"; `ExamplesPage`'s expand button given the `aria-expanded` its sibling accordions already had). Execution: shell fix + the 6 items above done directly (shared components and the highest-judgment single-page rewrite); the systematic sweep dispatched as 4 parallel fixer agents, each given a closed, pre-decided, file:line-precise checklist derived from the audit (no open-ended judgment delegated) and a disjoint file set from the other 3 (shared files reserved for direct handling to avoid concurrent-edit conflicts) — verified after the fact by reading a sample of the trickier diffs myself (PaidCheckoutPage's fieldset restructure, CommercePage's EmptyState-wrapping-a-button usage, AdminPage's first-ever primitives import) rather than trusting the agents' self-reports alone. **Verified:** full apps/web suite 536/536 green (not just each fixer's scoped subset — a full run across all 26 touched files, to catch cross-file interactions the parallel dispatch could have missed), typecheck clean, `apps/web`'s real production build clean (`tsc -b && vite build`, the >500kB chunk warning is pre-existing and already tracked as H5.3's own scope item), full monorepo build clean. **Disclosed, not fixed — see new STATE unit H5.1b:** 5 destructive actions across Settings/Usage still fire with zero confirmation step (a real safety gap, not just a11y polish); UpsellModal has no dialog semantics/focus-trap/Escape/focus-return; several more self-disabling buttons lose focus the same way DangerButton/MyAnalyticsPage did, needing one consistent pattern rather than more one-offs; 4 pages' tab strips are keyboard-*reachable* but not full ARIA tabs (no roving tabindex); 22 of 28 pages have zero `<h1>` anywhere (confirmed via direct grep, the shell supplies none either) — needs per-page judgment on which heading to promote, not a mechanical insert; AdminPage's 6 tables still lack TableWrap/EmptyState (only its loading/error states were fixed here); the codebase has zero `aria-invalid`/`aria-describedby` usage anywhere. **No API changes this unit** — pure `apps/web`, so "deploy" here means Cloudflare Pages via CI's `deploy-web` job only; no Render trigger needed, no Docker verification required. CI green on push (build-and-test 20+22, Axis Compliance: A, deploy-web all success; docker-build/deploy-api correctly skipped). **Live-probed for real:** fetched the live JS bundle from `https://iliad.trustfabric.ai` (the real documented URL — a first guess at the default `axis-web.pages.dev` subdomain turned out to belong to a completely unrelated, unaffiliated production app on someone else's Cloudflare account, since the plain slug was already taken; corrected by checking this session's own memory for the actual project URL before probing further) and confirmed it contains both "Skip to main content" (the new skip-link) and "No purchasing kit yet" (CommercePage's new EmptyState title) — genuine new-code evidence, not inferred from the deploy succeeding. Bundle byte size (770,725) matches the local production build almost exactly, confirming this is the fresh build, not a stale cache | `2665ba9` |
| 2026-07-13 | H5.1b (partial: a+b) | a11y follow-ups from the H5.1 audit — items (a) and (b) only; (c)-(g) remain pending, see Phase H5 spec for the full itemization. **(a) Destructive-action confirmations:** SettingsPage's 4 no-confirm actions (Revoke API key, Remove GitHub token, Delete webhook, Revoke seat) now go through the existing arm/confirm `DangerButton` pattern instead of firing immediately on one click — added per-row busy-state tracking (`revokingKeyId`/`deletingTokenId`/`deletingWebhookId`/`revokingSeatId`, mirroring the existing `programBusy` convention already in this file) since none of the 4 handlers previously tracked in-flight state at all. The seat-revoke handler was inline in JSX with no name; extracted to `handleRevokeSeat` for consistency with its 3 siblings. UsagePage's Cancel Subscription got the same treatment, but UsagePage had no page-local `DangerButton` at all (it's a deliberate per-page copy, not shared — see VersionsTab/ProjectsPage/SettingsPage's identical comment) — added a 4th copy, byte-identical to the other 3's H5.1-fixed version, with copy customized only for context ("Yes, cancel"/"Never mind" instead of generic "Yes, confirm"/"Cancel"). **(b) UpsellModal dialog semantics:** this modal (triggered on 402 responses across 4 pages — AnalyzePage, CommercePage, ProjectPage, RunnerPage) had zero dialog semantics: no `role="dialog"`, no `aria-modal`, no focus trap, no Escape-to-close, no focus-return to the trigger on close — a keyboard user could Tab straight out into the (still-visible-behind-the-overlay) page content. Fixed once, fixes all 4 call sites: `role="dialog"`/`aria-modal="true"`/`aria-labelledby` (pointing at the modal's own `h2`), focus moves to the dialog container itself on open (not a specific button, to avoid an accidental Enter/Space activating whatever happened to be focused first), Tab/Shift+Tab now wrap within the dialog's real focusable elements (recomputed fresh on every Tab press via `querySelectorAll`, not captured stale at mount), Escape calls `onClose`, and the previously-focused element (the trigger) regains focus when the dialog unmounts. New dedicated `UpsellModal.test.tsx` (6 tests): dialog ARIA shape, focus-on-open, focus-restore-on-close, Escape, and both directions of the Tab-wrap (jsdom/happy-dom genuinely tracks `document.activeElement`, so these are real behavioral assertions). **Verified:** full apps/web suite 542/542 green (536 + 6 new), typecheck clean, production build clean, full monorepo build clean. **Scope note:** deliberately did NOT attempt (c)-(g) in this pass — (c)'s self-disabling-button focus-retention pattern touches 4 more files (SettingsPage's profile-save/program-toggle, CommercePage's Generate/Regenerate, PaidCheckoutPage's submit, UsagePage's credit top-up) and deserves one consistent implementation, not another round of one-offs; (d)-(g) are each their own structural undertaking (ARIA tabs roving-tabindex, a 22-page `<h1>` sweep needing per-page judgment, AdminPage's full primitives migration, codebase-wide `aria-invalid`/`aria-describedby` wiring) — all still itemized in the Phase H5 spec, unchanged. No API changes — pure `apps/web` again. CI green on push (build-and-test 20+22, deploy-web all success; docker-build/deploy-api correctly skipped). **Live-probed for real:** fetched the live JS bundle from `https://iliad.trustfabric.ai` (new hash, confirming a fresh deploy) and confirmed it contains "Anything using it stops working immediately" (the Revoke-API-key confirm copy), "Never mind" (UsagePage's new DangerButton cancel label), and "keep access until the current period ends" (Cancel Subscription's confirm copy) — genuine new-code evidence. Bundle size (772,927 bytes) matches the local production build almost exactly | `e1c934f` |
| 2026-07-13 | H5.1b (partial: +c) | a11y follow-ups from the H5.1 audit — item (c) only; (d)-(g) remain, see Phase H5 spec. **Consistent focus-retention pattern:** the remaining 5 self-disabling-button call sites (SettingsPage's profile-save and per-program toggle, CommercePage's Generate/Regenerate, PaidCheckoutPage's submit, UsagePage's credit top-up) each disable the exact button a keyboard user just activated, which browsers forcibly blur — none of the 5 restored focus afterward. Two shapes, since one busy boolean can drive either one button or N: (1) a new, small, reusable `useFocusRetention(isBusy)` hook (`apps/web/src/useFocusRetention.ts`) for the single-button case — a ref + a `wasBusy` guard that calls `.focus()` exactly on the busy→not-busy transition, applied to SettingsPage's profile-save, CommercePage's Generate/Regenerate (one ref safely covers both — confirmed mutually exclusive on `files`, never both mounted), and PaidCheckoutPage's submit; (2) a small per-row ref-registry variant (a `Map<id, element>` + "which id was last busy" ref) for the two N-buttons-one-flag cases — SettingsPage's per-program toggle and UsagePage's credit top-up — since React's Rules of Hooks forbid calling a hook inside a `.map()` callback. 3 new tests (`useFocusRetention.test.tsx`) pin the hook's exact contract via a `vi.spyOn(button, "focus")` spy rather than asserting through `document.activeElement`, after discovering mid-write that happy-dom doesn't replicate a real browser's automatic blur-on-disable side effect — a genuine environment-fidelity gap, not a hook bug, so the test asserts the hook's actual logic (calls `.focus()` on exactly the true→false transition, never on mount, never on false→true) instead of fighting the environment. **Caught and fixed a real bug of its own, via the full-suite run, not the scoped one:** the first PaidCheckoutPage edit called `useFocusRetention` AFTER two early `return`s (the "loading" and "unavailable" steps) — a genuine Rules-of-Hooks violation (a hook skipped on some renders and called on others), which produced a hard React error and rendered a completely empty page. Scoped tests for the 3 other files passed cleanly and hid this, since none of the 3 has an early return before its hook call; only the full `apps/web` suite (which includes `pages.test.tsx`'s deeper PaidCheckoutPage coverage — a file I'd wrongly assumed had no test coverage at all after finding no dedicated `PaidCheckoutPage.test.tsx`, instead of grepping for the component name across every test file per rule 3's own "scope = grep, not memory" standard) caught it. Fixed by moving the hook call to the top of the component, before any early return. **Verified:** full apps/web suite 545/545 green (542 + 3 new), typecheck clean, production build clean, full monorepo build clean. No API changes — pure `apps/web` again. CI green on push (build-and-test 20+22, Axis Compliance: A, deploy-web all success; docker-build/deploy-api correctly skipped). **Push hiccup, disclosed:** the first two push attempts failed/hung on Git Credential Manager trying an interactive re-auth dialog this non-interactive session can't complete (`fatal: User cancelled dialog.`, then a plain hang) — confirmed via `git ls-remote` that neither attempt actually landed (a different failure mode than this loop's previously-seen benign client-side push timeouts, where the push had actually succeeded). Root-caused via `gh auth status` (gh's own token was fine — a separate credential path from GCM) and worked around by pushing with `gh auth token` inlined directly into the push URL for one command, touching no git config; saved as a memory for future sessions. **Live-probed:** fetched the live JS bundle (new hash, confirming a fresh deploy) — size (773,594 bytes) matches the local production build almost exactly (773,637 bytes, a trivial build-nondeterminism gap). Unlike every prior H5.x live-probe, this fix has no new user-facing string to grep for (pure behavioral/attribute wiring, no new copy) — disclosing that limitation rather than overclaiming: the evidence here is "a fresh, correctly-sized build deployed," not "the exact new logic is visibly confirmed live" | `5cbde27` |
| 2026-07-13 | H5.1b (partial: +d) | a11y follow-ups from the H5.1 audit — item (d) only; (e)-(g) remain, see Phase H5 spec. **Completed the ARIA Tabs Pattern** on HelpPage/QAPage/DocsPage's section tabs and McpPage's `PlatformTabs`: all four were keyboard-*reachable* (native `<button>`s take Tab+Enter/Space for free) but implemented none of the pattern a screen reader expects — no `tablist`/`tab`/`tabpanel` roles wired together (McpPage had a partial `role="tab"`/`aria-selected` from an earlier unit, the other 3 had zero tab semantics at all), no roving tabindex, no arrow-key navigation. New reusable `useTabList(tabs, active, onActivate)` hook (`apps/web/src/useTabList.ts`) returns prop-getters (`tabListProps`/`getTabProps`/`getPanelProps`) so each page keeps its own JSX/styling and just spreads the result — wires `role="tablist"` on the container, `role="tab"`/`aria-selected`/`aria-controls`/roving `tabIndex` (0 for the active tab, -1 for the rest — inactive tabs leave the normal Tab order entirely, matching the ARIA APG spec) on each button, and `role="tabpanel"`/`aria-labelledby` on the content region, plus Left/Right/Home/End arrow-key handling with **automatic activation** (arrow press immediately switches tabs and moves focus, same as a click) — chosen because all 4 pages already switch content on click, so keyboard users get an identical interaction model rather than a new two-step "arrow then Enter" pattern only they would face. QAPage's tab-switch handler also resets `expanded` (closes any open FAQ item) — preserved by wrapping `setCategory` in a small closure passed as `onActivate`, since the hook only needs a callback, not a raw state setter. 6 new tests (`useTabList.test.tsx`): role/aria-selected/tabindex wiring, ArrowRight/ArrowLeft activation + wrap-around, Home/End, and an ignore-unrelated-keys case. **Caught a real, generalizable test-fragility issue via the full-suite run:** giving tab buttons an explicit `role="tab"` is the ARIA-*correct* fix, but it overrides their native implicit `role="button"` — so `DocsPage.test.tsx`'s 14 existing `screen.getByRole("button", { name: /Programs|API Reference|.../ })` tab-switch queries could no longer find them, failing every non-Overview-tab test (14 failures, all in one file) even though the page itself worked correctly. Not a regression in app behavior — a required test update alongside a correctness fix; all 14 sites now query `getByRole("tab", ...)`, re-verified green. HelpPage/QAPage have no dedicated test files (confirmed via the same grep-the-component-name-across-every-test-file check the previous unit's lesson prompted, not just a same-named-file search) and `pages.test.tsx`'s coverage of them is click-free smoke/content tests, unaffected. McpPage.test.tsx needed no changes — its platform-tab queries, if any, already used `role="tab"` since that role predates this unit. **Verified:** full apps/web suite 551/551 green (545 + 6 new), typecheck clean, production build clean, full monorepo build clean. No API changes — pure `apps/web` again. CI green on push (build-and-test 20+22, deploy-web all success; docker-build/deploy-api correctly skipped). Pushed via the `gh auth token` workaround again (plain `git push` continues to hit the Credential Manager dialog this session — see [[git-push-credential-manager-hang]] memory). **Live-probed:** fresh bundle hash; size (774,823 bytes) matches the local production build almost exactly (774,866 bytes) — same trivial build-nondeterminism gap as the prior unit. No new user-facing string to grep for (ARIA attributes, not visible copy), same disclosed limitation as H5.1b(c)'s live-probe | `2427f07` |
| 2026-07-13 | H5.1b (partial: +e) | a11y follow-ups from the H5.1 audit — item (e) only; (f)-(g) remain, see Phase H5 spec. **Every page now has exactly one `<h1>`.** Re-counting turned up 23 pages missing one (not the original 22 — `tools/WebResearchPage` wasn't counted in the first "22 of 28" estimate), zero anywhere in the app shell either. Fixed via per-page judgment on what "the page's real title" actually is, not a mechanical insert: (1) 13 headings across 12 pages (AccountPage, AdminPage, AnalyzePage, DocsPage, HelpPage, MyAnalyticsPage, NotFoundPage, PaidCheckoutPage ×2 branches, PlansPage, ProjectPage, QAPage, TermsPage) were a direct, already-existing `<h2>` promoted in place to `<h1>` — confirmed per page that the h2 found was genuinely the page's own top-level title (not a nested/secondary heading) by reading the surrounding render logic, not assuming from the tag alone. (2) 10 pages (AccountDashboardPage, ChangelogPage, CommercePage, McpPage, PlaygroundPage, ProjectsPage, RunnerPage, SettingsPage, StatusPage, UsagePage) use the shared `SectionHeader` primitive as their title — rather than changing its default (which would create duplicate `<h1>`s on pages that embed it as a *secondary* section, e.g. AccountDashboardPage's own "Recent projects"/"Quick actions" sub-headers, or where it's embedded in shared components like `LiveDemoTeaser`/`VersionsTab` that live inside pages which already have their own real `<h1>` elsewhere), added an opt-in `level?: "h1" | "h2"` prop defaulting to `"h2"` — fully backward-compatible, applied `level="h1"` only at the 14 call sites confirmed to be a genuine page-top-level title (including all 3 mutually-exclusive loading/error/success branches on AccountDashboardPage/SettingsPage/UsagePage, verified each really is mutually exclusive before editing). (3) `tools/WebResearchPage` has no `<h2>`/`SectionHeader` at all — it's built on the shared `ToolPage` component, which had its own never-promoted `<h2>{name}</h2>`; confirmed `ToolPage` is only actually rendered by this one page (McpPage merely *mentions* it in a comment) before promoting directly, no `level` prop needed for a single-use component. **Visual-regression discipline:** index.css has a real global delta (`h1 { font-size: 1.75rem; font-weight: 700; }` vs `h2 { font-size: 1.25rem; font-weight: 600; }`) — every promoted heading either already had an explicit inline `fontSize` (5 pages share a `1.5rem` "title card" convention; ProjectPage/PlansPage/ToolPage had their own explicit sizes) or got one added matching its *current* computed style exactly (`1.25rem`/`600`, the h2 default) so this unit changes the accessibility tree only, never a single page's visual appearance — a redesign of heading hierarchy weight is a legitimate but separate, undiscussed question, out of scope here. `SectionHeader` itself now pins `fontSize`/`fontWeight` explicitly inside the component (covers all ~20 call sites at once, `level` or not). New dedicated `heading-structure.test.tsx` (27 tests): renders all 23 fixed pages plus the 4 pre-existing-`<h1>` pages (HomePage/ExamplesPage/ForAgentsPage/ProgramsPage — confirming the `SectionHeader` `level` change didn't introduce an accidental duplicate anywhere; KitchenSinkPage excluded as a hidden dev-aid route) and asserts `getAllByRole("heading", {level: 1})` has length exactly 1 — the literal WCAG 2.4.6 claim, not "a heading exists somewhere." Two test-setup wrinkles, not app bugs: AdminPage/PaidCheckoutPage/PlansPage all show a genuinely title-less loading spinner first (correctly left alone — that branch never had a title to begin with), so their tests `await screen.findAllByRole(...)` instead of asserting synchronously; AdminPage's real success-branch response shape wasn't worth fully modeling for this test, so its fetch stub is forced to an error response instead, landing on the (already-confirmed) error branch's `<h1>` — deliberately not a full multi-endpoint fixture. **Verified:** full apps/web suite 578/578 green (551 + 27 new), typecheck clean, production build clean, full monorepo build clean. No API changes — pure `apps/web` again. CI green on push (build-and-test 20+22, Axis Compliance: A, deploy-web all success; docker-build/deploy-api correctly skipped). Pushed via the `gh auth token` workaround (see [[git-push-credential-manager-hang]]). **Live-probed:** fresh bundle hash; size (775,431 bytes) matches the local production build almost exactly (775,474 bytes). Unlike H5.1b(c)/(d)'s live-probes, this fix DOES leave a real, if approximate, textual fingerprint: the compiled JSX for a literal `<h1>` tag emits the string `"h1"` as a createElement/jsx-runtime argument (the `SectionHeader`/`ToolPage` promotions via a dynamic `level` variable do not, since those compile to a variable reference, not a literal) — grepped 34 occurrences of `"h1"` in the live bundle, confirming real `<h1>` JSX is present in meaningful quantity; not an exact before/after count comparison (no baseline was captured), so this corroborates rather than proves precisely | `e9bb5aa` |
| 2026-07-13 | H5.1b (partial: +f) | a11y follow-ups from the H5.1 audit — item (f) only; (g) remains, see Phase H5 spec. **AdminPage's remaining primitives migration.** H5.1 only fixed its loading/error states (Skeleton+aria-busy, Callout); this unit's the 6 tables it never touched — "Calls by Source", "Calls by Tool", "Accounts by Tier", "Funnel by Stage", "Recent Accounts", "Recent Activity" — none had `TableWrap` (the keyboard-scrollable-region primitive every sibling account/billing page already uses) and none guarded the empty case, silently rendering a header-only table on a fresh install with zero accounts/activity/etc. (confirmed this is a real, reachable state, not theoretical: every one of the 6 data sources — `stats.accounts_by_tier`, `mcpUsage.summary.by_source`/`by_tool`, `funnelMetrics.by_stage`, `accounts.accounts`, `activity.events` — is a plain object/array with no minimum-size guarantee from the API). Wrapped all 6 in `TableWrap` with a distinct accessible `label`; added a `colSpan`-spanning empty-guard row per table with honest copy ("No accounts yet.", "No MCP calls in this window.", "No funnel events yet.", "No activity yet." — two pairs of tables share identical copy since they describe the same underlying fact from two angles, not four different messages). New dedicated `AdminPage.test.tsx` (3 tests, this page had zero prior coverage): a full-empty-response fixture across all 6 admin endpoints confirms every empty message renders (not just "the page doesn't crash"); a second test confirms all `TableWrap` regions are genuinely keyboard-reachable (`role="region"`, `tabindex="0"`); a third populates one data source and confirms its real rows render instead of the empty message (guards against a guard that never turns off). Two test-authoring fixes made mid-unit, neither an app bug: `getByText` throws on 2+ matches (two table pairs intentionally share copy) — switched those specific assertions to `getAllByText(...).toHaveLength(2)`; this codebase doesn't set up `@testing-library/jest-dom` matchers (confirmed via repo-wide grep — zero existing use of `toBeInTheDocument` anywhere), so the "absence" assertion uses plain `toBeNull()` on `queryByText` instead. **Full-suite discipline:** the post-change full local run showed 2 failures in `CommercePage.test.tsx` (a file untouched by this unit, last confirmed 578/578 green two units ago) — re-ran that file alone and it passed 7/7, confirming a local-only, load-related flake matching this exact machine's already-documented pattern (see the loop's own rule 5 and this session's memory), not a regression; did not chase it further, consistent with "trust CI, not local full-suite runs, for the authoritative signal." **Verified:** typecheck clean, new tests 3/3 green in isolation, production build clean, full monorepo build clean. No API changes — pure `apps/web` again. CI green on push (build-and-test 20+22, Axis Compliance: A, deploy-web all success; docker-build/deploy-api correctly skipped) — CI's own full run of `CommercePage.test.tsx` passing here reconfirms the local-only-flake diagnosis. Pushed via the `gh auth token` workaround (see [[git-push-credential-manager-hang]]). **Live-probed:** fresh bundle hash; size (776,497 bytes) matches the local production build almost exactly (776,540 bytes); grepped all 4 new empty-state strings in the live bundle — "No accounts yet." not re-checked (identical text existed nowhere before, but is generic enough to skip re-verifying), "No MCP calls in this window." appears exactly twice (matching its 2 separate JSX call sites), "No funnel events yet."/"No activity yet." appear once each — genuine confirmation the new copy is live, not inferred | `72d08cb` |
| 2026-07-13 | H5.1b (final: +g) | a11y follow-ups from the H5.1 audit — item (g), the last item; **H5.1b is now fully closed, (a)-(g) all shipped.** The audit found zero `aria-invalid`/`aria-describedby` usage anywhere in the codebase (confirmed via repo-wide grep) — form validation errors had no programmatic link from the invalid field to its message, so screen-reader users got a visual-only error cue. Scoped to the two most-affected forms rather than a blanket sweep: `PaidCheckoutPage`'s email field (single-purpose error state — the page has exactly one thing that can be wrong) and `SettingsPage`'s webhook-events checkbox group (a harder case — this page shares ONE global `error` state across 5 unrelated forms, so a naive `aria-describedby` would mis-link e.g. a failed API-key revoke to the events checkboxes). Added a new optional `id?: string` prop to `Callout` (backward-compatible default `undefined`, zero effect on ~20 pre-existing call sites that don't pass it), then: `PaidCheckoutPage`'s error `Callout` gets `id="paid-checkout-error"` unconditionally and its email `<input>` gets `aria-invalid={error ? true : undefined}` / `aria-describedby={error ? "paid-checkout-error" : undefined}`; `SettingsPage`'s shared error `Callout` gets `id="settings-error"` and its webhook-events group (newly wrapped in `<div role="group" aria-label="Webhook events">`) gets a conditional `aria-describedby="settings-error"` applied ONLY when `error?.message === "Select at least one event to subscribe to"` — a precise string discriminator chosen specifically because this page's error is global, not per-field, so correctness here depends on matching the exact message rather than "any error is currently showing." Extended/added 5 tests: `primitives.test.tsx` (2, Callout's `id` prop present/absent), `pages.test.tsx` (1, PaidCheckoutPage's `aria-invalid`/`aria-describedby` appear after a failed subscribe attempt), `SettingsPage.test.tsx` (extended the existing "requires at least one event" test to also assert the error Callout's `id` and the group's matching `aria-describedby`). **Full-suite discipline** (established after the earlier PaidCheckoutPage Rules-of-Hooks incident this same phase): ran the 5 directly-affected tests in isolation first (103/103 green, the full existing coverage of the 3 touched files), then the complete `apps/web` suite (28/28 files, 584/584 tests, exit 0) before trusting the change — a stray `AggregateError`/`ECONNREFUSED` against `localhost:3000` printed to the log but caused no test failure and matched no test file in this unit; not chased further per the loop's established local-noise discipline. **Verified:** typecheck clean, production build clean (776.83 kB main bundle / 218.54 kB gzip, `index-Sf2Y_hyA.js` — new hash from the `id`/`aria-*` prop additions), full monorepo build clean. No API changes — pure `apps/web` again, same as every other H5.1b sub-unit. Pushed clean on the first plain `git push` (no credential-manager hang this time). CI green (`build-and-test` 20+22, deploy-web all success; docker-build/deploy-api correctly skipped). **Live-probed:** live bundle `index-7tUISv7C.js` (776,789 bytes) vs local production build `index-Sf2Y_hyA.js` (776,832 bytes) — different content hash (expected, no lockfile in this repo per CLAUDE.md's own warning, so CI's dependency resolution can legitimately differ from local) but the same 43-byte delta seen on H5.1b(f)'s live-probe, reinforcing that this is a consistent, benign local-vs-CI artifact rather than drift; grepped all 5 new strings directly in the live bundle — `paid-checkout-error` ×2, `settings-error` ×2, `aria-invalid` ×1, `aria-describedby` ×2, `Webhook events` ×1 — all present at their expected call-site counts, genuine confirmation the new code is live, not inferred. **H5.1b is now closed in full** | `9dc0efd` |
| 2026-07-13 | H5.2 | Mobile pass on all new pages (Gate 4). Audited first (Explore agent, read-only) rather than guessing at fixes — headline correction to the working assumption: the IDE shell already has a real off-canvas mobile nav (hamburger + drawer, established 900px/600px breakpoint convention), so this was a narrow bug-fix pass, not a from-scratch mobile-nav build. 4 real findings, all fixed: (1) `StatusBar.tsx` — fixed 32px bar (load-bearing height, `.ide-shell`'s `max-height` calc depends on `--statusbar-h`), fully inline-styled with two non-wrapping flex groups and no className any media query could reach — the single highest-value fix since it's shared by all 28 pages via the shell. Added `statusbar`/`statusbar-optional` classNames plus a `!important` ≤600px rule hiding the secondary content (snapshot stats, ⌘K hint, version) — `!important` is required specifically because these spans still carry an inline `display` that no ordinary-specificity CSS rule could beat; the connection indicator (the one thing worth keeping on a phone) stays alone, comfortably on one line. (2) `MyAnalyticsPage.tsx`'s 3 tables had no `TableWrap`, inconsistent with every sibling account/billing page — wrapped all 3 ("Programs used", "API status mix", "All API calls by endpoint"), matching the established convention exactly. (3) `SearchTab.tsx` (zero prior test coverage of any kind) — text-search's matched-content span already had `overflow:hidden;text-overflow:ellipsis` but no `min-width:0`, so the classic flexbox min-width:auto floor silently defeated it (a flex item won't shrink below its content's min-content size — full pre-formatted-text width here — unless min-width is explicitly zeroed); symbol-search's symbol-name span had neither truncation property at all. Fixed both, deliberately keeping each row's file:line location span fixed/non-shrinking (`flexShrink:0` on the symbol row), consistent with the priority the text-search row already implied: location is the stable anchor, variable-length content is what truncates. (4) two tiny edge cases: `.modal-overlay` had no edge padding (dialogs touched the viewport edge exactly at ≤420px) — added 16px; `Toast`'s rail used a bare `maxWidth:380` with no viewport-relative bound and no wrap handling on the message — changed to `min(380px, calc(100vw - 32px))` + `overflow-wrap:break-word`. **Disclosed, not fixed** (found during the same audit, but pure hygiene with zero behavior change on any breakpoint, and >5 lines — out of scope for a bug-fix unit per the loop's own anti-gold-plating rule): a legacy pre-"ide-shell" nav system (`.header`/`.nav-desktop`/`.nav-mobile-controls`/`.hamburger` + its own dead 900px/600px media-query block) is forced off via `display:none` but never deleted, plus a duplicate/superseded `@media(max-width:600px){.ide-main...}` block that a later block in the same file silently overrides — both dead CSS, deferred to H6.2's hygiene self-scan. New tests: `SearchTab.test.tsx` (2 tests — this component's first ever), `MyAnalyticsPage.test.tsx` (1 test, new file — all 3 regions keyboard-reachable via `role="region"`/`tabindex="0"`), `StatusBar` extended in `pages.test.tsx` (1 test). One test-authoring fix, not an app bug: `minWidth:0` serializes to the DOM as `"0"`, not `"0px"` — the first SearchTab.test.tsx draft asserted the wrong string, caught immediately by the scoped test run and corrected before the full suite. **Verified:** typecheck clean, scoped tests 56/56 green (the 3 touched/new files), full apps/web suite 30/30 files, 588/588 tests, exit 0 (the same benign stray `AggregateError`/`ECONNREFUSED` against `localhost:3000` seen in prior units' logs reappeared, caused no failure, not chased further per established local-noise discipline); production build clean (777.27 kB main bundle / 218.64 kB gzip, `index-CvY2PD5q.js`; CSS bundle grew slightly to 42.16 kB from the new media-query rule); full monorepo build clean. No API changes — pure `apps/web` again. Pushed via the `gh auth token` workaround (plain `git push` hung again — [[git-push-credential-manager-hang]]; confirmed via `git ls-remote` that the plain attempt genuinely hadn't landed before retrying). CI green (`build-and-test` 20+22, deploy-web all success; docker-build/deploy-api correctly skipped). **Live-probed:** CSS bundle is a byte-EXACT, hash-EXACT match — `index-BEoaKX2n.css`, 42,161 bytes both live and local (CSS output is fully deterministic here, unlike JS). JS bundle `index-CjpgOI7k.js` (777,222 bytes) vs local `index-CvY2PD5q.js` (777,265 bytes) — different hash as expected (no lockfile) but the SAME 43-byte delta seen on both H5.1b(f) and H5.1b(g)'s live-probes, now a confirmed 3-for-3 pattern. Grepped all new strings: JS — `statusbar-optional` ×2, "Programs used" ×1, "API status mix" ×1, "All API calls by endpoint" ×1; CSS — `statusbar-optional` ×1 — every string at its exact expected call-site count, genuine confirmation the new code is live | `74107bc` |
| 2026-07-13 | H5.3 | Performance: lazy-load + Lighthouse a11y ≥ 95. `routes.tsx` converted 22 of 28 pages (everything except HomePage/AnalyzePage/ProjectsPage/ProjectPage/AccountDashboardPage/NotFoundPage — the true first-load/high-frequency set) from static imports to `lazy()`, with a single `<Suspense>` boundary added in `App.tsx` around `activeDef.render(routeCtx)`. Main bundle 777.27 kB → 456.96 kB (−41%), now fully under the 500kB warning threshold (the warning is gone from the build output entirely); each secondary page is its own small chunk, DocsPage (the biggest) standalone at 68.31 kB. "The 5 core pages" isn't defined anywhere in `docs/web-plan/BUILD-PLAN.md` beyond the phrase itself — used the same eager/high-frequency set from the lazy-load decision, minus NotFoundPage (not a real user-journey page): HomePage, AnalyzePage, ProjectsPage, ProjectPage, AccountDashboardPage. Auth-only pages bounce to a sign-in gate without a live backend, so a raw static-preview crawl can't reach their real content — substituted 3 public, backend-independent pages exercising the same shared primitives (DocsPage, RunnerPage, CommercePage) for a meaningful signal instead of re-auditing the same gate screen three times; disclosed, not silently swapped. Real Lighthouse run (Chrome + `npx lighthouse`, no new deps — npx doesn't touch package.json/lockfile) via a local `vite preview` of the production build: Home 100, Analyze 100, Docs 98, Runner 98, Commerce 95→100 after a fix — all ≥95. One genuine fix applied: Commerce's only failure was insufficient color contrast (3.53:1, needs 4.5:1) on the shared `PageFooter` copyright text and Terms/Status links — traced to `opacity:0.85` on `.ide-footer p` compounding an already-adequate `--text-muted`-on-background pair (computed ~4.71:1 at full opacity) below the AA threshold; removed the opacity dampening, re-verified 100 — a shared-component fix benefiting every page, not just Commerce. **Disclosed, not fixed — new H5.3b:** Docs/Runner's remaining 2-point gap is a `heading-order` violation (h1 straight to h3, no h2), a different, previously-undiscovered WCAG check from H5.1's "exactly one h1" audit. A repo-wide grep (pages with `<h3>` present and zero `<h2>` anywhere) found this is page-wide on both flagged pages and widespread: 15 pages / 109 total `<h3>` headings with no `<h2>` at all. Both audited pages already clear the ≥95 bar despite this — not gating — but fixing it properly needs the same per-page judgment discipline as H5.1's original heading-order work, applied to up to 15 pages; too large to fold in silently, tracked as H5.3b in STATE. Test/build changes: 3 `app-routing.test.tsx` tests needed real timeout headroom (10s `findBy` / 15s test budget) for genuine dynamic-import transform latency in the test environment, same rationale this file's own `PageFooter` test already established; the kitchen-sink test's heading assertion switched `getByRole`→`findByRole` since its page is now lazy. 2 `CommercePage.test.tsx` failures on the first full-suite run were the pre-existing, already-documented local-load flake (confirmed via isolated re-run, 7/7 clean) — CommercePage is tested via direct import, entirely bypassing `routes.tsx`'s lazy wiring, so this was never a candidate regression from this unit. **Verified:** typecheck clean, full apps/web suite 30/30 files, 588/588 tests (twice — before and after the footer-CSS fix), production build clean (456.96 kB main bundle / 138.25 kB gzip, `index-CyR0LsxX.js`), full monorepo build clean. No API changes — pure `apps/web` again. Pushed clean on the first plain `git push` (no credential-manager hang this time). CI green (`build-and-test` 20+22, deploy-web all success; docker-build/deploy-api correctly skipped). **Live-probed:** CSS bundle byte/hash-EXACT match (`index-BwfYut5G.css`, identical live and local). Main JS bundle `index-AMtYePqw.js` (456,927 bytes) vs local `index-CyR0LsxX.js` (456,964 bytes) — different hash as expected (no lockfile) but only a 37-byte delta, consistent with the established local-vs-CI pattern. Strongest evidence for this unit specifically: grepped the live main bundle for its `DocsPage-*.js` dynamic-import reference (`DocsPage-DRNsLNrH.js`), then fetched that exact chunk directly — HTTP 200, 68,314 bytes, matching the local build's 68.31 kB almost exactly. This is genuine, concrete proof the code-split lazy chunks are deployed and independently fetchable in production, not just present in the local build | `b3eee31` |
| 2026-07-13 | H5.3b | Heading-order sweep (H5.3's disclosed follow-up). A grep confirmed the h1→h3 skip Lighthouse flagged on Docs/Runner was page-wide across 15 pages: 109 `<h3>` headings with zero `<h2>` anywhere, plus 25 `<h4>` genuinely nested a level deeper in 3 of those pages (Docs 4, Help 5, Terms 16) — 134 headings total. Split across 6 parallel agents: one each for Docs/Help/Terms (the 3 files needing both h3→h2 AND h4→h3, given their scale — Terms alone had 29 headings), and 3 more splitting the remaining 12 files (AdminPage, ChangelogPage, CommercePage, McpPage, MyAnalyticsPage / PlansPage, PlaygroundPage, ProjectPage, RunnerPage, StatusPage / SettingsPage, UsagePage) into groups of ~5. Each agent independently verified every heading's surrounding render context before promoting (none were mislabeled, none skipped, none over-promoted) and pinned `fontSize`/`fontWeight` inline to the original tag's computed default (`index.css`'s real per-level defaults: h1 1.75rem/700, h2 1.25rem/600, h3 1rem/600, h4 1em/700) so the change affects only the accessibility tree. Two independent agents (DocsPage's and the Admin/Changelog/Commerce/Mcp/MyAnalytics group's) both flagged the same gap unprompted: the CSS also sets a different `margin-bottom` per level (h1/h2: 8px, h3: 4px, h4: 0px via the universal `* { margin: 0 }` reset) that the original instructions didn't cover. Did a consolidated pass myself afterward: grepped every promoted heading across all 15 files for one with no existing margin override (inline `margin`/`marginBottom` or a `.mb-N` className, both of which are tag-independent and already safe) — found 20 across 6 files (AdminPage ×7, MyAnalyticsPage ×4, PlansPage ×1, SettingsPage ×3, UsagePage ×3, DocsPage ×2) that would otherwise have picked up a 4-8px visual shift, added explicit `marginBottom` to each restoring the original tag's value. One transcription mistake caught by the Edit tool's exact-match requirement, not a real bug: initially mis-listed SettingsPage's "Danger Zone" heading as needing the margin fix; it already had `className="mb-2"` (verified directly against the live file before applying), so no action was needed there. **Verified:** typecheck clean, full apps/web suite 30/30 files, 588/588 tests — no test breakage (repo-wide grep confirmed no test anywhere asserts a specific non-1 heading `level`, only `heading-structure.test.tsx`'s existing `level:1` count assertions, which this unit doesn't touch). Production build clean (main bundle unchanged at 456.96 kB / 138.24 kB gzip, `index-JCpoGmvz.js` — new hash from the per-page chunk content changes, each secondary-page chunk grew a few hundred bytes from the added inline styles, e.g. DocsPage 68.31→69.19 kB); full monorepo build clean. This closes H5.3's full disclosed scope — no follow-up remains from the H5.3 Lighthouse pass. **CI caught a real gap in this unit's local verification** — disclosing in full rather than glossing over it: the first push (`9753d7c`) failed CI's `build-and-test` on both matrix legs. Root cause: `apps/cli/src/cli-docs-parity.test.ts` (a DIFFERENT package than `apps/web`) does a literal string search for `">Commands</h3>"` and `">CLI Options</h3>"` in DocsPage.tsx's source to cross-check the docs against `cli.ts`'s real dispatch table — both headings were correctly promoted to h2 by this unit, which broke the literal match. This wasn't caught locally because every verification step this whole unit ran was scoped to `pnpm --filter @axis/web` — the actual full monorepo suite (which CI runs, ~7020 tests) was never run locally for this unit, and this specific test's package (`apps/cli`) was never touched by any `apps/web`-scoped command. Fixed in a follow-up commit (`21a71c8`) updating the test's literal strings to match the new h2 tags — not a revert, the two changes needed to land together. Attempted to run the full monorepo suite locally to close this verification gap properly, but hit a DIFFERENT, pre-existing, unrelated limitation: 104 test files (all in `apps/api`/`packages/snapshots`) require a real Postgres `DATABASE_URL` this local machine doesn't have configured, which CI provides via a service container — confirmed via the exact error text (`resetTestDb() requires DATABASE_URL`) and confirmed every one of the 104 failing files sits in those two Postgres-dependent packages, none in `apps/web`/`apps/cli`. So the practical local ceiling is: run the SPECIFIC other-package test(s) that reference a changed shared file (verified `cli-docs-parity.test.ts` 7/7 green in isolation) rather than the full suite, and trust CI as the final gate for anything requiring infrastructure this machine lacks. **Lesson for future units:** grep the whole repo for literal references to a changed file's content (not just its own package's tests) before considering verification complete, especially for widely-referenced files like DocsPage.tsx. CI green on the fix commit (`build-and-test` 20+22 both success, deploy-web success). **Live-probed:** CSS byte/hash-exact match (`index-BwfYut5G.css`, unchanged — this unit touched no CSS). DocsPage's lazy chunk is a byte-EXACT match: `DocsPage-BtgcBKBM.js`, 69,193 bytes both live and local. Grepped the live chunk for JSX tag-name string literals: `"h2"` appears exactly 23 times and `"h3"` exactly 4 times — precisely matching DocsPage's 23 h3→h2 and 4 h4→h3 promotions, genuine confirmation the exact right headings are live, not approximate. No API changes — pure `apps/web`/`apps/cli` again | `21a71c8` |
| 2026-07-14 | H6.1 | Key-rotation runbook execution prep. Wrote `docs/SECURITY_ROTATION.md` — a full credential inventory + rotation procedure for axis-iliad's own operational surface, scoped deliberately narrow: Render's `axis-api` service env vars (every `sync: false` row in `render.yaml` — `AXIS_TOKEN_KEY`, `DATABASE_URL`, GitHub/Google OAuth secrets, `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`, `MPP_SECRET_KEY`, `PAID_API_KEY`/`PAID_WEBHOOK_SIGNING_KEY`, 4 proxied-tool keys) plus the 4 GitHub Actions repo secrets referenced across `.github/workflows/*.yml` (`GITHUB_TOKEN` auto-provisioned, `RENDER_DEPLOY_HOOK_URL`, `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID`, `AXIS_API_KEY`). Deliberately did NOT try to inventory the owner's full `key.txt` (~30 entries at the estate root, one level above this repo) — most of it (Plaid, Circle, Roblox, other LLCs' EINs, a Roblox JWT) belongs to entirely different products/business lines, out of scope for axis-iliad's own runbook; cross-referenced it only by LABEL for the entries that do overlap with this repo's Render/Stripe/PAI'D surface, never by value. **Handled the credentials-file read with care, per the Secrets law:** read `key.txt` once to extract variable NAMES/labels/inline comments for the inventory, never echoed a single value in any tool output, the doc, or this ledger row; grepped the finished doc afterward for every live Stripe/Render/Cloudflare/Resend/Google key-prefix pattern before committing, confirming zero matches (the one hit was the literal string "sk_live_" used in prose to describe a naming convention, not a real key). **Stripe restricted-key (`rk_`) migration prep, the task's other explicit ask:** enumerated every real (non-generated, non-test) Stripe API call site in the codebase by reading source directly rather than assuming — `apps/api/src/stripe.ts` (`GET`/`POST /v1/subscriptions/:id`, `POST /v1/checkout/sessions`), `apps/api/src/network-token.ts` (`GET /v1/payment_methods/:id`), `packages/agentic-compliance/src/dispute-clients.ts` (`GET`/`POST /v1/disputes/:id`). Confirmed `apps/api/src/commerce-integration.ts`'s own `api.stripe.com` string is a generated CODE TEMPLATE emitted for a customer's own repo (runs with the customer's own key, in their own runtime) — correctly excluded, not a real call this system's own key makes. Net result: the minimal restricted-key scope is just 4 resource permissions (Checkout Sessions: Write, Subscriptions: Write, Payment Methods: Read, Disputes: Write) — narrower than one might guess for a system this size, and `STRIPE_WEBHOOK_SECRET`/`PAID_WEBHOOK_SIGNING_KEY` need no API permission at all since they're HMAC-verification secrets, not API credentials (a common point of confusion the doc calls out explicitly). Flagged the two standing risks the spec named for the owner (not fixed, can't be — both are account-level/operational, not code): the Render API key's blast radius (shared across axis-iliad/Foundry/PAI'D per [[render-blueprint-governance]], no narrower per-service key exists), and org 2FA enforcement. **Verified:** grepped the new doc for secret-value leakage (clean), confirmed no existing test scans/enumerates `docs/` generically (`count-honesty.test.ts`'s `docs()` helper only reads README.md + `apps/web/src/**/*.tsx`, unrelated despite the name; `strategic-docs-honesty.test.ts` doesn't scan directories at all) — a new file under `docs/` needed no test updates. Pure documentation addition, no code/API changes, no build or test-suite run needed. Pushed clean, CI green (`build-and-test` 20+22, deploy-web success — redeployed the unchanged web bundle since this unit touched no `apps/web` files; docker-build/deploy-api correctly skipped). No live-probe performed for this unit — nothing web-facing changed for the bundle to evidence; `docs/SECURITY_ROTATION.md` is a repo document, not something the deployed app serves | `045a42c` |
| 2026-07-14 | H6.2 | Dependency + hygiene self-scan. **pnpm audit:** found 5 vulnerabilities (1 low, 4 moderate) across 4 advisories, all transitive (not direct dependencies this repo declares): `uuid` (via `dockerode`>uuid AND `packages/snapshots`>uuid directly), `protobufjs` (via dockerode), `tar` (via `node-llama-cpp`>cmake-js>tar), `@babel/core` (via `@vitejs/plugin-react`). Investigated each one's exact version-range math before touching anything: `tar` (cmake-js declares `^7.5.6`, patched `>=7.5.16` exists at `7.5.20`), `protobufjs` (dockerode declares `^7.3.2`, patched `>=7.6.3` exists at `7.6.5`), `@babel/core` (plugin-react declares `^7.27.4`, patched `>=7.29.6` exists), and `packages/snapshots`'s own direct `uuid: ^11.1.0` (patched `>=11.1.1` exists) — all 4 fixed via a pure `pnpm update` within their EXISTING semver ranges, zero package.json changes. The one exception: `apps/api`'s `dockerode>uuid` path is structurally capped — dockerode@4.0.12 itself declares `uuid: ^10.0.0`, which cannot resolve to the patched `>=11.1.1` range no matter what. Checked dockerode@5.0.1 (the latest major) and found it removes the uuid dependency entirely (not just bumps it) — bumped `apps/api`'s dockerode `^4.0.12`→`^5.0.1` and `@types/dockerode` `^3.3.47`→`^4.0.1` (a real major-version dependency change, done carefully): checked the actual dockerode API surface this codebase uses (`code-sandbox.ts`'s `ping()`, `createContainer()`, `getImage().inspect()`, `pull()`, `modem.followProgress()` — all long-stable methods), then verified empirically rather than assuming — typecheck clean, `code-sandbox.test.ts` 16/23 passing (7 skipped, need a real Docker daemon, unaffected either way). End result: `pnpm audit` now reports 0 vulnerabilities (was 5). **`iliad_hygiene` self-scan:** wrote a one-off script loading this repo's own 1152 tracked text files (of 1187 total, skipping binaries) into the exact `{path, content, size}` shape `apps/api/src/mcp-tool-impls.ts`'s `coerceHygieneFiles` builds, then called the compiled `runHygieneScan` directly. Result: grade F, 11 findings across 5 dimensions (1 high, 2 medium, 7 low, plus todo_debt/test_peer_coverage informational) — investigated every single one individually before concluding anything, since a HIGH-severity "possible committed Stripe live secret key" finding demands real scrutiny, not a shrug. **Handled with care per the Secrets law:** classified the matched string's shape (length, entropy pattern) without ever printing the actual substring, confirmed it was only 28 characters total (a real Stripe live key is ~99-107) — then read the masked surrounding context and confirmed it's `apps/api/src/mcp-tools.ts`'s own tool-description example payload, literally named `"Paid fix plan for a committed secret"`, demonstrating what the `iliad_hygiene` tool itself detects. Confirmed false positive, no rotation needed, no code change — a real leaked secret would have been escalated immediately, not silently patched. The 2 stub_detection findings (hygiene.ts, mcp-tools.ts) are the same class of self-reference: the scanner's own pattern-definition regexes and self-documentation text necessarily contain the words ("placeholder", "stub", "not implement") they're designed to detect. The 6 duplicate_content sets are confirmed intentional: `.ai/*` vs `packaging/*` file pairs are a documented, deliberately-regenerated-together convention (`git log` shows a dedicated "regenerate the committed .ai/ + packaging/ sample artifacts" commit), and 5 identical tsconfig.json files across packages is standard, expected monorepo boilerplate. todo_debt (31 markers, threshold 25) and test_peer_coverage (52.4%, already floored to B) are informational; spot-checking the TODO occurrences found the same self-reference pattern (quality-generator templates discussing TODO markers as a concept, including the hygiene tool's own example payload literally containing the string `// TODO: implement`). **Net conclusion, stated plainly: zero of the 11 findings represent a genuine hygiene problem in this codebase** — every one is an artifact of this specific repo legitimately building the hygiene/quality-scanning tools that a naive pattern scanner would flag when pointed at itself. No source code changes made as a result of the scan itself. **Also closed two adjacent findings discovered/disclosed earlier:** (1) H5.2's disclosed dead CSS — removed the legacy pre-"ide-shell" nav system (`.header`/`.header-brand`/`.nav-desktop`/`.nav-mobile-controls`/`.hamburger`, ~64 lines, confirmed zero references in any `.tsx` file first) plus its own `display:none` override rule (whose comment literally said "fully removable later"), and the duplicate/superseded `@media(max-width:600px){.ide-main...}` block confirmed dead via the later, winning block already containing H5.2's `.statusbar-optional` rule. (2) A stale `CLAUDE.md` warning claiming "No lockfile found" — false: `pnpm-lock.yaml` is real, tracked, and actively maintained (confirmed via `git ls-files` + `git log`); this also retroactively explains that H5.1b(f)/(g)/H5.2/H5.3's live-probe entries incorrectly attributed a small local-vs-CI bundle byte delta to a missing lockfile — the lockfile was never missing, the real explanation for that delta is unconfirmed (likely a minor toolchain-patch-version difference), noting the correction here rather than silently leaving the wrong claim standing. **Verified:** typecheck clean (apps/api, apps/web), full apps/web suite 30/30 files 588/588 tests, full apps/api suite confirmed the only 69 failing files are the same pre-existing, already-documented local `DATABASE_URL`-required Postgres tests (not a regression — every failure's error trace matches `resetTestDb() requires DATABASE_URL`), production + monorepo builds clean (CSS bundle measurably shrank 42.15→41.22 kB from the dead-code removal, confirming the cleanup took effect). **Follow-up catch during live-probe (trust but verify, not a new unit):** fetching the deployed CSS and grepping for the removed classNames as a sanity check found a SECOND, separate leftover — the first pass's `.tsx`-reference grep was exhaustive but the CSS-side search wasn't: a whole extra `@media(max-width:900px){.nav-desktop{display:none}.nav-mobile-controls{display:flex}.header{padding:10px 0}}` block, plus one `.header-brand h1{font-size:1.1rem}` rule living INSIDE an otherwise-live 600px media query (which also legitimately styles `.trust-banner`/`.trust-item`/generic `h1`/`h2` — left every one of those alone, removed only the one dead rule). Removed both, re-verified: full apps/web suite 30/30/588/588 again, production build clean, CSS bundle shrank further 41.22→41.08 kB. Re-ran the exhaustive grep (`grep -n ".header\b\|.header-brand\b\|.nav-desktop\b\|.nav-mobile-controls\b\|.hamburger\b" src/index.css src/theme.css`) across BOTH stylesheet files this time, zero remaining matches — confirmed via a fresh live-probe afterward too, not just asserted. Pushed clean on both commits (`656fce7` main unit, `a7e7118` the follow-up fix). CI green on both (`build-and-test` 20+22 both success each time; the first commit's run also confirmed the dockerode major-version bump against CI's real Postgres-backed apps/api suite, the strongest signal available since local couldn't run it; deploy-web success both times, docker-build/deploy-api correctly skipped both times — axis-api's own deploy path is Render's independent Blueprint, not this GH Actions job). **Live-probed (twice — the second specifically to close the loop on the follow-up catch):** final CSS bundle `index-9Gx5iLTx.css` is a byte-EXACT match, 41,081 bytes live and local, and the exhaustive legacy-class grep against the LIVE deployed file returns zero matches — genuinely confirmed clean in production, not just asserted from a local build | `a7e7118` |
| 2026-07-14 | H6.3 | Live-probe battery script. **Phase H6 is now fully wrapped (H6.1, H6.2, H6.3 all DONE).** Wrote `scripts/live-probe.mjs`, a repeatable version of the manual "July systems check" the spec named: 6 checks (health, ready, MCP tools count, PAI'D config, the anon-caller 413 file-size gate, the web bundle marker), zero dependencies (pure native `fetch`/`AbortSignal.timeout`, no imports at all). Delegated the endpoint research to an Explore agent rather than guessing at paths — worth noting what it found, since some of it corrected assumptions: `GET /v1/mcp/server.json` gives the tool count via plain REST (`tools.length`), no JSON-RPC `tools/list` call needed; the anon 413 gate can be tripped with a client-declared `size` field and 6 bytes of real payload (`{files:[{path,content:"x",size:6291456}],programs:["skills"]}`) rather than an actual multi-MB upload, since `analyze.test.ts` itself confirms `file.size` is trusted as-is when present — `programs` must name a free program (`skills`) or an anonymous request 401s on auth before ever reaching the size check at all; and `apps/api/src/paid-handlers.ts`'s own doc-comment claiming the PAI'D config response includes `publishable_key`/`plans` is stale — the code (and the web client's own TS interface) only ever sends `{configured}`. `checks.payment_rail` in the readiness response is deliberately diagnostic-only per the codebase's own existing design (absence degrades paid calls to 429, it's never treated as an outage) — the script surfaces it in the log line but correctly excludes it from the pass/fail test, matching that intent rather than second-guessing it. Wired `pnpm run live-probe` into root `package.json`, and a new non-gating `live-probe` job into `.github/workflows/ci.yml` running after `deploy-web` — needs no `pnpm install` since the script has no dependencies. **Deliberately non-gating** per the spec's own words, for a real structural reason, not just caution: `axis-api`'s actual deploy path is Render's own independent Blueprint autoSync, decoupled from this GitHub Actions workflow entirely (per [[render-deployment-audit]]) — there is no fixed timeline after a push when "the API is now live" becomes true that this job could safely wait for, so the job reports CURRENT production state rather than asserting this specific push's changes are already live. **Verified:** ran the script directly against real production before wiring anything in — all 6 checks passed (health ok, ready with database ok, MCP tools 36/36, PAI'D configured=true, anon 413 gate correctly returns `FILE_TOO_LARGE`, web bundle marker found). Validated the new CI YAML via `npx js-yaml` (ad-hoc, not added as a dependency) — parses clean, the new job's structure confirmed correct. Ran the existing `scripts/check-artifact-freshness.ts` gate to confirm no interaction — 0 errors (2 pre-existing soft warnings about route/domain-model count drift, unrelated to this unit, out of scope). No apps/web or apps/api source changes — pure new operational tooling. Pushed clean (`35eb8dc`), but **CI caught a real gap the local verification couldn't**: the new `live-probe` job failed with "Unable to locate executable file: pnpm" — `actions/setup-node@v5`'s own cache-detection step shells out to pnpm (this repo's `package.json` declares `packageManager: pnpm`) even though the job's steps never call pnpm themselves, and every OTHER job in this file runs `pnpm/action-setup@v5` first for exactly this reason — the new job was the one place that pattern got skipped, reasoning (wrongly, for CI's purposes) that a dependency-free script didn't need it. Fixed in a follow-up commit (`5733ecd`) adding the same `pnpm/action-setup@v5` step every sibling job already has. **This is genuinely the live-probe evidence for this unit** — the fixed job's own CI output, not a separate manual curl pass: all 6 checks ran inside real CI against real production and passed (health ok, ready with database ok, MCP tools 36/36, PAI'D configured=true, anon 413 gate returned `FILE_TOO_LARGE`, web bundle marker found) | `5733ecd` |
| 2026-07-14 | H7.1-H7.4 | Model-cascade doctrine productization (owner directive, closes Phase H7). New generator `model-cascade.md` (skills program, free tier): a three-tier planner/executor/mechanical delegation table with task-type rows deterministically gated on real repo signals (CI detected -> mechanical CI-triage; tests detected -> executor test-backed-implementation; monorepo/infra pattern -> planner cross-cutting-design; >1 framework -> planner framework-migration; separation score <0.5 -> planner layer-boundary-refactor), plus the delegation contract, cost rule, and an honest-limits note (capability CLASSES only, no vendor SKU/pricing/benchmark strings, test-enforced). No LLM calls at generation time -- the artifact teaches the cascade, it doesn't come from one. Registered in `generate.ts`'s REGISTRY and `program-manifest.ts`'s GENERATOR_PROGRAMS; `TOTAL_GENERATORS`/`ARTIFACT_COUNT` auto-derived 141->142, no manual edit needed there. H7.2 wiring found the spec's own "skills entry in PROGRAM_OUTPUTS" doesn't exist -- the real skills-output list `/v1/analyze` serves is a local array at `apps/api/src/handlers.ts` (`skillsOutputs`), wired there instead (disclosed, not silently substituted). Ran the pinned-literal artifact-count sweep and let the enforcing tests do the real work per the spec's own instruction, which caught more than the named doc list: `README.md`/`LAUNCH_CLAIMS.yaml`/`AXIS_Board_Pitch.md`/`launch-content.md`/`config.ts` (as named) plus `marketing-pack.md` (same `launch-claims.test.ts` corpus, caught by the research agent), `CONTRIBUTING.md` + `begin.yaml` (both guarded by `strategic-docs-honesty.test.ts`, not in the spec's named list -- begin.yaml is the live autonomy-layer manifest woven into every generated analysis, not a historical doc), and two hardcoded generator-count literals in `generate.test.ts`/`generate-programs.test.ts` that would have broken CI once `TOTAL_GENERATORS` auto-derived to 142. Genuine second-order effect the sweep surfaced by running the actual tests rather than guessing: skills is a free program, so the free-tier generator count also moved 15->16, not just the global 141->142 -- fixed in `LAUNCH_CLAIMS.yaml`'s `free_tier_generators` claim, `marketing-pack.md` (three spots, one of which had a pre-existing, unrelated 13-vs-15 drift predating this unit, corrected in passing), the skills program's own description string in `generators-agentic-purchasing.ts`, and two `generators-agentic-purchasing-develop.test.ts` bundle-count assertions (free-bundle 15->16, dev-essentials 31->32 since it also includes the skills program). MCP tool count confirmed unchanged at 36 (no new tool; rides the existing skills/analyze surface as specified). H7.3 tests: added the 6th generator to the shared hostile-fixture injection/escaper suite (`generators-skills-injection.test.ts`) -- passed clean on the first run, no new sink logic needed since it reuses the same `mdText`/`mdInline` sanitizers every sibling skills generator already uses for repo/manifest-derived strings. Content-contract tests in `generators-skills.test.ts` cover every conditional task-type row's signal-gating in both directions, the four always-present sections, and a case-insensitive check for vendor names/pricing symbols/benchmark names. Determinism covered by the suite-level `determinism.test.ts` once registered. One test-design bug caught by an actual test run, not by review: "Test-backed implementation" also appears verbatim inside the static tier table's Owns column, so a bare-substring `.not.toContain` check was a false positive against the generator's own always-present content -- narrowed to the row-specific text before it passed. H7.4: one short, unconditional "## Model Cascade" cross-reference section in `generateWorkflowPack`'s output pointing at `model-cascade.md` (not duplicated doctrine), placed after the four existing workflow yaml blocks. Grepped every `workflow-pack.md`-referencing test across the whole repo (10 files) before running anything, per the lesson from H5.3b's CI failure -- none assert an exact heading count or exhaustive content, so no test updates were needed and none broke. **Verified:** full monorepo build clean (all packages including apps/web's production bundle, unaffected at 456.96 kB). `packages/generator-core`'s full suite 2400/2400 real tests passing (+8 Postgres-gated skips, the known local-environment gap). Targeted `apps/api` suites (`launch-claims.test.ts`, `strategic-docs-honesty.test.ts`, `count-honesty.test.ts`) all green. Ran the FULL `apps/api` suite (2624 tests, ~21 minutes) specifically to rule out anything hiding among the expected Postgres-gated noise rather than assuming -- all 150 failures across 71 files traced to one of three sub-patterns, all pre-existing and unrelated to this unit: direct `DATABASE_URL`-required errors, cascading afterEach/afterAll TypeErrors on objects that never got constructed because their beforeEach already failed on the same DATABASE_URL gap, or two isolated 5000ms timeouts (code-sandbox's Docker-daemon-dependent test, count-honesty.test.ts) confirmed transient resource-contention artifacts of running 2624 tests in one process by re-running both files in isolation immediately afterward (30/30 clean, 0 timeouts). Zero genuine regressions. **Committed and pushed (`0113208`), with one disclosed self-correction along the way:** the first push accidentally targeted a brand-new remote branch literally named `build/close-doc-gaps` (this session's own local branch name, used as a literal push target instead of `main` by mistake) rather than `main`, which every prior unit this entire session had pushed to -- caught immediately by checking `origin/main`'s tip against the local parent commit before assuming anything had shipped, then pushed the same commit to `main` as a clean fast-forward (`3754947..0113208`). The stray `build/close-doc-gaps` remote branch (byte-identical content, no unique commits, zero risk) was deliberately left in place rather than deleted unilaterally, since branch deletion is explicitly named as an action warranting confirmation first, not a same-session cleanup call to make alone. CI green on `0113208`: `build-and-test` (Node 20 + 22) both success, `Axis Compliance: A` success, `deploy-web` success, `docker-build`/`deploy-api` correctly skipped (axis-api's real deploy is Render's independent Blueprint autoSync per [[render-deployment-audit]]), and H6.3's own `live-probe` job ran as part of this same CI run and passed all 6 checks -- a nice bonus confirming that infrastructure is already paying for itself. Separate `Compliance Check` / `AP2 / Visa Compliance Gate` workflow also success. **Live-probed the specific thing this unit shipped**, not just general system health: POSTed a real analyze request (`programs:["skills"]`) to production `api.iliad.trustfabric.ai/v1/analyze` and confirmed `model-cascade.md` appears in the live output list alongside the other 5 skills files, then fetched its actual generated content and confirmed all four required sections are present (Capability tiers, Delegation contract, Cost rule, Honest limits) with the project name correctly interpolated -- genuine proof the deployed generator produces correct output for real requests, not just that the deploy succeeded. No FAILURES-ledger entry -- no unit halted. | `0113208` |
| 2026-07-14 | H8.1 | Unhappy-path completeness audit, begins Phase H8 (foundation engineering). A plain `grep fetch(` found 9 files but missed real sites using an injectable-fetch testability pattern (`fetchImpl`/`FetchLike`/`typeof fetch =`) -- widened the sweep via a dedicated research agent: 14 real external call sites total, one (`email.ts`) missing from the initial candidate list entirely, one initial candidate (`url-guard.ts`) reclassified as shared infrastructure other sites route through rather than its own site. Before: timeout 0/14, transport-error 5/14, malformed-response 3/14 tested. After: transport-error 14/14, malformed-response 14/14 (12 tested + 2 confirmed N/A -- stripe.ts's cancel-subscription and alerting.ts's postAlert never read the response body), timeout 6/14 tested (every site with a real client-side AbortController) + 8/14 explicitly waived with a one-line inline comment at the call site (`H8.1 WAIVER: ... Tracked as H8.1b`) -- zero unwaived gaps. Split across 6 parallel agents (each 1-2 files: handlers.ts Firecrawl / stripe.ts malformed-response / web-research.ts+web-fetch-sovereign.ts full 3-test / document-parsing.ts+speech-to-text.ts / network-token.ts+github-pr.ts / embeddings.ts+email.ts timeout-only) plus alerting.ts done directly. A research-agent report wrongly classified alerting.ts's postAlert as having no timeout mechanism -- caught by reading the source directly before dispatching work based on it (it has a real 5s AbortController), corrected, and postAlert exported (a one-word change) so it could be tested directly like every sibling site. Re-verified the OTHER 7 "no timeout mechanism" sites myself against source before finalizing waivers -- all 7 confirmed accurate. Six disclosed findings, not fixed (test-writing scope, not a fix pass -- each is its own future unit): (1) handleFirecrawlScrape charges the account AND writes the empty result into the 24h shared cache when Firecrawl returns a malformed 200 -- bills for nothing and serves that nothing to every other caller of the URL for 24h; the crawl sibling is billing-safe (0 pages -> 0 charge) but still returns a silent 200. (2) handleCreateCheckout returns 201 "success" when Stripe's 200 response is missing id/url (valid JSON, wrong shape) -- checkout_url/session_id are silently dropped by JSON.stringify(), a caller that doesn't defensively check key presence would report a completed checkout with no redirect URL. (3) firecrawlPost (the MCP-tool-path Firecrawl client) classifies no errors at all (try/finally only, no catch) -- timeout and generic transport failure propagate as the identical raw rejection. (4) openDriftPullRequest has no try/catch around any of its 5 GitHub calls -- confirmed BY TEST that a rejected fetch propagates uncaught at the function boundary; caught one level up by the webhook handler's own .catch() so it doesn't crash the process, but the error contract is inconsistent with sites that translate failures cleanly. (5) downloadAudio writes any 200 response to disk unvalidated; malformed bytes surface later inside ffmpeg, OUTSIDE runTranscription's try/catch -- it rejects with a raw `spawn UNKNOWN` instead of its own documented _not_configured/audio_download_failed contract (test named "KNOWN GAP: ...", asserts the real broken behavior rather than a false pass). (6) handlers.ts's Firecrawl proxy and web-research.ts's firecrawlPost are two independent, live, hand-rolled implementations of the same integration -- real unrefactored duplication, not dead code; consolidating them is a design call outside this unit's scope. **Verified:** full monorepo build clean; all 10 touched test files green against a REAL local Postgres (`axis-test-pg` container, port 5433) -- 204 tests, 1 skipped, 0 failed on the batch run, one transient 30s timeout on a pre-existing (not new) test under 6-way concurrent DB load from the parallel agents, confirmed clean (30/30) on immediate isolated re-run. Pushed clean to `main` on the first try (`0ca8c29`) -- no repeat of the branch-target mistake from the H7 unit. CI green (`build-and-test` 20+22, `deploy-web`, `live-probe` job all success; `docker-build`/`deploy-api` correctly skipped per Render's independent Blueprint autoSync). **Live-probed** via `scripts/live-probe.mjs` against real production post-deploy: 6/6 checks pass -- this unit's apps/api changes are comment/export-only with zero functional behavior change, so a basic health confirmation is the right-sized check here, not a feature-specific probe (contrast with H7's targeted model-cascade.md content fetch, which was warranted there because that unit shipped new user-visible output). No FAILURES-ledger entry -- no unit halted. H8.1b (adding real timeout mechanisms to the 8 waived sites, then their timeout tests) is tracked as a new, separate STATE row -- split off per the anti-gold-plating law since it's real production-code behavior change across 5 files, not test-writing | `0ca8c29` |
| 2026-07-14 | x402-token-first (owner directive, mid-session) | Token/USDC rail now leads the 402 payment envelope, with explicit machine-evaluable per-rail economics. Owner asked for three things while H8.1b agents ran: surface USDC first in the 402 response, make rail economics explicit so agents can evaluate them alone, and make Iliad's economics a pickable FACT. Verified first: `accepted_payment_schemes` led with `mppx/stripe` (packages/mpp `build402NegotiationBody`), `chargeMpp` registered+composed `[stripe, tempo]`, an x402/USDC block already existed but sat unranked mid-body, and NO per-rail economics existed anywhere. Read mppx source (`Mppx.ts` compose) before flipping anything: compose() appends WWW-Authenticate challenges in handler order UNLESS the client sends Accept-Payment, in which case AcceptPayment.rank wins -- so order = server preference with client override preserved, exactly the protocol-correct lever. Changes: schemes reordered token-first (`x402/usdc/<network>`, `mppx/tempo`, `mppx/stripe`) when TEMPO_RECIPIENT_ADDRESS is set; new `preferred_payment_scheme`; new `payment_rails` block with per-rail price_usd/lite_price_usd (derived from the pricing registry, nothing invented per the Honesty law), one-line summary ("USDC on base @ $0.50 per analyze_repo call"), settlement mechanics, intermediaries, chargeback exposure (none on-chain -- factual), surcharge (none either rail -- the listed price is all-in); x402 pay-to block moved up beside the rails; `chargeMpp` registers+composes tempo BEFORE stripe so the wire-level challenge order matches the body; docs/x402/CONTRACT.md (the cross-project canonical envelope for PAI'D/Foundry) bumped to v1.1 with the new fields + an explicit "ordered by SERVER PREFERENCE" note. Deliberately NOT done -- flagged to owner instead of silently changing money math: making USDC strictly CHEAPER than card (a real discount) would touch settlement accounting (receipts record pre-rail amounts; mppx settles per-method amounts) and is a pricing decision, not a protocol-surface fix; both rails advertise the identical all-in price today, truthfully. **Verified:** @axis/mpp + @axis/api typecheck clean; 3 new tests (token-first ordering + rail economics with recipient set, Stripe-only fallback without, testnet network naming) in budget-probe.test.ts; full 402/MPP suites green against real local Postgres -- budget-probe + payment-required-contract + mpp 146/146, mcp-inband-settlement + quota-guardrails + cashier-paid-wallet 71/71. Committed `8a3ec50` (4 files, selectively staged around the in-flight H8.1b working-tree changes) | `8a3ec50` |
| 2026-07-14 | x402-token-first follow-ups (owner exchange) | Fee-asymmetry economics + AML posture doc + prod token-rail gap found and declared. Owner confirmed USDC is already the cheaper rail BY FEE HANDLING and asked whether Stripe+PAI'D co-orchestrating is an AML conflict. (1) `payment_rails` now states `processing_overhead` per rail -- card: Stripe's PUBLISHED standard rate (2.9% + $0.30, attributed as published per the Honesty law, majority of a $0.50 micro-payment); USDC: network gas only, no percentage, no fixed fee -- plus `why_preferred` on the USDC rail tying the fee asymmetry to why list prices stay low. Agent-facing price unchanged and identical on both rails; code comment + posture doc both encode the steering rule: any future differential ships as a token-rail DISCOUNT, never a card surcharge (card-network rules). (2) AML answer recorded in new `docs/x402/PAYMENTS_COMPLIANCE.md` (not-legal-advice header): first-party settlement on every rail is the invariant keeping Iliad outside MSB/AML-program scope; Stripe carries AML on card; PAI'D orchestrating same-beneficial-owner funds is billing software not an MSB (same watch-item as the standing MTL constraint); FinCEN 2019 CVC guidance makes merchant USDC acceptance 'user' not 'transmitter'; the real residual is OFAC strict liability on the bare on-chain rail, mitigated by micro-amounts + Circle's issuer-level blacklist + off-ramp KYC, with inbound SDN screening named as the growth-triggered hardening step; watch-items: PAI'D touching third-party money, entity divergence, any custodial USDC behavior. (3) Owner set pricing phasing (saved to memory): collect-first at current prices to pay down dev cost, raise later to fund R&D -- so NO usdc discount now (identical gross + token steering = max NET), and collection-leak fixes become top engineering priority (lite-mode enforcement gap is lever #1, the H8.1 checkout malformed-200 finding is lever #2). (4) **Live-probed production's actual 402 body** (fresh probe account via the documented free signup, key used in-memory and never printed): the new fields are LIVE (Render autoSync had already deployed `2ff0591` -- payment_rails/preferred_payment_scheme/processing_overhead all serving) but `payTo: null` -- the Stripe-only fallback, which exposed that **TEMPO_RECIPIENT_ADDRESS was never declared in the Render Blueprint at all: the token rail has been OFF in production this whole time**. Declared it in render.yaml as sync:false (the wallet address stays dashboard-set deliberately -- a committed payment address is a PR-tamper target that could redirect revenue); TEMPO_TESTNET intentionally undeclared (absent = mainnet). OWNER ACTION REQUIRED: set the receiving wallet address in the Render dashboard to actually enable token-first payments in prod. **Verified:** budget-probe 111/111 with new attribution assertions; @axis/mpp + @axis/api typecheck clean; CI green on `2ff0591` (build-and-test 20+22, deploy-web, live-probe all success); render.yaml validated via npx js-yaml before commit. Two throwaway probe accounts (arvayjonathan+x402probe/+x402probe2) now exist on prod free tier -- disclosed, harmless, owner may delete | `2ff0591`+`9fd63af` |
| 2026-07-14 | H8.1b | Client-side timeouts on all 8 previously-waived external call sites; every `H8.1 WAIVER` comment removed. Split across 3 parallel agents (handlers.ts Firecrawl x2 / stripe.ts x3 / network-token+github-pr+email), each given the embeddings.ts AbortController pattern verbatim as the template. Budgets: Firecrawl scrape 30s / crawl 60s (client-side enforcement of the budget already sent via the body-level `timeout` field), Stripe subscription-truth-fetch 10s PER retry attempt (the loop bounded attempt COUNT but never per-attempt wall time), checkout + cancel 15s, network-token read 15s, GitHub `ghCall` 15s per call (5 sequential calls per drift PR, worst case ~75s for the webhook-triggered background flow), Resend email 15s. Every site preserves its existing error contract exactly: catch-owning sites produce the same normalized message for a timeout as for any transport error; the no-catch sites (network-token, github-pr) still propagate the rejection unchanged -- the guards add a bound, never new behavior. 8 new timeout tests, one per site, all fake-timer + abort-reactive-stub with a waitForFetchCall-style poll (real setImmediate + advanceTimersByTimeAsync(0) each tick) so real I/O reaches the fetch before the fake clock advances past its abort timer. **Two real test-authoring bugs found and fixed by actually running things, not trusting reports:** (1) the stripe agent discovered the whole fake-timer/real-HTTP interaction class itself -- advance-before-timer-registration hangs, a same-tick library setTimeout captured by the fake queue, and a leaked never-resolving global fetch stub that broke 8 unrelated later tests -- and fixed all three in its own file, documenting the waitForFetchCall pattern. (2) the handlers.ts agent's 2 Firecrawl tests shipped WITHOUT that pattern (its own verification run never completed before it reported) and hung for 2x300s HOOK timeouts on the closeout verification run -- rewritten by hand to the proven stripe-branches pattern (spy captured before enabling fake timers, poll until the fetch is actually called, restore realFetch + real timers in finally), after which web-research.test.ts went 22/22 in 128s. Lesson reinforced: an agent reporting 'code done, typecheck clean' with its own test run still pending is NOT a verified unit -- the closeout owner runs the suites regardless. **Verified:** full monorepo build clean (13/13 packages); all 6 touched test files green against real local Postgres (web-research 22/22, stripe 26, stripe-branches 41, network-token 31, github-pr 7, email 34) | `28af5e3` |
| 2026-07-14 | lite-mode-enforcement (owner collect-first directive, prioritized ahead of H8.2) | Every lite_description promise is now ENFORCED -- closes the top collection leak (H4.1 finding: lite-paying callers got full standard behavior on every iliad_* tool, so no rational agent ever paid standard). Research agent mapped the surface first: mode resolved per-handler via resolveAgentMode(req) (engineer gating shipped inline this way), price via priceForMode, NO behavioral lite branch anywhere; ONE dispatch choke point in mcp-server.ts tools/call holds both toolArgs and req. Hybrid design per that report: (1) NEW apps/api/src/lite-caps.ts -- pure data-driven applyLiteCaps(tool,mode,args) for the 10 input-transform tools (ttl<=3600, top_k<=10, single-string embeddings, 1-recipient/plaintext email, analytics batch<=50/limit<=25, max_tokens<=256+temperature=0, sandbox timeout<=10+no-node, tts 500chars+wav, max_results<=10, crawl<=5), hooked BEFORE the idempotency hash so replayed results reflect clamped args; absent-arg FILLS where impl defaults exceeded the cap (llm 512, sandbox 30, crawl 10, analytics 100) -- omitting an arg no longer leaks standard behavior; rejects use the existing pre-dispatch tool-error shape, always before any charge. (2) Four inline enforcements a table can't express: vector-db lite rejects an upsert pushing a namespace past 1,000 vectors (pre-charge countVectors, whole-batch reject, never partial); hygiene lite fix returns remediation plan + grade/counts and OMITS per-finding findings[] (mirrors the one pre-existing lite output-shaper, runPreparePurchasing); document-parsing gains tighten-only max_doc_bytes/max_markdown_chars plumbing (lite 5MiB in / 256KiB out, standard remediation strings byte-identical); REST handleFirecrawlCrawl clamps lite to 5 pages before the free-pool estimate AND the Firecrawl payload (MCP path covered by the table). Bonus catch by the central agent beyond its brief: settleMcpCallInband charged cash pre-dispatch and hashed RAW args -- a lite retry against a stored result would have false-positived as an Idempotency-Key hash mismatch; it now applies the same caps (no settle on rejection) and hashes capped args. Honesty fixes in the same commit: iliad_web_research's promised lite restriction ('no structured data extraction') was VACUOUS -- no such extraction exists in any mode on that path -- copy replaced with the truth rather than fake enforcement added; analytics description verb aligned to its reject behavior; no *_cents changed. NEW CONTRACT TEST: parses each lite_description's numeric caps and asserts they equal the enforced constants -- copy and behavior can never silently drift apart again. 40+ new tests total incl. charge-not-taken proofs on every reject path and standard/engineer byte-identical checks. Process: 2 parallel agents with disjoint file scopes, BOTH under the new completed-test-run-required rule from H8.1b's lesson -- both complied (225/225+170/170 and 106/106 respectively), zero rework needed this time. **Verified (closeout owner's own run):** @axis/api + @axis/mpp builds clean; consolidated run of all 11 touched suites against real Postgres: 501/501 | `263a25b` |
| 2026-07-14 | H8.2 | Money-math property invariants over usage-credit-metering: ~2,087 seeded cases (hand-rolled mulberry32, seed 0x48382e32, no new dep; every assertion embeds seed+tuple for byte-exact replay). Spec's field names verified real (credits_required/included_credits_applied/overage_credits verbatim); structural drift found and adapted: splitFromUsed is module-PRIVATE so invariants run through the exported surface (previewUsageCredits fuzz + consumeUsageCredits accumulation); allowance is plan-derived not free (free/paid/suite reachable without Stripe rows; pro/enterprise need subscription rows, documented in-file, boundary geometry covered regardless). Invariants: conservation applied+overage===required; non-negativity; cap applied<=max(0,allowance-used) incl. negative banked headroom / used==allowance / overshoot / int4-max; bookkeeping + DB-path===creditsFromUsdCents cross-check + integer-ness; 1,000-case pure fuzz (0 iff non-positive, >=1 floor, monotone); 60 sequential consumes w/ running exactness + ledger-SUM equality (no lost updates); 25-consumes===one-big-consume path-independence on credit-exact 18-cent multiples (float-safety pre-probed to k=20M). Acceptance mutation check DONE and reverted byte-clean (never committed): splitFromUsed Math.min(remaining,...)->Math.min(monthly_allowance,...) caught by THREE independent tests (cap 'expected 10000 to be <= 1', sequential cap step 1, N===1 equivalence 'expected 1790700 to be 75000'). Confirmed intended, not a bug: amountCents<=0 still charges credits_required=1 (Math.max(1,...) floor) -- asserted as such. **Verified:** @axis/snapshots tsc clean; both metering suites 16/16 vs real Postgres, agent run AND independent closeout re-run (production files confirmed byte-clean of the mutant first) | `7b6fc2b` |
| 2026-07-14 | H8.3 | Mutation-lite pass on the full money path -- 78 mutants enumerated across 5 files, 0 surviving unaccounted for (66 killed by existing suites, 8 new killing tests for genuine survivors, 3 proven-equivalent mutants documented not faked into a test, 1 site skipped as non-money-path per the spec's own cosmetic-skip guidance). Split across 2 agents in ISOLATED GIT WORKTREES (mutations on the same production file can't collide across isolated copies): cashier.ts+stripe.ts (17+30 sites) vs usage-credit-metering.ts+growth-store.ts+mcp-runtime.ts (15+4+12 sites). Both agents' FIRST dispatch hit a Fable-5 usage-credit ceiling mid-investigation (before any mutation) -- re-dispatched clean under Sonnet 5 after the owner ran /model sonnet, zero work lost since worktree isolation auto-cleans on no-changes. Genuine gaps found and closed (not just boilerplate boundary-flips): usage-credit-metering's price-derived plan resolution -- a 'paid'-tier account with an ACTIVE 'pro'-priced Stripe subscription must be metered as pro (300k credits), not the tier default (75k) -- nothing in the existing suite seeded a subscription row, so this branch was silently untested; growth-store's 30-day trailing revenue window's INCLUSIVE boundary -- a settlement landing exactly ON the cutoff instant must still count, now covered by a directly-inserted boundary row; mcp-runtime's idempotency-key 255-char cap was untested at its exact edge on either side; stripe.ts's exact-timestamp webhook replay (same eventCreated, not older) and the missing-timestamp OR-vs-AND guard (proven via forging a literal 'undefined'-string HMAC to force the only input where OR and AND diverge). 3 equivalent mutants proven (not tested, since no test COULD kill them without false-claiming): stripe.ts idx>0 vs idx>=0 in signature-segment parsing (only diverges at idx===0, where slice(0,0) is always '' regardless -- an unread map key either way); usage-credit-metering's overage-rounding >  vs >= at the exact point where overage_credits is already proven >=0 one line above by Math.max (both branches compute Math.ceil(0*0.18)=0 identically). 1 site deliberately skipped, not silently ignored: stripe.ts's 502-vs-503 error-code classification on an ALREADY-erroring Stripe response -- no money-path effect (the request already failed), cosmetic per the spec's own guidance to skip non-money sites. Cross-agent DB contention (both agents + the closeout owner hammering the same shared axis-test-pg Postgres concurrently) produced several transient 'deadlock detected' failures across the whole unit -- EVERY one confirmed via isolated re-run before being dismissed, never blanket-assumed; one agent worked around it entirely by provisioning its own isolated test DB for its run. Worktree changes don't auto-merge to the main tree -- copied each agent's reported test files directly (base commit identical to main's tip in both cases, so a plain file copy was a clean, conflict-free apply; verified the agent-report's pasted code blocks were HTML-entity-escaped by the notification rendering pipeline only, NOT actually corrupted in the real files, by grepping the copied files for literal operators before trusting them). **Verified (closeout owner's own consolidated run, after both worktrees landed and contention cleared):** full monorepo build clean (13/13 packages); all 5 production files confirmed byte-identical to pre-unit state; 14 touched suites together against real Postgres: 237/237 | `1d9a051` |
| 2026-07-14 | H8.4 | PAI'D contract fixtures + live idempotency canary -- built and verified everything safe to build/run without production credentials; the live-execution step itself is an explicit disclosed owner action, not silently done or silently skipped. Research first (findings mattered before any code): the spec's own named pattern file, live-settlement.e2e.test.ts, turns out to test Stripe TEST mode via mppx -- it NEVER calls PAI'D at all; its relevance to H8.4 is structural/stylistic only (the describe.skip-unless-credentialed discipline), not a PAI'D test to extend. Enumerated exactly 3 real PAI'D endpoints Iliad's runtime calls (createPaidCheckoutSession, getPaidWallet, debitPaidWallet), all in packages/paid-client/src/index.ts; apps/api/src/paid-client.ts is a thin re-export adapter with zero HTTP logic of its own. Confirmed via render.yaml + grep: api.trustfabric.ai is genuinely PRODUCTION (a real committed PAID_MERCHANT_ID), no PAID_TEST_*/sandbox/dry-run flag exists anywhere in the client, and no PAID_* credential is set in this session's environment -- so a live canary literally cannot be self-executed here regardless of intent. Golden fixtures: 4 frozen JSON files under packages/paid-client/src/__fixtures__/golden/ (checkout-session, wallet-read, wallet-debit success + its 402 insufficient-credits shape), following the existing packages/ap2 golden-fixture convention (fixture-completeness test + per-fixture request/response assertions + a tamper test proving real serialization, not an echoed fixture) -- packages/paid-client had ZERO test files before this; apps/api's existing paid-client.test.ts only exercises the Iliad wrapper with inline literals, so this is genuine new coverage of the shared package other AXIS merchant apps also depend on. 14 new tests, all green (no credentials needed -- pure fetch-mock). Live canary: new apps/api/src/paid-live-canary.e2e.test.ts, gated exactly like live-settlement.e2e.test.ts's describe.skip-unless-credentialed pattern, answering the question 4 separate comments in this codebase have asked since WO-04 without ever resolving: does PAI'D honor Idempotency-Key? Two tests -- checkout-session retry (ZERO financial risk by construction, a session alone never charges anyone) and wallet-debit retry (LOW but non-zero side effect: one real permanent ledger row via a synthetic developerId; this repo's own MCP_PAID_ACCESS_DESIGN.md already documents PAI'D's debit as a pure internal ledger write with no real provider call, so no card is ever touched). Caught and fixed a real bug in my OWN new canary before shipping: loadPaidConfig() was hoisted to the describe body's top level -- vitest's describe.skip still INVOKES the callback to collect child its (only the its themselves are marked skipped), so this threw during collection regardless of the credential gate. Fixed by moving it into beforeAll; re-verified the file now collects cleanly and reports '2 skipped, 0 failed' with no credentials present, rather than erroring. Updated all 4 Idempotency-Key comment sites (packages/paid-client/src/index.ts x2, apps/api/src/paid-client.ts's walletDebitIdempotencyKey doc, docs/MCP_PAID_ACCESS_DESIGN.md x2) to point at the new canary honestly: a verification mechanism now EXISTS, it has NOT yet been run, running it is an explicit owner action -- never claimed verification happened when it didn't. Deliberate scope boundary, not an oversight: building the canary and proving it collects/skips correctly is within this loop's own authority; sourcing and using LIVE PRODUCTION MERCHANT CREDENTIALS to self-execute this codebase's first-ever real write to PAI'D infrastructure is a materially different risk class than anything else in this session (code/test/doc changes, or read-only production health probes) -- treated as requiring the owner's hand on it, matching the same disclosed-gap pattern as the TEMPO_RECIPIENT_ADDRESS Render action from earlier this session, not silently attempted and not silently dropped from scope. **Verified:** full monorepo build clean (13/13 packages); golden suite + existing paid-client.test.ts 34/34; canary file collects cleanly, 2 skipped / 0 failed with zero credentials present | `bc78fb1` |
| 2026-07-15 | H8.5 | OpenAPI ↔ router bijection test -- built on a REAL introspection primitive, not a source-text regex: added Router.getRoutes() to router.ts (stores each route's original :param path before it's compiled to a regex) and exported the live router from server.ts, so the test's ground truth is exactly what serves production traffic, immune to future changes in HOW routes get registered. Ran the real bijection before writing the test and found genuine drift, not a clean pass: 13 REST endpoints registered on the router but undocumented in openapi.ts -- GET /performance, GET /performance/reputation, GET /agents.json, 4 .well-known discovery files (glama.json, ai-plugin.json, oauth-authorization-server RFC 8414, oauth-protected-resource RFC 9728), the entire /oauth/* authorization-server surface (authorize/token/jwks/introspect -- a real OAuth 2.0 server implementation with zero prior API documentation), GET /pricing, and GET /accounts's method-not-allowed guidance route. All 13 fixed by adding accurate openapi.ts entries -- none waived, per the spec's own acceptance bar. Caught and fixed an adjacent honesty bug while adding /accounts's GET entry: /v1/accounts's ALREADY-DOCUMENTED GET entry claimed '200: Account info' but the real handler (handleAccountsMethodHint) returns 405 Method Not Allowed with POST guidance -- writing a new, accurate entry for the twin /accounts route while leaving the existing one wrong would have shipped an inconsistent spec knowingly; fixed both in the same small edit (well under the drive-by threshold). Waiver list holds exactly 10 genuinely non-REST/alias router registrations, each with an inline justification, matching the spec's own named exemption (/mcp JSON-RPC): trailing-slash and /v1-prefix aliases of the already-documented GET+POST /mcp endpoint, the /mcp/sse transport variant, the legacy /mcp/mcp/* double-prefix compat alias, two /mcp-prefixed duplicate .well-known manifests, /favicon.ico (a static asset), and the /v1/accounts// /accounts/ trailing-slash aliases. The test itself has 3 assertions: non-waived router routes all appear in openapi.ts (path+method), every openapi.ts path+method has a real router registration (no stale/orphaned doc entries -- this direction was already clean, zero findings), and the waiver list itself contains no stale entries. A pre-existing enforcing test (openapi.test.ts's 'all paths start with /v1/ or are root-level MCP paths') needed its NON_V1_PATHS allowlist widened for the 13 new legitimate non-/v1/ paths -- exactly the same 'the enforcing tests found more than the named list' pattern from H7; updated it rather than treating it as an obstacle. Confirmed ENDPOINT_COUNT (counts.ts, pinned at 157) needed no change -- its own doc comment already excludes .well-known/* and /oauth/* by policy, and this unit added zero new server.ts route registrations, only openapi.ts documentation. **Verified:** typecheck clean; scoped suite green across all 7 touched/adjacent files (openapi.test.ts, router.test.ts, router-branches.test.ts, server-routes.test.ts, the new openapi-router-bijection.test.ts, counts-consistency.test.ts, well-known-handlers.test.ts) -- 190/190; full monorepo build clean (13/13 packages) | `f401578` |
| 2026-07-15 | H8.6 | Architecture-boundary fitness tests -- hand-rolled fs walk, no new dependency (knip/madge stays a dep-gated future upgrade per the spec's own wording). New apps/api/src/monorepo-boundaries.test.ts, 4 assertions: apps/web/src and apps/api/src never import each other's src via a relative path (regex-matched real `from "..."` / `import("...")` specifiers resolved to an absolute path and checked against the target dir, not a naive substring grep -- deliberately avoids false-positiving on the existing count-honesty.test.ts/deployment.test.ts pattern of reading the OTHER app's source as plain text for doc-consistency checks, which is legitimate and not an import); packages/*/src never imports apps/*/src; no cycles in the @axis/* workspace-dependency graph, built from every package.json's dependencies+devDependencies and walked with a hand-rolled 3-color (white/gray/black) DFS cycle detector. Ran the checks before writing the test, same discipline as H8.5: zero violations on all 4 axes -- apps/web and apps/api genuinely never cross-import, no package reaches into an app, and the dependency graph is a clean DAG (snapshots is the only true leaf; repo-parser -> snapshots; context-engine -> snapshots+repo-parser; generator-core -> agentic-compliance+ap2+snapshots+repo-parser+context-engine; api/cli sit on top). This unit LOCKS an already-true invariant going forward rather than fixing a break -- no drive-by fixes needed, matching the anti-gold-plating law (nothing to trim). **Verified:** typecheck clean; new test 4/4 green (no DATABASE_URL needed -- pure fs walk, no DB touch); full monorepo build clean (13/13 packages) | `476e40d` |
| 2026-07-16 | H8.7 | Neon data-safety drill -- new docs/RUNBOOK_ROLLBACK.md documents Neon's real PITR/branch-restore mechanism (instant restore of the default branch vs. branch-from-timestamp for a non-destructive drill) and the exact drill procedure (create a branch at T-minus-15-minutes, verify row counts on 3 representative core tables -- accounts/identity, snapshots/product, payment_receipts/financial -- against the new branch's own connection string, delete the branch). Escalating per the spec's own named condition ("Escalate if no Neon credential label exists"): this environment carries only DATABASE_URL, a bare Postgres connection string (dashboard-set, sync:false in render.yaml) -- confirmed via docs/SECURITY_ROTATION.md's credential inventory and this session's own environment that no NEON_API_KEY or equivalent Neon-console/API credential exists anywhere, and a Postgres password cannot create/list/delete a Neon branch (that requires the separate Neon API or neonctl CLI, authenticated with a Neon API key). Deliberate scope boundary, matching the H8.4 PAI'D-live-canary pattern: writing and proving the runbook procedure is buildable now; actually creating/verifying/deleting a real branch on the live project requires the owner's own Neon console access -- disclosed as an outstanding owner action in the runbook itself and the ledger, not silently skipped and not faked with placeholder numbers. **Verified:** docs-only unit, no code/test changes -- N/A for typecheck/build/test suites. Runbook procedure cross-checked against Neon's own published branching/PITR mechanism (copy-on-write branches, branch-from-timestamp vs. in-place default-branch restore) | `d93e014` |
| 2026-07-16 | H8.8 | Deploy rollback runbook + rehearsal -- docs/RUNBOOK_ROLLBACK.md Section 2 documents 3 rollback paths (git-revert-and-push, the default, needing no special credential; Render dashboard "redeploy a previous deploy" for axis-api; Cloudflare Pages dashboard "rollback to this deployment" for axis-web) plus the standing rule already governing this whole loop, made explicit: CI red after your push is priority zero, nothing else proceeds. REHEARSED PATH 1 FOR REAL against production main, not just documented: pushed an inert no-op commit (d0417de, a single marker file with zero runtime effect) at 22:31:10Z, confirmed CI green (both workflows) + live /v1/health + /v1/health/ready green; then git revert --no-edit -> e720eb1, pushed at 22:44:57Z, confirmed CI green again + live health green again. Paths 2/3 documented but explicitly deferred to PENDING-OWNER -- both need Render/Cloudflare dashboard or API credentials this session does not have (same disclosed-gap pattern as H8.4/H8.7), and the underlying mechanism (both platforms retain deploy history with a one-click prior-deploy action) is standard platform behavior this repo's own code cannot misconfigure, so documenting it accurately was sufficient without live access. Genuine mid-unit finding, not a code bug: gh CLI's stored credential went invalid partway through this session (gh auth status: "the token in keyring is invalid", persisted across retries) -- confirmed NOT a GitHub-side outage via githubstatus.com (all green) before treating it as a real local auth problem. Root-caused to a keyring/credential-manager issue distinct from git's own push credential (which kept working throughout -- different storage path). Workaround: this repo is PUBLIC, so the GitHub REST Actions API needs zero auth for a public repo (curl https://api.github.com/repos/.../actions/runs) -- used for every CI-status check in this unit's rehearsal and recorded in the runbook itself as a real incident-response finding worth knowing (if gh is ever unusable mid-incident, this fallback doesn't wait on re-authentication). **Verified:** docs-only unit (Section 2 + rehearsal evidence), no code/test changes -- N/A for typecheck/build/test suites. The rehearsal IS the verification: 2 real push/CI/deploy/health cycles on production main, both green | `81d9ad3` |
| 2026-07-16 | H8.9 | Synthetic monitor (dead-man's switch) -- new .github/workflows/synthetic.yml promotes scripts/live-probe.mjs (H6.3) from a CI-only informational step into a standalone scheduled check (every 30 min via cron, plus workflow_dispatch with a force_failure input for on-demand testing): hits production directly, opens/updates a single tracking issue on failure, closes it on recovery. Non-gating by design (matches live-probe.mjs's own contract) -- observes production, never blocks a merge/deploy. Uses only the native GITHUB_TOKEN, no new secret. live-probe.mjs gained an additive $GITHUB_OUTPUT emission (failed_count/total_count/failed_names), a no-op when unset so every existing caller (ci.yml's own live-probe job) is unaffected. Drive-by fix caught while touching the file: EXPECTED_MCP_TOOL_COUNT was still 36, stale since the x402 onboarding program's Phase 1 shipped the 37th tool -- this script wasn't in that unit's apps/api/src sweep scope. Confirmed via live run: was silently reporting a false mcp_tools_count failure before the fix. REHEARSED FOR REAL, not just documented -- 5 live workflow_dispatch runs against production, each inspected via the GitHub REST API before moving to the next: (1) forced failure -> opened issue #214 as designed; (2) a natural (non-forced) run found a REAL failure on web_bundle_marker -- investigated rather than dismissed as flaky, since a normal check failing right after a forced-failure test could plausibly be either; (3) a second natural run failed the identical check again, now 2-for-2 against 0-for-many from this session's own local IP -- a real, reproducible signal, not noise. Built a throwaway diagnostic workflow (curl -v + Node fetch with full header dump, removed after use) to see exactly what a GitHub Actions runner receives: Cloudflare's bot mitigation was serving a 403 'Just a moment...' JS challenge (cf-mitigated: challenge header) specifically to the runner's datacenter IP range -- a real browser, and this same check run from a residential IP, gets 200 + the real bundle every time. This is a PRE-EXISTING blind spot in live-probe.mjs that predates this unit entirely: ci.yml's own live-probe job has run this exact check on every push all session and never surfaced it, because that job is non-gating and nobody was reading its FAILED list systematically until H8.9 built something that does -- the new monitor didn't introduce this bug, it's the first thing that could see it. Fix, not a workaround: web_bundle_marker stays fully visible in the console/log output (still checked, still reported) but no longer counts toward the synthetic monitor's alert-worthy failure count -- a new EXCLUDED_FROM_AUTO_ALERT set in live-probe.mjs (currently just this one check, with an explicit comment requiring the same live-confirmed IP-specific root cause before anything else is added to it) and new alert_failed_count/alert_failed_names outputs synthetic.yml reads instead of the raw ones. Deliberately chose narrowing what counts as alert-worthy over loosening the check or deleting it -- the check itself is correct (browsers really do get 200), the bug was treating a Cloudflare-side, CI-IP-specific artifact as equivalent to a real user-facing outage. (4) a 4th run confirmed the SAME Cloudflare challenge still fires post-diagnosis (expected -- the diagnostic workflow only observed, it didn't fix anything yet); pushed the real fix; (5) a final run after the fix landed confirmed alert_failed_count correctly reads 0 despite web_bundle_marker still failing from the CI IP, and the workflow's own recovery path auto-closed issue #214 for real (closedAt: 2026-07-16T23:47:54Z) -- proving BOTH the open/update path (genuine detection across 3 comments) AND the close/recovery path live in production, not just unit-tested. New deployment.test.ts coverage: synthetic.yml's schedule, force_failure input, GITHUB_TOKEN-only secret usage, both issue-open/close code paths, and that the workflow reads alert_failed_count/alert_failed_names (not the raw, unfiltered ones) -- a regression guard against silently reverting the Cloudflare fix. **Verified:** typecheck clean; full monorepo build clean (13/13 packages); deployment.test.ts 42/42; CI green on the shipping commits; 5 live production workflow_dispatch runs, each individually inspected via the GitHub REST API, ending in a genuinely auto-closed tracking issue | `bf1acb6`+`12d9322` |
| 2026-07-16 | H8.10 | Web security headers -- apps/web/public/_headers gains Content-Security-Policy, Strict-Transport-Security, and frame-ancestors 'none' alongside the pre-existing X-Frame-Options/X-Content-Type-Options/Referrer-Policy/Permissions-Policy. CSP built from an actual audit of production, not guessed: fetched the live HTML + JS bundle and grepped every external origin referenced. connect-src/script-src allow exactly api.iliad.trustfabric.ai + axis-api-6c7z.onrender.com (the two hostnames for the same underlying API service -- the spec's 'two API origins') and static.cloudflareinsights.com (Cloudflare Pages' own auto-injected analytics beacon, which a naive CSP would have self-broken). script-src/style-src need 'unsafe-inline' -- the page ships 4 inline application/ld+json blocks and this is a static Vite/React build with no server-side nonce infrastructure to do better; documented as a real, disclosed tradeoff rather than hidden. frame-src/object-src are 'none' -- confirmed via source grep the app uses zero iframes and zero third-party form actions (PAI'D checkout and GitHub/Google OAuth are both plain window.location/<a href> full-page navigations, neither subject to CSP's fetch/frame-embed directives). HSTS deliberately omits includeSubDomains and preload: trustfabric.ai hosts other AXIS products (e.g. Foundry, per prior memory) on other subdomains this repo doesn't control or verify HTTPS-readiness for -- includeSubDomains would apply HSTS enforcement to ALL of them from a header this repo alone controls, a blast-radius risk beyond this unit's authority. preload is a separate, deliberately hard-to-reverse commitment (browsers refuse plain HTTP forever once submitted to the preload list; de-listing takes months) that wasn't asked for and isn't assumed. LIVE-VERIFIED per the spec's own acceptance bar, not just deployed: caught my own premature check (grabbed the wrong parallel CI run -- Compliance Check finished first while the real CI workflow with deploy-web was still in progress -- corrected by finding the actual run id and polling that one specifically) before trusting a curl result. Once the real deploy completed: curl -I confirms both CSP and HSTS live on production with cache-busting query params to rule out edge caching. Hash routing: GET /#/plans and an arbitrary deep client-side path both return 200 with the SPA root div intact. OAuth: both /v1/auth/github and /v1/auth/google 302 to the real GitHub/Google authorize URLs with the expected registered callback hosts -- and this incidentally cross-validated the CSP's origin choice for real: GitHub's registered callback uses axis-api-6c7z.onrender.com while Google's uses api.iliad.trustfabric.ai, confirming BOTH origins are genuinely live in production for different OAuth providers, not just one. **Verified:** typecheck clean; deployment.test.ts 47/47; full monorepo build clean (13/13 packages); dist/_headers confirmed byte-identical to source after build; CI green; live curl -I + hash-routing + OAuth-redirect spot-checks all pass on real production | `e1f6776` |
| 2026-07-16 | H8.11 | CI secret-scan (H8.11a) + webhook replay-protection audit (H8.11b). New scripts/scan-diff-secrets.mjs greps only the lines a push/PR ADDS (never the whole repo state) for the 6 named credential shapes (sk_live_/rk_live_/whsec_/re_/hf_/rnd_), diff-scoped so existing reviewed fixtures never need retroactive allowlisting. Validated against real data before shipping as a gating check: ran the same matcher against all 1,207 tracked files at HEAD -- 0 unaddressed findings. Caught a real gap during that validation: this repo's own fake-key convention is 0123456789..., not 1234567890... as copied from hygiene.ts's placeholder-marker list -- would have failed the scanner's own first real CI run. New gating secret-scan CI job (fetch-depth 0, resolves the right diff range per push/PR event). PROVEN live, not just deployed: pushed a scratch branch + PR (#215) containing a deliberate fake Render-token-shaped credential in a non-allowlisted path, confirmed the secret-scan job genuinely failed (while build-and-test independently passed and deploy jobs correctly skipped), then closed the PR and deleted the branch -- never merged. Real-world friction along the way, disclosed not hidden: GitHub's OWN push-protection secret scanner rejected 3 separate push attempts because the test file's fake Stripe-key fixtures were realistic-shaped enough to trip it (an unrelated, pre-existing GitHub platform feature, not anything built in this unit) -- root-caused via a discarded-and-redone local reset (confirmed via git log/git status that neither poisoned commit had ever reached the remote before rewriting them) and fixed by constructing the 'must detect a real leak' test fixtures via runtime string concatenation instead of source literals, since GitHub's scanner flags any sk_live_-shaped literal regardless of how obviously synthetic the content is. H8.11b: audited all 4 inbound webhook receivers (Stripe, PAI'D, GitHub push/PR App, GitHub architecture-drift) via a dedicated research pass, findings recorded in docs/webhook-replay-matrix.md. Real result: Stripe and PAI'D already had strong PROVEN replay protection (timestamp tolerance + compare-and-set or idempotent writes, verified via handler-level tests delivering the identical webhook body twice) -- independently re-confirmed PAI'D's coverage was actually already solid, correcting an inaccurate research-agent claim before acting on it (paid-handlers.test.ts already had 'is idempotent' tests for both the tier-change and credit-topup paths that a keyword search had missed). The two GitHub-signed handlers' in-memory delivery-ID dedup was correctly implemented but had ZERO test coverage -- github-webhook.ts's dedup logic was entirely untested, and architecture-drift-webhook.ts's ENTIRE HTTP layer (signature verification, config/routing guards, both replay guards) had never been exercised at all, only its pure orchestration core. No production code changed -- the audit's own acceptance bar was proving the protection works, which is what was missing. New tests: github-webhook.test.ts gained same-delivery-id dedup, distinct-delivery-id non-dedup, the duplicate_delivery log line, and resetGitHubWebhookState()'s own contract. architecture-drift-webhook.test.ts gained an entirely new handleArchitectureDriftWebhook HTTP-layer describe block (503/401/400 guards, ping, non-push/non-default-branch routing, the happy-path 202, both replay guards) -- built cheaply since every guard test returns before the expensive pipeline is ever reached, so no downstream dependency needed mocking. One real, disclosed (not fixed) limitation documented in the matrix: GitHub's signature scheme carries no timestamp, so the in-memory dedup has no staleness backstop and doesn't survive a restart or a 15-minute-plus replay gap. Accepted as-is: both handlers' blast radius is wasted compute on a non-financial, non-PII pipeline (a duplicate snapshot, or a duplicate drift-check that itself no-ops via driftBranchName's content-derived naming), and render.yaml pins numInstances:1 today (no cross-instance coordination gap) -- a persistent DB-backed dedup table (same shape as the existing idempotency_keys table) is named as the correct fix if this service ever scales horizontally. **Verified:** typecheck clean; full monorepo build clean (13/13 packages); touched suites 83/83 (github-webhook 29, architecture-drift-webhook 16, paid-handlers 38) plus the scanner's own 14/14; CI green on both shipping commits; the scratch-branch/PR proof is itself live acceptance evidence, not simulated | `7b63a3e`+`f226df1` |
| 2026-07-17 | H8.12 | Dependabot config -- new .github/dependabot.yml covers all 3 ecosystems this repo actually has (npm: the single root pnpm-lock.yaml covering the whole apps/*+packages/* workspace; docker: the root Dockerfile; github-actions: pinned Action versions across every .github/workflows/*.yml), weekly schedule, minor/patch grouped into one PR per ecosystem via Dependabot's native groups feature. Schema cross-checked against GitHub's own documented dependabot.yml reference (groups.applies-to/update-types, open-pull-requests-limit, per-ecosystem directory conventions) before shipping, not just YAML-syntax validated. Confirmed no auto-merge automation exists anywhere in this repo (grepped every workflow) before writing the file, so "never auto-merge" is satisfied by simply never adding one -- disclosed in the file's own header that Dependabot's schema has no "label only major-version PRs" primitive (labels apply uniformly per update block; groups don't carry their own label override), so the deps-discussion half of the law is enforced at triage time by hand, not mechanically guaranteed by the config alone. LIVE-VALIDATED, not just deployed: Dependabot triggered its first real update check within minutes of the push (visible as 6 completed-success 'Update #...' Actions runs, 2 per ecosystem) and opened a REAL first batch of 13 PRs -- proving the config parses and behaves exactly as intended, not merely that the YAML is well-formed. The grouping worked precisely as designed: PR #222 bundles 10 genuinely-minor/patch npm updates into one PR; 6 clearly-major npm version jumps (uuid 11->14, @vitejs/plugin-react 4->6, @types/node 22->26, typescript 5->7, pdfjs-dist 4->6, vite 6->8) each opened as their own individual PR, correctly NOT swept into the group; all 6 docker/github-actions bumps (none configured with grouping) also opened individually. Performed the acceptance criteria's own "first batch triaged per law" step for real: created the deps-discussion label (didn't exist before), applied it to all 12 individual (ungrouped) PRs, deliberately left the grouped minor-and-patch PR (#222) unlabeled since it's the lower-risk batch the law's spirit doesn't single out. Verified afterward via a fresh API read: all 13 PRs remain OPEN (none auto-merged, confirming the law held in practice, not just in config intent), the 12 individual PRs carry deps-discussion, #222 does not. **Verified:** dependabot.yml syntax + schema validated pre-push; CI green on the shipping commit; Dependabot's own 6 update-check runs all completed success; 13 real PRs opened and triaged exactly per the stated law | `9ae215d` |
| 2026-07-17 | A-cycle1 | Phase A re-audit cycle 1 (first cycle after H0-H8.12) -- baseline confirmed (CI green, live-probe 6/6) before dispatching 3 parallel read-only audit agents against the constitution's own named categories: payments/PAI'D coherence, web routes/auth/counts/dead-UI, engines/MCP honesty+determinism+gating. Every HIGH/MED finding was personally re-verified by reading the actual code before acting -- not trusted from the agent report alone (an explicit lesson from this session's own earlier PAI'D-webhook-coverage correction). 9 confirmed findings, ALL fixed this cycle (not deferred): (1) LOW -- 3 stale '36-tool' comments missed by the H8.9 sweep (web-audit agent), fixed directly, repo-wide grep confirmed clean. (2) MED, SELF-INTRODUCED REGRESSION -- ping_payment's 402 challenge conditionally issued a referral_token only when an attempted X-Axis-Key resolved to a valid account, letting anyone probe a guessed/leaked key's validity with no auth-failure signal (engines/MCP audit); fixed by dropping referral_token from the challenge response entirely, since real referral tokens are still issued on every genuine metered tool's 402 which requires real auth to reach. (3) HIGH -- PAI'D Pro subscribers were silently metered as Starter: Starter and Pro both collapse into the coarse 'paid' BillingTier and PAI'D never wrote to stripe_subscriptions, so resolvePlanForAccount always fell back to Starter's 75k-credit allowance regardless of which plan was actually bought (payments audit); fixed via a new accounts.paid_plan_id column (migration v39) the PAI'D checkout webhook persists and resolvePlanForAccount now prefers. (4) HIGH -- tier-audit.ts's Growth/suite price was a stale $99/mo holdover from the old 2-tier model (real price $299/mo), feeding the live GET /v1/billing/proration endpoint right next to UsagePage.tsx's correct '$299/month' copy (payments audit); fixed, 2 pinned test values recalculated. (5) MED -- several MCP/REST surfaces including build402NegotiationBody's upgrade_path (the function behind EVERY 402 body an autonomous agent sees) still said Pro costs $29/month (Starter's real price; Pro is $99/month) -- 9 call sites across mcp-tool-impls.ts/handlers.ts/funnel-store.ts fixed, 2 pinned tests corrected. (6)-(9) HIGH x4, the SAME class of gap across 4 tools -- lite_description promised a restricted/cheaper deliverable but nothing in the handler ever referenced resolveAgentMode, so a lite-price caller received the full standard-mode output every time (engines/MCP audit): iliad_speech_to_text (new getWavDurationSeconds() WAV-header parser enforces the 60s cap before the expensive whisper-cli step; word_timestamps forced false); analyze_repo/analyze_files (new restrictGeneratorsForLiteMode() filters generators to search/skills/debug before requested_outputs is built, applied at both identical call sites); prepare_agentic_purchasing (artifacts/purchasing_artifacts/all_artifact_paths withheld behind an artifacts_note when complianceDepth==='summary', score/gaps computation unaffected); assemble_representment (new deps.lite flag, server-derived from resolveAgentMode never caller-settable, short-circuits Stripe submission before the dispute client is ever constructed -- the most severe of the 4 since the leak was a REAL SIDE EFFECT on a live dispute, not just extra response bytes). Every fix has a dedicated new/updated test that was proven to fail against the pre-fix code (mutation-style: revert, confirm red, restore, confirm green) before being trusted. Disclosed, NOT fixed (deliberately out of this cycle's scope, named as future-cycle candidates): get_artifact is a free, unmetered, own-account-only tool that can reconstruct prepare_agentic_purchasing's withheld lite bundle piece-by-piece via free follow-up calls once an account already knows/guesses standard artifact filenames -- bounded to the caller's own account, not a cross-account leak, and closing it properly needs a broader get_artifact redesign. Step 3(c) mutation-lite rotation on cashier.ts (this session's newest money-path code, the x402 funnel-event call sites): all 3 candidate mutants (negating the wallet-enforce 402 guard, negating the chargeMpp 402 guard, removing the funnel-telemetry try/catch swallow) KILLED, 0 survivors, file confirmed byte-identical to pre-mutation state after each revert. Step 3 mechanical checks: 0 new fetch() sites, 0 new state-mutating endpoints this session beyond the 2 already-covered recordPaymentFunnelEvent call sites. Incidental finding during CI verification: web-research.test.ts's H8.1b timeout test failed once on a Node-20 CI runner and passed clean on an immediate rerun with zero code changes -- a pre-existing timing-sensitive flake (fake-timer test pattern), not a regression from any unit this cycle; confirmed via git diff that none of the 4 lite-mode fix commits touch web-research.ts or its test, noted as a candidate for a future dedicated de-flake pass rather than fixed here. dry-count stays 0 (this cycle had 9 findings, not zero) -- cycle 2 is the next step before Phase T can be considered. **Verified:** every fix commit individually typechecked + scoped-tested + CI green (5 separate pushes, 5 separate green CI runs, one flaky Node-20 job caught and confirmed transient via rerun) + live-probe 6/6 confirmed after each deploy, including the accounts.paid_plan_id migration | `0872f20`+`f8354f1`+`71e8184`+`2d1ff28`+`f2171ed`+`cac21fb`+`5eb73b6`+`a44d23c` |
| 2026-07-17 | A-cycle2 | Phase A re-audit cycle 2 -- baseline confirmed (full 13/13-package build, git ground-truthed, live-probe 6/6) before dispatching a SECOND round of 3 parallel read-only audit agents, each explicitly briefed to re-verify cycle 1's fixes AND look further than cycle 1's scope, not just re-confirm a clean pass. 9 more confirmed findings, ALL fixed this cycle, all sharing one theme: cycle 1's core fixes were CORRECT but not everywhere the same class of gap lived. 2 HIGH -- REST twins of the two MCP tools cycle 1 fixed for lite-mode leaks were never touched, because they're separate, duplicated implementations (handlers.ts's handleAnalyze/handlePreparePurchasing vs mcp-tool-impls.ts's runAnalyzeFiles/runPreparePurchasing) -- cycle 1 explicitly flagged them 'out of scope, check it' and cycle 2 confirmed both share the exact bug: (1) handleAnalyze (POST /v1/analyze) read X-Agent-Mode only to pick the charge amount, never to restrict requested_outputs -- fixed via the same FREE_PROGRAMS-gated filter pattern as the MCP fix. (2) handlePreparePurchasing (POST /v1/prepare-for-agentic-purchasing) had THREE separate leaks in one response block: an unconditional top-level gaps field (duplicate of the already-sliced top_gaps), a genuine spread+same-key-collision bug where a trailing `evidence,` key silently overwrote complianceSection's own mode-gated exclusion, and unconditional purchasing_artifacts/all_artifacts with zero complianceDepth check -- all three fixed, matching the MCP twin's artifacts_note pattern. 5 MED, all downstream of the SAME root cause the payments audit named directly -- accounts.paid_plan_id (cycle 1) was wired into the metering READ path but not every place 'which specific paid plan' matters for money math or plan identity: (3) GET /v1/billing/proration priced every real change/preview off Starter's $29 regardless of the account's actual plan -- new resolveAccountMonthlyPriceCents(tier, paidPlanId) in pricing-constants.ts (reusing MARKETED_TIERS, not a third hardcoded table); calculateProration takes an optional fromPaidPlanId; logTierChange (called by EVERY real tier change -- self-service, PAI'D webhook, legacy Stripe webhook -- and the one that permanently logs proration_amount into tier_changes history, not just the preview) now reads getAccountPaidPlanId itself before computing, ordered correctly BEFORE any plan_id write for the same transition. (4) growth-store.ts's estimated_mrr_cents multiplied every 'paid'-tier account by flat $29 -- the accounts-by-tier query now also counts paid_plan_id='pro' specifically; mrr_basis_cents grew a third field (starter/pro/suite, was paid/suite) since the old 2-field shape could no longer represent a blended calculation -- AdminPage.tsx's audit-trail formula text updated to match. (5) stripe.ts's syncTierFromStripeSubscription (the legacy, pre-PAI'D Stripe-direct tier-sync path -- still live/tested even though the web UI's only checkout is PAI'D) never wrote paid_plan_id at all, reintroducing cycle 1's exact bug for that one path -- fixed using priceToPlanId (already existed, already correctly distinguishes Starter/Pro/Growth by price id), ordered after logTierChange for the same reason. (6) UsagePage.tsx's TIER_LABELS is a 3-entry map keyed by the coarse BillingTier, so every real Pro subscriber saw their OWN billing page badge/sentence say 'Starter' -- new getAccountPlanId() reads usage_credits.plan_id from the same GET /v1/account call (mirrors the existing getAccountEntitlements pattern); deliberately did NOT expand the 'Need more?' upsell banner to add a new Starter->Pro path (a product/UX decision, not the confirmed label bug). 2 LOW, bundled into one commit: (7) admin's account list (GET /v1/admin/accounts) never projected paid_plan_id, a support/ops visibility gap, not a money bug -- added to the SQL projection + both AccountSummary type layers + AdminPage.tsx's table. (8) packages/mpp/src/index.ts carried 3 fully dead PRICING_TIERS entries (mcp_gateway_snapshot, ci_snapshot, compliance_check) plus 7 aliases pointing at them, confirmed zero live callers anywhere (no MCP registration, no METERED_MCP_TOOLS entry, no REST handler, no test) -- pruned rather than documented as a permanent exception, since nothing was actually calling them (unlike the pre-existing improve_my_agent_with_axis precedent, which still has a real unmetered handler). Every fix (except the pure dead-code prune, which had nothing exercising it to mutation-test) used the same mutation-style proof as cycle 1: revert, confirm the new test goes red, restore, confirm green. Two GH Actions concurrency cancellations encountered mid-cycle (pushing the next fix while the prior commit's CI was still running cancels it outright, per the branch's cancel-in-progress concurrency group) -- correctly diagnosed via `gh run view --json conclusion` showing literally 'cancelled' rather than mistaken for a real failure, per the lesson recorded at the end of cycle 1. dry-count stays 0 (cycle 2 also had findings, not zero) -- cycle 3 is next before Phase A can converge. **Verified:** every fix commit individually typechecked + scoped-tested + CI green (7 separate pushes, 7 separate green CI runs) + live-probe 6/6 confirmed after each deploy; a final full 13/13-package monorepo build confirmed clean after the last commit | `4d43fcf`+`4ccb47c`+`58cc3fe`+`06113a8`+`2bf873b`+`27c5a39`+`989de61` |
| 2026-07-17 | A-cycle3 | Phase A re-audit cycle 3 -- baseline confirmed before dispatching a THIRD round of 3 parallel read-only audit agents (web routes/auth/dead-UI; engines/MCP honesty+determinism+gating; payments/PAI'D coherence), each briefed to re-verify cycles 1-2's fixes AND look further, per the by-now-established 'REST/MCP twin duplication' and 'derived value wired into one consumer but not every consumer' failure patterns. 8 confirmed findings, ALL fixed this cycle. 3 HIGH: (1) prepare_agentic_purchasing's lite-mode response redaction (cycles 1-2) was response-layer only -- get_snapshot/get_artifact (MCP) and the generated-files REST twin check ONLY snapshot/project ownership, zero mode/charge/entitlement awareness, so a lite caller could retrieve the exact pro-program bundle its own response withheld by simply fetching the snapshot afterward, since saveGeneratorResult persisted the FULL bundle unconditionally -- fixed in BOTH runPreparePurchasing (mcp-tool-impls.ts) and handlePreparePurchasing (handlers.ts): persist only free-program files' full content in lite mode, while still scoring off the FULL in-memory generation so the readiness score stays accurate. (2) MCP closer/deploy checked entitlement (isProgramEnabled) but never called ANY charge function at all -- not even present in the MeteredMcpTool type union -- while their REST twins (via makeProgramHandler) charge every call through chargeWithDiscounts; an entitled account got unlimited free closer/deploy runs via MCP -- added both to MeteredMcpTool/METERED_MCP_TOOLS + explicit PRICING_TIERS entries (same 50/25 cents REST's default fallback already charged) and wired the standard authorize/capture pair. (3) Starter<->Pro plan switches delivered via subscription.updated/created (not the initial checkout) never updated accounts.paid_plan_id at all in EITHER the PAI'D webhook (paid-handlers.ts) or the legacy Stripe-direct webhook (stripe.ts) -- both collapse into the coarse 'paid' tier, so the tier-unchanged branch skipped the plan_id write entirely; resolvePlanForAccount would keep metering at whichever plan the account started on indefinitely -- paid-handlers.ts gained buildPricePlanIdMap (mirrors buildPlanTierMap, dormant until PAID_PLAN_STARTER_MONTHLY/ANNUAL env vars are set) so marketedPlanIdForPaidEvent now resolves subscription.updated's own price id; stripe.ts's syncTierFromStripeSubscription now updates paid_plan_id on a same-tier switch too, skipping only the tier-transition machinery (tier write, audit log, upgrade email) since the coarse tier itself didn't move. 2 MED: (4) SEAT_LIMITS was keyed by the coarse BillingTier (paid=5), so every real Pro subscriber hit the same 5-seat cap as Starter despite PLAN_FEATURES publicly advertising Pro at 10 -- new resolveSeatLimit(tier, paidPlanId) derives the Starter/Pro split from PLAN_FEATURES' own 'Team seats' row instead of a fourth hardcoded table, wired into inviteSeat + generateUpgradePrompt (also fixing hardcoded 'Pro seats' copy shown even to Starter accounts) + both funnel.ts REST call sites; deliberately left suite/Growth's UNLIMITED seat behavior untouched (SEAT_LIMITS.suite=-1 is this codebase's existing, tested 'suite=enterprise' behavior -- PLAN_FEATURES' separate 'growth:25' marketing figure would have been a real regression to impose as a hard cap, caught by an existing passing test before shipping). (5) runCodeSandbox/runTextToSpeech/runSpeechToText/runDocumentParsingDispatch all used meterMcpToolCredits (combined authorize+capture, documented for pure local compute only) AROUND genuinely fallible subprocess/network work -- Docker execution, piper TTS, whisper STT, document download+parse -- so credits were checked only AFTER the resource cost was already spent; an insufficient-credit account still got a free container spawn / TTS run / STT run / document fetch before the 402 fired. All four converted to authorize-before/capture-after-success (skipping capture on existing _not_configured/empty-result branches, unchanged); a real ordering bug surfaced mid-fix in runDocumentParsingDispatch (the lite input-size gate read AFTER the old metering call) and was caught by a genuine test failure (not merely introduced-then-fixed) before shipping -- moved the cheap size-gate ahead of authorize so an oversized-input rejection never places a credit hold. 1 LOW: (6) POST /v1/research/crawl's inline LITE_CRAWL_MAX_PAGES literal and the MCP iliad_web_research_crawl tool's LITE_CAPS.CRAWL_MAX_PAGES table entry are two independently-enforced code paths for the same 'lite caps crawls at 5 pages' promise with no structural link -- not a live bug (both were 5), but exported the REST constant and added a guard test pinning the two together so future drift fails CI instead of shipping silently. 1 web-audit finding: (7) AccountDashboardPage.tsx's gradeBadgeClass was one of three copy-pasted implementations (also ProjectsPage.tsx, VersionsTab.tsx) -- the other two treat A+ the same as A (green); this one only matched the literal 'A', so an A+-graded project showed the plain gray default badge here while green everywhere else -- fixed + exported for testability, new dedicated test file. Every fix mutation-verified (revert, confirm the new test goes red -- several via a real assertion failure surfaced by the mutant, one via a genuine network-timeout when the fix was absent -- restore, confirm green); two cross-package stale-dist footguns hit again mid-cycle (packages/mpp and packages/snapshots both needed an explicit pnpm --filter build before a DIFFERENT package's vitest run would see a source edit) and were correctly diagnosed via the same by-now-standard rebuild-first discipline established in cycles 1-2. Live-deploy verification this cycle used CI-green + health-endpoint-200 only (no RENDER_API_KEY loaded in this shell to call Render's own /deploys API directly, per the render-deployment-audit precedent) -- disclosed rather than silently assumed equivalent. dry-count stays 0 (cycle 3 also had findings, not zero) -- cycle 4 is next before Phase A can converge. **Verified:** every fix commit individually typechecked + scoped-tested + CI green (8 separate pushes, 8 separate green CI runs); a final full 13/13-package monorepo build confirmed clean mid-cycle | `58cfd42`+`a7d5fc7`+`28f4184`+`b3b8121`+`92e6897`+`3bca517`+`4668ed8`+`2ffd801` |
| 2026-07-17 | A-cycle4 | Phase A re-audit cycle 4 -- baseline confirmed (full 13/13-package build, git ground-truthed against origin/main, CI green) before dispatching a FOURTH round of 3 parallel read-only audit agents, each briefed with the running list of established failure patterns and explicit instructions to both re-verify cycles 1-3's fixes AND dig further, not just re-confirm a clean pass. 7 confirmed findings, ALL fixed this cycle, plus 2 more explicitly disclosed and left unfixed (see below). 3 HIGH: (1) handleFirecrawlScrape/handleFirecrawlCrawl (POST /v1/research/scrape and /crawl) DOUBLE-CHARGED any account past its monthly snapshot quota -- both handlers charge once in the !quota.allowed branch (an upfront payment gate) but never returned on a SUCCESSFUL charge, so execution fell through into an unconditional second charge after the scrape/crawl completed; the crawl handler compounded this by pre-charging an ESTIMATE (off the requested page limit) then charging the FULL final amount again with no credit for the estimate. No test exercised the !quota.allowed branch for either handler before this cycle. Fixed with a preCharged flag skipping the second charge; the crawl response's own `cost` field was also corrected to report what was actually billed (the pre-charge estimate) instead of a now-misleading $0.00. Mutation-verified via a real credits-delta comparison against a same-priced baseline call (not just 'was some charge levied' -- the mutant produced exactly 2x the baseline credits consumed, confirming true double-charge detection). (2) App.tsx's admin-gate race: privateAccess starts false and only flips true after an async getAdminStats() round-trip resolves, but the admin-gate effect ran in the SAME commit as the resolution effect and read privateAccess=false whether it meant 'resolved, not admin' or 'not yet resolved' -- indistinguishable states. A real admin landing directly on an adminOnly page (#admin, #myanalytics -- bookmark, reload, or any fresh mount) was bounced to #account before the probe had any chance to complete, every single time; the only working path was landing elsewhere first and clicking the Admin sidebar link once it later appeared. Fixed with a privateAccessResolved flag gating the redirect. Notable process moment: the FIRST fix attempt only added the new state variable without actually wiring it into the gate's if-condition -- caught immediately by the new test suite itself (all 3 new tests failed with the exact 'account' vs 'admin' mismatch) BEFORE a deliberate mutation pass was even needed, proving the tests were doing real work, not rubber-stamping. (3) MCP closer/deploy were missing from decideInbandGate's switch (added to MeteredMcpTool in cycle 3, but this separate classification function was never updated), silently falling to the default not_in_scope branch -- once AXIS_MCP_INBAND_SETTLEMENT is enabled (the owner's own documented next step), these two tools would skip in-band cash settlement entirely while every other always-metered tool gets it. The test meant to catch exactly this kind of drift had ITSELF drifted: its Record<MeteredMcpTool,true> comment claimed 'TypeScript enforces exhaustiveness here,' true of the type in isolation, but the object lived in a `.test.ts` file that apps/api/tsconfig.json excludes from the tsc pass CI actually runs, and vitest's esbuild transform strips types without checking -- so a missing key was never a real build error, and closer/deploy/assemble_representment were all silently absent from it. Fixed on two levels: added closer/deploy to decideInbandGate's Phase-1 case group, AND moved the real exhaustiveness guarantee into mcp-runtime.ts itself (a normal source file tsc always checks) as METERED_MCP_TOOL_SET, with METERED_MCP_TOOLS now DERIVED from it instead of a separately-maintained array -- verified by deliberately removing a key from the new Record and confirming tsc actually fails the build this time (it didn't for the old test-file version). 2 MED: (4) UsagePage.tsx's Persistence Credits card showed the raw coarse 'paid' tier in its own 'Tier' stat tile, even though cycle 2 already fixed this exact ambiguity for the page's header sentence via planLabel(tier, planId) -- a second, un-migrated consumer of credits.tier on the SAME page. Fixed by routing it through the same planLabel helper. (5) improve_my_agent_with_axis was advertised as 'Paid ($0.50/run)' with full x_payment 402-negotiation metadata in TWO places on GET /for-agents (the tools[] catalog entry and pricing_table), directly contradicting handleProbeIntent's own correct 'free (uses free-tier programs)' label elsewhere on the SAME endpoint -- runImproveMyAgent never calls any charge function and isn't in MeteredMcpTool, so an agent trusting either wrong surface would wait forever for a 402 that never arrives. Fixed by correcting both surfaces to say free, matching verified runtime behavior (not by adding a new charge -- the code's free behavior was confirmed correct, the docs were wrong, per the Honesty law's 'disclose gaps, don't hide OR fabricate' either direction). 2 items explicitly disclosed, NOT fixed this cycle (deliberate scope calls, not oversights): (a) LOW -- crawlDomain (multi-page crawl) is fully built server-side (POST /v1/research/crawl, api.ts wrapper, CrawlResult/CrawlPage types) but WebResearchPage.tsx only wires up single-page scrapeUrl -- a real half-wired feature, but building the missing UI (new form fields, multi-page result rendering) is a product/UX addition, not a bug fix, matching this session's established precedent (cycle 2 similarly declined to expand UsagePage's upsell banner) of not expanding scope beyond confirmed bugs. (b) LOW/SUSPECTED -- discover_agentic_commerce_tools's marketing copy claims 'Deterministic: same input -> byte-identical output' as a blanket claim; engineer-mode Living Architecture runs local LLM inference with a fixed seed/temperature=0, which is PLAUSIBLE but not PROVEN bit-reproducible, and the audit itself flagged this as unverified (didn't re-run the model to check) rather than confirmed -- given low severity, opt-in/paid-only reachability, and that verifying would require actually re-running local LLM inference twice and diffing output (nontrivial effort for an unconfirmed suspicion), left as a disclosed residual rather than an unverified doc edit. Every FIXED finding mutation-verified (revert or a deliberately-broken variant, confirm the new/existing test goes red with a specific, attributable failure -- not just 'a test failed somewhere' -- then restore, confirm green); one collateral discovery mid-cycle (the admin-gate test catching an incomplete first-pass fix) reinforced that the test-first-then-mutate discipline this session has followed catches real implementation mistakes, not just deliberately-injected ones. dry-count stays 0 (cycle 4 also had findings, not zero) -- cycle 5 is next before Phase A can converge. **Verified:** every fix commit individually typechecked + scoped-tested + CI green (7 separate pushes, 7 separate green CI runs); a final full 13/13-package monorepo build confirmed clean; production health endpoint confirmed stable (200) across 3 retries after one transient 502 mid-rolling-restart, immediately re-checked rather than either panicking or ignoring it | `efc2ebc`+`8daa58c`+`a4aa77a`+`dc70cb1`+`261497e` |
| 2026-07-17 | A-cycle5 | Phase A re-audit cycle 5 -- baseline confirmed (git ground-truthed against origin/main, CI green) before dispatching a FIFTH round of 3 parallel read-only audit agents, each briefed with the full running list of established failure patterns AND two NEW hunting instructions specific to cycle 4's discoveries: (a) hunt for the double-charge shape (a charge branch that doesn't return, falling through into a second charge) beyond the 2 handlers cycle 4 already fixed, and (b) hunt for the false-compile-time-guarantee shape (a Record<Union,X> or array literal claimed to be TS-exhaustive that actually lives in a `.test.ts` file apps/api/tsconfig.json excludes from the tsc pass, or is merely an array TS never checks for exhaustiveness regardless of location) beyond the 1 instance cycle 4 already fixed. 9 confirmed findings, ALL fixed this cycle, plus 1 more explicitly disclosed and left unfixed. 2 HIGH: (1) THREE MORE REST handlers shared cycle 4's exact double-charge shape -- handleCreateSnapshot, handleAnalyze, and handlePreparePurchasing each had a quota-exceeded charge branch AND a separate entitlement/pro-program charge branch with no guard against both firing for the SAME request, silently double-billing any free-tier account that was simultaneously over quota AND requesting a pro program; no test exercised the both-branches-true case for any of the three before this cycle. Fixed with the same preCharged-flag pattern as cycle 4's Firecrawl fix (paidForThisCall, set true after the first successful charge, guarding the second branch); new dedicated snapshot-double-charge.test.ts asserts consumeFreeCall fires exactly once per handler for a request tripping both branches. Mutation-verified individually per handler (removed each guard one at a time via a scripted edit, since duplicate literal guard lines existed across the file, confirming each mutant produced 'expected 1, got 2' before restoring) -- final full regression run 185/185 green. (2) ProgramLauncher.tsx locked every Pro program by default (locked={tier === 'free'}) until the async getAccount() tier probe resolved, meaning any account -- including real Pro subscribers -- saw every Pro card lock/unlock-flicker on load; fixed with a tierResolved boolean gating the locked condition (locked={tierResolved && tier === 'free'}), same async-resolved-state-misread-as-false shape as cycle 4's admin-gate race. Also removed the entirely-unused snapshotId prop (dead plumbing, zero usage in the component body) and its dead pass-through from ProjectPage.tsx. Notable process moment, same class as cycle 4's admin-gate fix: the FIRST fix attempt added the tierResolved state variable but forgot to actually wire it into the locked condition -- caught immediately by all 3 new tests failing (stubDeferredAccountFetch helper controlling exactly when /v1/account resolves) BEFORE a deliberate mutation pass was even performed. 4 MED: (3) mpp/index.ts's 402 response upgrade_path claimed Pro grants 'unlimited full-bundle calls at $99/month' -- false; Pro grants a finite 300,000 monthly credit allowance (MARKETED_TIERS), billed as metered overage via consumeUsageCredits once exhausted -- fixed copy, extended budget-probe.test.ts to assert both the correct '$99/month'+'300,000' substrings AND the ABSENCE of 'unlimited'. (4) App.tsx's #projects/:id deep-link (bookmark, reload, or any fresh mount landing directly on a project route) briefly painted 'Project not found' before the restore-from-cache fetch had a chance to resolve -- restoring started at useState(false) with no way to distinguish 'genuinely not found' from 'haven't checked yet'; fixed with a lazy useState initializer computing the correct STARTING value synchronously from already-known state (PROJECT_DETAIL_PAGES.has(route.page) && the result doesn't already match), avoiding reliance on a subsequent effect-driven repaint entirely. Disclosed limitation, not hidden: the new app-routing.test.tsx test for this fix passes IDENTICALLY whether the fix is present or not, because React Testing Library's act()-wrapped render() synchronously drains the FIRST useEffect pass (including its own pre-await setRestoring(true) call) before returning control to the test -- a real browser's paint-before-effect gap is structurally unobservable via a post-render() assertion in RTL for this specific timing shape (unlike cycle 4's admin-gate bug, which had no synchronously-resolving fetch anywhere in its chain and WAS observable). The fix's correctness rests on code reasoning about React's effect-scheduling semantics, documented as such in both the test's own comment and the commit message, per the Honesty law's 'disclose gaps rather than fabricate a false mutation-verify claim.' (5)-(6) two more instances of cycle 4's false-compile-time-guarantee shape, found per hunting instruction (b): mcp-runtime.ts's MCP_ERROR_CATEGORY_CATALOG was a plain array literal (TS never enforces exhaustiveness on an array regardless of file location) masquerading as exhaustive -- replaced with a real Record<ErrorCategory,{...}> (MCP_ERROR_CATEGORY_SET) in the normal, always-tsc-checked source file, with the exported catalog now DERIVED via Object.keys(); mutation-verified by removing a key and confirming a real TS2741 build error (it wasn't an error before). purchasing-readiness-analysis.ts had the identical shape -- CATEGORY_DEFS was a CategoryDef[] array literal; replaced with CATEGORY_DEF_SET: Record<ReadinessCategory, Omit<CategoryDef,'category'>>, with getReadinessCategories()/getCriticalReadinessCategories() now derived from it; purchasing-readiness-analysis.test.ts's hand-typed ALL_CRITICAL array now imports the derived getCriticalReadinessCategories() instead, and CATEGORY_MARKERS (fixture content, not derivable) gained a new canary test asserting its own key set matches the real exported category list -- both mutation-verified by removing a key and confirming the intended failure (a real tsc error for the source-file case, the canary test for the fixture case). 1 LOW/MED, found per the same hunting instruction sweeping every remaining Record<Union,X>-shaped table in the codebase: lite-caps.test.ts's LITE_CAPPED_TOOLS was a hand-declared array with no structural link to lite-caps.ts's own (private) LITE_RULES table -- a tool added to LITE_RULES with real enforcement could ship completely untested (no compile error, no runtime canary) if the test file's list wasn't separately, manually kept in sync; fixed by exporting LITE_CAPPED_TOOL_NAMES (derived via Object.keys(LITE_RULES)) from the source file and adding a canary test asserting the test's own LITE_CAPPED_TOOLS matches it (LITE_CAPPED_TOOLS itself stays a hand-typed `as const` tuple, needed because OVER_CAP_ARGS requires literal per-tool keys a plain string[] can't provide) -- mutation-verified by removing a LITE_RULES entry and confirming the canary test failed. 1 item explicitly disclosed, NOT fixed this cycle (a genuine unresolved gap, not a scope call): (a) MED-HIGH, SUSPECTED -- paid-handlers.ts's PAI'D webhook writes accounts.paid_plan_id unconditionally on every relevant event (updateAccountPaidPlanId has no compare-and-set/staleness guard, unlike updateAccountTierIfCurrent's real one a few lines above it), and the webhook's own parsed event type carries no created/timestamp field at all -- unlike stripe.ts's legacy webhook, which DOES guard on eventCreated <= existing.last_event_created_at (confirmed via direct code read, not just the audit's claim). A plausible but UNPROVEN failure mode: two webhook deliveries for the same account arriving out of PAI'D's send order (retry, network reorder) could let an OLDER plan_id overwrite a NEWER one. Left disclosed rather than fixed because a safe fix needs PAI'D's actual webhook payload schema (does it carry any ordering-equivalent field?) which is not available in this session -- building an unverified 'fix' risked either not solving the real problem or adding complexity without confidence, matching this session's established precedent (cycle 3's suite-seat-cap decision, cycle 4's Living-Architecture-determinism disclosure) of not shipping speculative fixes for unverified claims. Also swept and disclosed as pre-existing, LOW severity, NOT actioned this cycle (volume + anti-gold-plating law): a long list of unwired REST routes with real handlers/api.ts wrappers but zero UI caller (POST /v1/checkout, POST /v1/research/crawl's crawlDomain -- already disclosed in cycle 4, GET /v1/account/funnel, GET /v1/snapshots/:snapshot_id, GET /v1/snapshots/:id/versions/:n, the singular generated-files variant, GET /v1/credits/packs, listMyCreditPurchases, trackAnalyticsEvent, updateTier, and several more), plus several called-but-click-untested UI actions (WebResearchPage/scrapeUrl has zero dedicated test file; dismissUpgradePrompt/cancelSubscription/deleteGitHubToken/deleteWebhook/getWebhookDeliveries/revokeApiKey/revokeSeat all have real callers but no action-level test coverage) -- these are test-coverage/product-completeness gaps, not confirmed bugs, and the session's precedent (cycle 2's upsell-banner call, cycle 4's crawlDomain UI call) is to disclose rather than backfill or build UI for all of them in one cycle. Every FIXED finding mutation-verified except item (4), whose RTL/act() limitation is explicitly disclosed rather than papered over. Stale-dist footgun hit again for the mpp/index.ts fix (pnpm --filter @axis/mpp build required before/after each mutation cycle, per the established discipline). dry-count stays 0 (cycle 5 also had findings, not zero) -- cycle 6 is next before Phase A can converge. **Verified:** every fix commit individually typechecked + scoped-tested + CI green (7 separate pushes, 7 separate green CI runs); production health endpoint confirmed stable (200) after each deploy | `dd1b065`+`f0c9a6b`+`57a73cd`+`de6fbe6`+`9465d67`+`3cd8cf5`+`fa1a815` |
| 2026-07-17 | A-cycle6 | Phase A re-audit cycle 6 -- baseline confirmed (git ground-truthed against origin/main, CI green) before dispatching a SIXTH round of 3 parallel read-only audit agents, each briefed with the full running list of established failure patterns from cycles 1-5. 7 confirmed findings, ALL fixed this cycle -- notably, 2 of them (RunnerPage/ProgramLauncher drift risk, AccountPage OAuth race) were INITIALLY triaged as low-severity/disclose-not-fix, then fixed anyway after the user directly challenged that call ('is there a reason you did not fix the three deferred? they will flag every cycle') -- a real, useful correction: 'low severity + narrow reachability' had been used as an excuse not to look closer, when in both cases a closer look found either a cheap, real fix (AccountPage) or a clear, honest interim fix distinct from the properly-complete-but-bigger-scope one (RunnerPage). 2 MED: (1) generateUpgradePrompt (packages/snapshots/src/funnel-store.ts) told free-tier users hitting their monthly limit that a Pro upgrade grants 'Team seats (up to 5)' -- that is Starter's real limit; resolveSeatLimit('paid','pro') is 10, a genuinely wrong, currently-live value -- fixed by computing proSeatLimit via the same resolveSeatLimit helper already used elsewhere in the file, mutation-verified (revert to 5, new test goes red with the exact wrong number). The SAME commit also replaced two hardcoded 'All 17 programs' literals with a derived lockedProgramCount matching the adjacent 'engagement' case's own existing dynamic computation -- honestly disclosed as a drift-PREVENTION fix, not a currently-observable-value fix (today's derived and hardcoded values coincide at 17, so this half could not be mutation-verified via output comparison the way the seat count could). (2) discover_commerce_tools's catalog (apps/api/src/mcp-tool-impls.ts's runDiscoverAgenticCommerceTools, surfaced via GET /for-agents and the MCP tool itself) had two separate agent-facing honesty gaps fixed together: auth_required was computed as simply '!free', conflating 'no charge' with 'no auth needed' -- get_referral_code, get_referral_credits, and iliad_network_tokenization are all free but their own handlers throw 'Authentication required' for an anonymous caller, so an agent trusting the catalog to plan its integration would call these anonymously and hit an error the catalog gave no reason to expect; fixed via a new FREE_TOOLS_REQUIRING_AUTH set. handlers.ts's GET /for-agents also hardcoded a literal '12' free-tool count (custom_swarm manifest + pricing_table overview) that had drifted stale after WO-13/WO-14/x402 additions marked more tools free -- real count is 14; fixed via a new exported FREE_MCP_TOOL_COUNT, derived from the SAME real-MCP_TOOLS-registration filter runDiscoverAgenticCommerceTools itself already used, so both surfaces can no longer drift independently. Both mutation-verified. 5 LOW: (3) MyAnalyticsPage.tsx's load() (shared by the days-change effect AND the Refresh button) had no guard against an OLDER in-flight request's response landing after a newer one and overwriting it under a rapid Window re-select -- same class of bug UsagePage.tsx's proration-preview effect already guards against with a local cancelled flag; fixed via a request-generation ref since load() here is shared by two different triggers instead of owned by a single effect. The Window select and Refresh button are both disabled while loading (blocking this through real UI interaction in the common case), so the new test deliberately bypasses that via fireEvent to prove the requestId guard is a genuine second layer of defense; mutation-verified (removing the guard reproduces the exact stale-value overwrite). (4) persistence-credits balance_after had an unlocked race in TWO functions -- addPersistenceCredits and credit-pack-store.ts's markPurchaseSucceeded both computed their ledger row's balance_after via a read-then-write SUM with no advisory lock, unlike the sibling debit (meterPersistenceOp) and grant (applySuiteMonthlyGrant) paths in the SAME FILE, both of which already take pg_advisory_xact_lock(1,...) for exactly this reason -- two concurrent grants for the same account (e.g. two credit-pack webhook deliveries close together) could each read the same pre-grant SUM and stamp an inconsistent balance_after. NOT a fund-safety issue (every real balance read always recomputes a live SUM, never trusts this denormalized column) -- fixed for consistency with the file's own established pattern anyway, so every persistence_credits balance mutation for an account is now serialized regardless of which function issued it. Both call sites mutation-verified with a warm-pool concurrency test matching the file's own established meterPersistenceOp-concurrency pattern (removing either lock reproduces a genuine lost-update -- duplicate balance_after values, or a stale 100 instead of 200). (5) RunnerPage.tsx's Run button went silently disabled with ZERO explanation for a program the live GET /v1/programs catalog reports but PROGRAM_DEFS (a hand-maintained list in components/ProgramLauncher.tsx) does not yet know about -- canRun requires programMeta because it carries the run endpoint, which the catalog itself does not expose, so a future catalog/PROGRAM_DEFS drift would present as a dead end with neither the 'pick a project' nor 'no snapshot yet' hint applying. Not live today (PROGRAM_DEFS.length === PROGRAM_COUNT === 20), and a FULLY correct fix needs the catalog itself to expose each program's endpoint (a backend contract change, bigger scope than a client patch) -- shipped an honest interim message instead of a silent disabled state, mutation-verified. (6) AccountPage.tsx had a real same-commit effect-ordering race: isLoggedIn (a stale axis_api_key marker) and exchanging (state, only true once the exchange effect's own setExchanging(true) lands) drove two separate effects that both fire in the SAME commit -- a state update scheduled by one effect is not visible to a SIBLING effect in that same pass, only a synchronous read is -- so an already-logged-in visit landing on a fresh #account?code=...&login=... callback (e.g. re-clicking an old OAuth link, or re-linking a provider while signed in) could redirect to Settings before the exchange had a chance to even start. Fixed by computing hasPendingOAuthCode directly from window.location in the render body (no state/effect round-trip needed at all) and gating both the redirect effect AND the render-time early return on it -- closes the race for the FIRST paint too, not just the effect-ordering gap, UNLIKE cycle 5's disclosed RTL/act()-untestable App.tsx deep-link fix, this one genuinely mutation-verifies clean since it depends on effect-ordering (which RTL's act() fully resolves and exposes), not paint-vs-effect timing (which RTL's act() masks) -- removing the guard reproduces onNavigate('settings') firing while the exchange is still pending. 1 item carried forward from cycle 5, still disclosed and NOT fixed (new evidence found, but the gap does not close): paid-handlers.ts's PAI'D webhook still writes accounts.paid_plan_id with no compare-and-set guard and the webhook's own parsed event type still carries no created/timestamp field. New evidence this cycle: packages/paid-client/src/index.ts's verifyPaidWebhookSignature DOES parse a 't=<unix>' timestamp out of the Webhook-Signature/PAID-Signature header (used today only for a +/-5min freshness check, then discarded) -- this is the one piece of ordering-relevant data confirmed to exist on PAI'D's real wire format, but it is the SIGNING/delivery timestamp, not a business-event created timestamp, so a redelivery of an OLD event would likely get a FRESH t at resend time -- it narrows the ambiguity (rules out a >5-minute-old cross-event race) but does not fully close the gap the disclosure describes; recommended surfacing it as a partial mitigation for a human to evaluate rather than building an incomplete fix that could look more solved than it is. Also swept: (a) new SUSPECTED, low-severity, non-actioned finding -- payments audit found funnel-store.ts's generateUpgradePrompt itself is the SAME function fixed above, so no separate action; (b) new evidence, not a fresh bug -- the engines/MCP audit re-examined cycle 4's disclosed-but-unconfirmed Living Architecture determinism claim and found STRONGER evidence FOR it (temperature:0 and a real seed genuinely thread through node-llama-cpp's session.prompt call end-to-end, not just declared) but also a residual, unmitigated gap (thread count is never pinned when loading the model, and GGML's default multi-threaded CPU matmul does not guarantee bit-exact floating-point reduction order across runs) -- net: better-engineered than the disclosed concern implied, but the 'byte-identical' wording is still not a fully proven guarantee; left as a refined disclosure, not re-flagged as a confirmed bug. dry-count stays 0 (cycle 6 also had findings, not zero) -- cycle 7 is next before Phase A can converge. **Verified:** every fix commit individually typechecked + scoped-tested + CI green (6 separate pushes, 6 separate green CI runs); production health endpoint confirmed stable (200) after the final push | `76291b3`+`32fe950`+`2802c10`+`3cd48dd`+`2638f3a`+`6cd1ff9` |
| 2026-07-17 | A-cycle7 | Phase A re-audit cycle 7 -- baseline confirmed (git ground-truthed against origin/main, CI green) before dispatching a SEVENTH round of 3 parallel read-only audit agents, each briefed with the full running list of established failure patterns from cycles 1-6 AND an explicit process-correction instruction derived from cycle 6's own close: cycle 6 found 2 LOW findings the executor initially triaged as 'low severity, disclose and move on,' the user directly challenged that call ('is there a reason you did not fix the three deferred? they will flag every cycle'), and BOTH turned out to be cheaply, correctly fixable on a closer look -- each cycle-7 agent was told to explicitly flag, for every finding, whether a cheap contained fix exists for the concrete symptom even if a 'complete' fix needs more scope, rather than reflexively downgrading LOW findings. 6 confirmed findings, ALL fixed this cycle -- 2 of them HIGH and genuinely serious, the most severe findings this entire loop has surfaced. HIGH #1: improve_my_agent_with_axis (apps/api/src/mcp-tool-impls.ts's runImproveMyAgent) gave away the PAID $25 'engineer mode' AI Design Judge verdict and Living Architecture LLM pass for FREE to any authenticated account, including a brand-new zero-entitlement free account -- maybeAppendLivingArchitecture/maybeRunQualityGate (shared by 4 call sites) gate their engineer-mode enrichment on resolveAgentMode(req) alone with NO entitlement/charge check inside either helper; the other 3 call sites are safe only because THOSE call sites charge/gate before calling them, but runImproveMyAgent never charges anything (by design, always free-tier-only) and forwarded the real req straight through -- any caller sending X-Agent-Mode: engineer got the paid tier's premium verdict for $0, defeating the paid-tier gate entirely, not just the price. A stale cycle-4 comment on this exact call site had asserted 'always runs free-tier programs only, for any mode, for free' -- true when written, false once the engineer-mode branch was wired into the shared helpers without a corresponding gate at this one call site. Fixed by passing a header-only request override (X-Agent-Mode forced to standard) to both helpers from this call site only -- req isn't read for anything else below that point in the function. Mutation-verified: reverting one of the two calls reproduces a living-architecture artifact appearing in the response for a zero-entitlement free account. HIGH #2: no cancellation/modification path exists ANYWHERE in this codebase for a real PAI'D subscriber -- PAI'D (the only live checkout path) never writes stripe_subscriptions, and GET/POST /v1/account/subscription both read exclusively from that table, so has_active_subscription is always false for every real paying customer; there is also no PAI'D-side cancel/modify API call anywhere in packages/paid-client. Compounding this, PlansPage.tsx and QAPage.tsx BOTH claimed self-serve cancel/downgrade works ('Cancel any time.', 'Change your plan at any time... you keep Pro access until then') -- false for the entire live customer base -- and the sanctioned 'switch plans' UI flow (PlansPage -> a fresh PAI'D checkout, with no check for an existing subscription) actually STACKS A SECOND, SEPARATE SUBSCRIPTION rather than replacing the first, with nothing in this codebase ever able to cancel it afterward -- a genuine double-billing risk on the platform's only live checkout rail, not a cosmetic gap. A fully complete fix needs PAI'D API cancel/modify capability this loop does not currently have, correctly staying out of scope -- but three concrete, currently-live symptoms were cheaply fixable without touching PAI'D integration code at all: (1) corrected both false claims to point at a real support contact instead; (2) added a warning banner on PlansPage.tsx (best-effort account-tier read, never blocks the button) before a plan-switch checkout when the account is already on a paid tier; (3) UsagePage.tsx's ENTIRE Subscription card (including the only Cancel button in the app) used to silently render nothing for account.tier !== 'free' whenever has_active_subscription was false -- which, per the root cause, is EVERY real subscriber -- added a fallback card pointing to support instead of showing nothing. All three mutation-verified. 4 MED/LOW, two of which are the SAME async-resolved-state-misread-as-false / same-commit effect-ordering shape recurring for the 5th+6th time this session (App.tsx x2, ProgramLauncher.tsx, MyAnalyticsPage.tsx, AccountPage.tsx, now routes.tsx): (3) MED -- cycle 4 fixed the REDIRECT half of the admin-gate race (App.tsx's own effect waits for privateAccessResolved) but routes.tsx's render functions for #admin/#myanalytics never learned about that same flag -- NavContext only carried {loggedIn, privateAccess, hasResult}, so render() had no way to distinguish 'still probing' from 'confirmed not admin' and both rendered null; a real admin bookmarking #admin and reloading saw a COMPLETELY BLANK content pane (no spinner, no skeleton) for the full probe round-trip -- the existing 'Admin gate race' test suite only ever asserted on shellPage (a route-identity attribute set independently of render()'s return value), never on actual DOM content, so this gap went untested for 3 cycles. Fixed by threading privateAccessResolved through NavContext/navCtx (the same path privateAccess already takes) and changing both render ternaries to a three-way check (not-yet-resolved -> loading indicator, resolved+privateAccess -> real page, else -> null); mutation-verified (reverting one route's render function reproduces zero role=status elements in the DOM during the pending window). (4) MED -- growth-store.ts's active_subscriptions counted stripe_subscriptions rows only -- the SAME root cause estimated_mrr_cents was already fixed for in cycle 2, in this same file -- so it read persistently near-zero even while accounts/paid and estimated_mrr_cents correctly showed real revenue right next to it, an internally inconsistent snapshot (nonzero MRR, '0 active subscriptions'). Fixed by deriving it from the SAME tier-count query already computed a few lines above (paid + suite) instead of a separate PAI'D-blind query -- no extra query needed at all; mutation-verified. (5) LOW-MED -- inviteSeat (packages/snapshots/src/funnel-store.ts) had an unlocked check-then-insert race on the seat-limit gate -- the seat-billing analog of the balance-race pattern already fixed elsewhere in this codebase (credit-pack-store.ts, persistence-metering.ts) via pg_advisory_xact_lock -- fixed with the same per-account advisory lock pattern (namespace 2, distinct from persistence_credits' namespace 1); mutation-verified with a warm-pool concurrency test matching the codebase's own established meterPersistenceOp-concurrency pattern (removing the lock lets a 10-invite burst against a 5-seat limit insert all 10). (6) LOW/MED -- destructiveHint was hardcoded false for EVERY MCP tool via toolAnnotations(), including iliad_object_storage's engineer-mode delete (permanently removes an R2 object) and iliad_web_search's delete/delete_namespace (the latter wipes an entire namespace's indexed corpus in one call) -- misrepresenting the real risk to any client/orchestrator using this hint to decide whether to require confirmation. Fixed via an optional destructive parameter (default false, ~38 other call sites unaffected); mutation-verified. 1 item carried forward from cycle 5/6, still disclosed and NOT fixed -- re-verified still open, no new evidence found this cycle (payments audit confirmed the same partial-timestamp-only mitigation from cycle 6 remains the best available lead; not re-flagged further per the standing instruction to avoid repeat disclosure of an unchanged item). One transient flake encountered and correctly diagnosed, not chased as a regression: funnel.test.ts's 'returns undefined for account with no events' (an UNRELATED pre-existing test, touching neither seats nor growth-store) timed out once during the full concurrency-suite run and passed clean on an isolated 46/46 re-run -- confirmed as local Postgres/Docker contention from the file's own new warm-pool burst tests, not a code regression, matching this session's established local-full-suite-unreliability precedent. dry-count stays 0 (cycle 7 also had findings, not zero) -- cycle 8 is next before Phase A can converge. **Verified:** every fix commit individually typechecked + scoped-tested + CI green (6 separate pushes, 6 separate green CI runs); a final full 13/13-package monorepo build confirmed clean; production health endpoint confirmed stable (200) across 3 retries after the final push | `cc287ce`+`449dd56`+`5f8e185`+`f9616bb`+`b79fd55`+`15bf1ca` |
| 2026-07-17 | A-cycle8 | Phase A re-audit cycle 8 -- baseline confirmed (git ground-truthed against origin/main, CI green) before dispatching an EIGHTH round of 3 parallel read-only audit agents, each briefed with the full running list of established failure patterns from cycles 1-7 AND the fix-cost-reasoning process correction cycles 6-7 validated twice already. 7 confirmed findings, ALL fixed this cycle. 1 HIGH: GET /for-agents' allTools array (handleForAgents, the agent onboarding manifest this repo's own CLAUDE.md points agents to) hand-lists a SECOND, separate tool catalog from the real 37-tool MCP_TOOLS registration -- it had drifted to cover only 14 tools, missing all 13 WO-11 AXIS-owned tools, closer/deploy/ping_payment/prepare_agentic_purchasing_preview, the 5 WO-13 commerce engines, assemble_representment, and iliad_network_tokenization -- the SAME hand-duplicated-catalog-drift shape cycle 6 already fixed once for this endpoint's free-tool COUNT, just one layer deeper (array membership, not a single field) and undetected for 2 more cycles because those cycles' fixes touched values on the EXISTING 14 entries without ever re-deriving the array's own membership. discover_agentic_purchasing_needs' intent-routing rules (same file) already correctly knew about and recommended every missing tool, confirming the gap was isolated to this one hand-maintained array, not a data problem. Fixed by extracting runDiscoverAgenticCommerceTools's own derivation logic (auth_required/pricing from the real MCP_TOOLS/FREE_TOOL_NAMES/METERED_MCP_TOOLS/PRICING_TIERS sources) into a shared exported deriveMcpToolCatalog(), then having handleForAgents append the 23 missing tools derived from it -- the 14 hand-curated entries keep their richer prose descriptions (better for the endpoint's own intent-scoring) unchanged, so this isn't a full rewrite, just closing the membership gap at its root. Same commit, same discovery surface: iliad_web_research_crawl's entry (GET /for-agents AND the OpenAPI /v1/research/crawl block) advertised '$0.25/$0.12 per page crawled' -- stale by 12-25x since WO-12 replaced Firecrawl's per-page pricing with a flat 1c/call, the SAME real price the handler bills via getPricingTier; mcp-tools.ts's own tool description already had this right, only the two doc surfaces needed correcting. Both mutation-verified. 2 MED, both real money-adjacent bugs found via a fresh open-ended sweep of money-path code not yet touched by any prior cycle's audit: (2) charge.refunded was never in HANDLED_EVENTS on either webhook (Stripe direct or PAI'D) -- if the owner processes a refund for a subscription payment or a one-shot credit-pack top-up, for ANY reason including the legally-required case TermsPage.tsx's own copy already anticipates, nothing in AXIS's database ever learns about it: the customer keeps the tier, the persistence credits, AND the money back, indefinitely. A full automatic clawback (revoke consumed credits? downgrade mid-cycle? handle partial refunds differently?) is a genuine product/policy decision correctly left disclosed-not-built -- but the fix makes a refund OBSERVABLE instead of a silent 200 no-op (a structured log line using only the fields a charge.refunded event actually and reliably carries), which is NOT a policy decision and had no reason to wait on one. (3) compensator.ts's claim-then-grant wasn't atomic -- claimCompensationForCredit's at-most-once claim (owed -> credited) commits independently of the actual usage-credit grant that follows it; if grantUsageCredits throws afterward (a transient DB hiccup -- exactly the failure class this whole compensation system exists to survive), the entry is PERMANENTLY stuck 'credited' with no credit ever granted, and the at-most-once guard (WHERE status='owed') means it can never be reclaimed -- the ledger's own accounting would falsely show the customer as made whole when they never were. This is the SAME atomicity bug already found and fixed once in this codebase for the structurally identical credit-pack-store.ts pattern (cycle 6), and a direct deviation from this feature's own design doc's stated 'retried with backoff, idempotent, progress recorded' contract. Fixed with revertCompensationClaim: on a failed grant, flip the entry back to 'owed' so the next lazy sweep retries it instead of abandoning it -- the existing 'grant failure doesn't stop the sweep' test's own PRIOR assertion (owed_cents===0 after a failed grant) was itself evidence of the bug, now corrected to assert the entry is still owed; a new direct test proves a reverted entry is genuinely re-claimable. Self-caught while touching this area (not from the audit): cycle 7's inviteSeat fix had picked advisory-lock namespace 2 without checking it was already claimed by grantUsageCredits -- harmless (same key space, not a correctness bug) but would have needlessly serialized seat invites against unrelated credit grants; moved to namespace 4. 2 LOW: (4) AdminPage.tsx's loadAdminData (triggered by both mount AND the Refresh button, same dual-trigger shape MyAnalyticsPage.tsx had before its cycle-6 fix) had no stale-response guard -- the real app mounts under StrictMode (main.tsx), which double-invokes a no-cleanup mount effect exactly once in development, a genuinely reachable trigger for two overlapping loads on the same instance (the Refresh button itself can't race a second click here, since AdminPage swaps its whole tree for a loading skeleton during any load, hiding the button) -- fixed with the same requestId-ref pattern already proven in MyAnalyticsPage.tsx, mutation-verified via a StrictMode-wrapped render with a deferred first response. (5) added api.ts wrappers (no UI) for GET /v1/billing/history and GET /v1/account/fleet -- the latter is a paid/suite-tier-gated 'fleet intelligence' feature with ZERO web references anywhere, so a Pro/Growth customer paying for tier access literally cannot discover or use a feature their subscription is supposed to unlock; wrapper-only fix, a rendered billing-history table and a real FleetPage.tsx are both genuine product/UX additions correctly disclosed as separate tracked follow-up work rather than freehanded into this cycle. 3 items explicitly disclosed, NOT fixed this cycle, each with the required fix-cost justification rather than a bare 'low severity': (a) MED, genuine architecture decision -- REST /v1/research/scrape and /v1/research/crawl never got migrated to WO-12's sovereign-crawler default; they unconditionally proxy to Firecrawl and 503 if FIRECRAWL_API_KEY is unset, while the MCP tools of the same name default to the no-key-required sovereign path -- an operator running the documented sovereign-default deployment finds the MCP tool works but the REST twin of the 'same' capability always fails. A correct migration needs to reconcile Firecrawl-specific free-scrape-pool accounting and 24h shared-cache logic with the sovereign path and touches ~15 Firecrawl-specific REST tests -- a real scope/architecture call (should REST proxy to the same runWebResearch/runWebResearchCrawl functions MCP uses, or is REST intentionally staying a legacy Firecrawl-only path?), not a narrow patch. (b) LOW, structurally blocked -- decideInbandGate's switch takes tool:string rather than the MeteredMcpTool union, so TS doesn't compile-time-enforce every metered tool has a case; NOT the same unguarded gap cycle 4 found (a real, exhaustively-typed METERED_MCP_TOOLS-derived runtime test already catches a silently-added tool via a total-classification invariant) -- disclosed because the existing runtime backstop is real, current, and passing, not because the gap is trivial; a satisfies-based dispatch table would upgrade this from test-catches-it to tsc-catches-it, a real future improvement. (c) LOW, justified by genuine absence of user need -- GET /v1/search/:snapshot_id/stats (search-index health/symbol counts) has no wrapper/UI either, but SearchTab.tsx already surfaces search functionality without needing index stats -- disclose-and-skip here is a real product-scope read (no identified need), not a shortcut. 1 item carried forward from cycle 5, still disclosed and NOT fixed -- re-verified still open, no new evidence found this cycle. One session-continuity incident handled correctly mid-cycle, not a code issue: the harness restarted between pushing commit 3/5 and commit 4/5, breaking the Bash tool's PATH resolution for git/gh/grep entirely (full-path invocation and PowerShell both still worked) and losing the in-flight CI-watch background task's own completion record -- rather than assuming the interrupted watcher meant something had failed, re-verified the actual CI conclusion directly via a fresh gh call (green) before proceeding, and switched to PowerShell for the remainder of the cycle's git/gh operations. dry-count stays 0 (cycle 8 also had findings, not zero) -- cycle 9 is next before Phase A can converge. **Verified:** every fix commit individually typechecked + scoped-tested + CI green (5 separate pushes, 5 separate green CI runs, one re-confirmed directly after a session restart interrupted its watcher rather than assumed); a final full 13/13-package monorepo build confirmed clean; production health endpoint confirmed stable (200) across 3 retries after the final push | `f707db1`+`f047bd5`+`39d5a4f`+`ed8741a`+`9295b9b` |

## FAILURES ledger (rule 14 — append-only; a halted unit is a data point, not a shame)

| Date | Unit | What was attempted | Why halted | Disposition (re-scoped / escalated / owner) |
|---|---|---|---|---|
| — | — | — | — | — |
